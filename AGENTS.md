# AGENTS.md — 高中数学二级结论 PDF 完善执行规范

版本：2.0  
生效日期：2026-08-29

本文件是仓库根目录的执行规则。目标不是“增加文字和页数”，而是把每个二级结论做成数学正确、图像直觉、可编译、可逐页验收的教学 PDF。

## 0. 最高优先级：尽可能使用有效教学图

本项目默认采用“图像优先、文字辅助”的教学表达方式。只要一幅准确的图能够明显缩短理解时间，就必须优先画图，不能让关键含义只停留在文字或公式中。

1. 宁可多图，也不要让位置、变化、对应、比较、边界、分布、构造和因果关系只能靠文字脑补。
2. 宁可增加页数、拆分页面或减少重复文字，也不要压缩正文、公式和图内标签的字号。
3. 图必须直接呈现数学对象及其关系，能够替代或明显缩短文字；把公式放入彩色框、增加箭头或重新排版，不算数学可视化。
4. 数学图必须精确。优先使用 TikZ、PGFPlots、程序计算或项目现有的矢量绘图方式；生成式图片不得用于最终的函数曲线、坐标、几何比例、统计数据、概率分布和数量对应。
5. 必须保留现有 PDF 的品牌风格、页眉、页脚、水印、颜色体系、章节结构和同一数学对象的视觉编码，除非用户明确要求重设计。
6. 配图数量不以“达到最低张数”为目标，而以“独立认知障碍是否全部获得视觉表达”为判断标准。
7. 一个结论存在多个不同认知卡点时，原则上分别作图。只有这些卡点使用同一对象、同一尺度并且合并后仍能通过“5秒测试”时，才允许组合成多面板图。
8. 不得为了减少页数，把多个独立含义塞入一幅拥挤总图；图中文字需要缩小才能容纳时，必须拆图或增加页面。
9. 有价值的图数量不设上限。只禁止表达完全相同含义的重复图、装饰图和不能承担理解任务的伪图。
10. 若存在能够明显加快理解的图形方案而最终没有实现，该结论不得判定为“已完善”。

## 1. 本轮工作边界

- 一次只处理用户指定的一个二级结论 ID。完成该 ID 后立即停止，不自动继续下一条。
- 不启动子 Agent，不并行委派，不建议或自动切换模型；用户固定使用一个模型。
- 不做全仓库扫描、批量重写、发布、部署、Git 提交或服务器修改，除非用户明确要求。
- 修改前先检查当前结论目录和工作树，保留用户已有改动；不得顺手修改无关结论。
- 若目标 ID、目录或任务类型不明确，只问一个必要问题；其余情况直接执行。
- 回复和过程记录保持简短，把额度用于实际编辑、制图、编译和验收。

## 2. 完成的硬性定义

只有以下条件全部满足，才可报告“已完善”：

1. 条件、变量、公式、边界、等号情形和推导均正确。
2. 六段正文职责清楚，例题、误区和总结不是前文重复。
3. 至少有一幅真正承担理解任务的主教学图。
4. 所有命中“必须配图”的认知障碍均有对应图形，不得只靠文字描述。
5. 主教学图替代或明显缩短了原有文字，而不是附加装饰。
6. PDF 已成功编译并逐页渲染检查。
7. 图、公式、文字、箭头、坐标和边框零重叠、零裁切、零压线。
8. meta.json 与最终正文、图形和更新时间一致。

“编译成功”“文件齐全”“页数增加”“增加彩色框”都不等于完成。缺少合格主教学图时，必须报告“未完成”，不得使用“基本完成”“已优化”等模糊表述。

## 3. 正式内容源

每个结论目录通常包含：

    <module>/<ID_slug>/
    ├─ 01_statement.tex
    ├─ 02_explanation.tex
    ├─ 03_proof.tex
    ├─ 04_examples.tex
    ├─ 05_traps.tex
    ├─ 06_summary.tex
    ├─ meta.json
    ├─ main.tex
    └─ source.tex

独立图形放在：

    assets/tikz/<ID>_<descriptive_name>.tex

使用 \input 引入。文件名必须包含结论 ID 和可读的英文主题名。

- 01—06、meta.json 与引用的 TikZ/PGFPlots 资源是正式内容。
- 12_pipeline/generated_sources/ 是生成产物，不作为人工精修源。
- 12_pipeline/input/<ID>/source.tex 是输入材料，不得覆盖已经精修的六段正文。
- 不通过直接修改生成产物来掩盖源文件问题。

## 4. 六段正文职责

### 01_statement.tex：结论

只回答“结论是什么、在什么条件下成立”：

- 定义变量、参数、定义域和取值范围；
- 给出核心公式、等价表述、方向限制和等号条件；
- 区分充分、必要、充要、恒等与近似；
- 不塞入长证明、重复解释或大量例题。

### 02_explanation.tex：直觉理解

这是主教学图的默认落点，必须遵循：

具体对象或代表数值 → 操作/变化 → 可见结果 → 一般符号与公式。

- 先设计主图，再围绕主图写短解释。
- 原则上至少放一幅主教学图。
- 图后只保留关键观察、必要条件以及从具体情形推广到一般公式的一步。
- 不得先写满页面，再把图片缩进角落。

### 03_proof.tex：证明或推导

- 明确前提、定义和所用定理；
- 推导不得省略决定结论成立的关键步骤；
- 不用“显然”“易知”跳过学生真正不明白的部分；
- 分类讨论必须覆盖全部分支并解释分界；
- 证明末尾回扣原结论；
- 近似值注明来源、精度和舍入位置；
- 不得循环论证。

### 04_examples.tex：典型例题

通常 1—3 题，每题承担不同任务：

- 直接应用；
- 有价值的变式或逆向使用；
- 易混、边界或实际模型。

每题包含识别依据、完整推理、答案和回验。不得只换数字。若识别模型或推理过程需要脑补结构，至少一题配图。

### 05_traps.tex：易错提醒

每个误区必须形成：

错误做法 → 为什么错 → 正确替代。

若错误来自两个相似模型、图形状态或公式，必须优先画“错误模型 vs 正确模型”的同尺度对照图，并直接标出真正不同的结构；红叉、绿勾和两块文字框不算对照图。

### 06_summary.tex：复盘

只保留：

- 一句话核心；
- 2—4 条速查公式或等价关系；
- 必要的判断流程；
- 3—5 条最后检查。

不得复制整段前文，不得新增未经解释的新结论。

## 5. 哪些内容必须有图

出现下列任一情况，就必须设计对应的主教学图：

1. 抽象参数要与具体对象建立对应；
2. 需要想象位置、形状、方向、尺度、面积、斜率、轨迹或运动；
3. 数量随参数、步骤、状态或时间变化；
4. 两个公式相似，但机制、分布或适用条件不同；
5. 结论来自最大值、最小值、端点、极端配置或退化情形；
6. 涉及随机抽样、概率分布、频率变化或长期平均；
7. 证明依赖构造、分割、拼接、展开、投影、对称或对应；
8. 常见错误来自文字相似但结构不同的模型。

配图数量按认知障碍决定：

配图数量不按固定张数配额决定，也不以达到最低数量为完成标准：

* 每个独立认知障碍原则上必须有对应的视觉表达。
* 同一幅图可以解决多个高度相关的问题，但必须使用同一对象和同一尺度，而且各个关系都能在5秒内被识别。
* 若合并后需要缩小字号、增加远距离图例、堆叠大量公式或先阅读长段文字才能看懂，必须拆成多幅图。
* 图的数量不预设上限；只要新增图承担了新的理解任务，就应保留。
* 不得因为现有页面已经排满、预计页数增加或编译工作量增加而放弃关键图片。
* 空间不足时依次选择：删除重复文字、增加页面、拆分章节、拆分图形；不得缩小正文或主图解决。
* 交付前必须逐项检查：是否仍有能够被图像直接表达，却只能依靠文字脑补的关键含义。只要存在，此结论就仍未完成。


纯符号运算若确实不存在对象映射、过程变化、空间关系、模型比较、随机分布和边界结构，才可申请无图例外。无图例外不能由 Agent 自行放行，必须在交付前向用户说明具体理由并获得用户确认。

## 6. 什么才算主教学图

一幅合格主教学图必须让学生直接看见至少一种关系：

- 对象和数量如何对应；
- 操作前后发生了什么；
- 两种模型的结构差异；
- 参数改变时什么变、什么不变；
- 最大值、最小值或边界从哪里来；
- 随机结果如何波动并形成长期平均；
- 证明中的构造、分解、投影、对称或一一对应。

以下内容一律不计入主教学图：

- 把正文或公式复制进彩色方框；
- 只有公式、箭头、流程节点和装饰边框；
- 用流程图复述一段正文；
- 只有图标、人物或无关插画；
- 公式截图、页面截图、未经重构的教材截图；
- 图放在文字旁边，但不承担任何文字表达；
- 必须先读完长段说明才能看懂的图。

流程图只适合真实的步骤、决策和分支；它不是数学可视化的默认答案。

## 7. 图必须真正代替文字

编辑前先形成不超过 6 行的“视觉计划”：

| 图形任务 | 学生卡点 | 必须看见的关系 | 图形类型 | 将替代/缩短的文字 | 正确性检查点 |
| -------- | -------- | -------------- | -------- | ----------------- | ------------ |

随后直接实现，不因视觉计划再次等待用户确认，除非数学含义存在歧义。

每幅主图必须满足：

1. 一图一主要任务；超过三个独立任务时拆图。
2. 标题和图内标签直接说明对象，不依赖远处图例。
3. 通过“5 秒测试”：只看图、标题和直接标签，能说出一个核心关系。
4. 明确指出它替代或缩短了哪段文字。
5. 图完成后删除重复说明，只保留观察引导、必要条件和推广步骤。

若加入图片后原段落仍需原样保留，该图大概率只是装饰，必须重画或删除。

## 8. 认知问题与图形类型

| 学生需要理解什么 | 优先图形                           |
| ---------------- | ---------------------------------- |
| 参数与对象对应   | 具体对象图、直接标注图             |
| 操作或状态变化   | 分步骤连续图、前后对照图           |
| 函数参数变化     | 同坐标、同尺度控制变量图           |
| 两种模型易混     | 左右并列、同尺度比较图             |
| 最大/最小/端点   | 极端配置图、端点图                 |
| 概率与期望       | 抽样图、点图、直方图、分布图       |
| 几何公式来源     | 分割、展开、拼接、投影、面积对应图 |
| 证明中的关系     | 构造图、映射图、局部高亮图         |
| 易错点           | 错误模型与正确模型并列图           |

连续变化优先使用函数图或连续状态图；随机性优先使用抽样图、点图、直方图或分布图；几何关系优先使用构造、分割、展开、拼接或投影图。

## 9. 图形正确性与视觉规范

- 精确数学图使用 TikZ、PGFPlots 或程序计算生成的矢量图。
- AI 生图只能做风格草案，不得直接生成最终公式、坐标曲线、概率柱、对象数量或需要精确对应的数学图。
- 函数、概率和统计图必须来自真实公式或数据，不凭感觉手画。
- 控制变量图每次只改变一个关键量，通常选 3 个代表值；明确标出变化量、固定量和不变量。
- 比较图必须使用相同坐标范围、刻度和归一化方式；若不同，必须醒目标注。
- 同一数学对象在整份 PDF 中保持相同颜色、线型和形状编码。
- 三条及以上曲线使用跨色相配色，并辅以实线、虚线、点线或标记点；不得只靠同色系深浅区分。
- 若长度、面积、斜率或高度承担数学含义，不得为美观任意改变比例。
- 示意图不按比例时必须标注“示意”，且不得用失真比例支持结论。
- 主图通常占所在页有效内容区域的 35%—55%，宽度通常不小于 0.70\linewidth。
- 主标签原则上使用 \small 或更大字号；\scriptsize 仅限辅助标注；禁止使用 \tiny 救场。
- 文字实际包围盒到边框至少约 3 mm；主体到标题至少约 4 mm；相邻节点至少约 6 mm；含箭头标签时至少约 10 mm。
- 箭头不得穿过文字、公式、节点或其他标签；交叉超过一次时重排结构。
- 空间不足时依次选择：删重复文字、增加页面、拆图、重排；不得缩小主图或正文解决。

## 10. 数学硬检查

逐项核对：

- 变量首次出现是否定义；
- 定义域、参数范围、正负性和非零条件；
- 单位或量纲是否一致；
- 严格/非严格不等号、区间开闭和端点；
- 等号成立条件；
- 任意、存在、唯一等量词；
- 精确等式与近似等式；
- 特殊值、极端与退化情形；
- 例题能否代回或用另一方法验证；
- 图中的对象数量、位置、颜色、坐标、比例和标签是否与正文一致。

数学错误优先于美化。先修内容，再制图和排版。

## 11. meta.json 同步

完成正文后复核：

- id、模块、标题与目录一致；
- core.title、core.summary 与 01_statement.tex 一致；
- 公式、变量、条件、结论和关键词与正文一致；
- interactive.has_diagram 只在存在真正数学图时设为有图；
- remarks 记录主图路径、用途、认知问题以及替代的文字；
- 关联 ID 必须真实存在；
- 实质更新后同步 updated_at。

不得出现正文已修正、元数据仍保留旧结论的双版本状态。

## 12. 强制执行顺序

### A. 审计

1. 读取当前 ID 的六段正文、meta.json、主文件、引用图和现有 PDF。
2. 列出数学错误、内容缺口、认知障碍、缺图和排版问题。
3. 标记每个认知障碍是否必须配图。
4. 确认本轮只需修改的文件。

### B. 先图后文

1. 输出不超过 6 行的视觉计划。
2. 先实现所有必须的主教学图。
3. 围绕最终图形压缩和重写 explanation。
4. 再补全 statement、proof、examples、traps、summary。
5. 同步 meta.json。

禁止“先把六段文字写满，最后再考虑能不能塞图”。

### C. 编译

单个结论预览示例：

    $ConclusionId = "R030"
    python -B .\scripts\build_conclusion_pdfs.py $ConclusionId --overwrite --map-json ".\.tmp\${ConclusionId}_conclusion_pdf_map.preview.json"

需要纯 ID 文件名时，只有用户明确要求才添加 --pdf-name-mode id。使用独立预览映射，不能让单条构建覆盖正式映射。

### D. 逐页验收

1. 用 pdfinfo 检查页数、A4 尺寸和文件状态。
2. 用 pdftoppm 以 180—220 DPI 渲染完整 PDF。
3. 对每幅主图以 220—300 DPI 放大检查。
4. 每幅主教学图必须生成包含完整外框的 250—300 DPI 局部裁切图；整页图、联系表或自动缩放后的预览不能代替逐图检查。
5. 逐图先检查标题实际包围盒与上、左、右边框的净距，再检查图内标签；任何文字触碰、跨越或视觉压住边框均判定失败。
6. 逐页检查公式、正文、边框、页眉、页脚和分页。
7. 逐图核对数量、坐标、比例、颜色、标签和正文。
8. 执行 5 秒测试和“图是否替代文字”测试。
9. 任何实质修改后重新编译并检查受影响页。
10. 清理本轮临时渲染文件。

只看源码、日志或编译返回码不能代替视觉验收。

## 13. R030 超几何分布期望的强制视觉基准

R030_hypergeometric_expectation 同时涉及对象对应、随机抽样、模型比较、非整数期望和极端边界，属于高视觉需求结论。必须覆盖以下五个视觉任务，可以组合为 3—5 幅清晰的主图：

1. 总体—抽样—计数：直接表现 N、M、n、X 分别对应什么。
2. 指示变量：每个抽取位置贡献 0 或 1，并看见 X=X₁+⋯+Xₙ。
3. 超几何 vs 二项：用同尺度概率图说明“期望相同不等于分布相同”。
4. 非整数期望：用多次试验的整数结果及其平均说明期望可以是小数。
5. 取值上下界：用“尽量少取到”和“尽量多取到”的极端配置解释范围公式。

只有彩色文字框、公式箭头或流程节点时，本结论必须判定为未完成。不能用一幅拥挤总图敷衍五个任务。

R032_normal_3sigma_rule 只可作为字号、间距、箭头走线和渲染质量参考，不得作为所有结论的图形类型、图数或教学结构模板。

## 14. 交付格式与停止条件

完成后只汇报：

- 当前结论 ID 和最终 PDF 路径；
- 修改的文件；
- 新增主教学图：每幅图回答的问题及替代的文字；
- 数学核验结果；
- 编译、页数和逐页视觉验收结果；
- 仍存在的明确问题。

以下任一情况出现时，不得宣称完成：

- 没有合格主教学图且用户未批准无图例外；
- 命中的认知障碍仍只能靠长段文字脑补；
- 图没有替代任何文字；
- 数学条件、推导或例题仍未核验；
- PDF 未逐页渲染检查；
- 存在重叠、裁切、压线、过小标签或错误分页。

本 ID 通过全部质量门后立即停止。不得自动开始下一条结论。
