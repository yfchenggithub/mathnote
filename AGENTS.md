# 高中数学二级结论完善规范

版本：1.5  

生效日期：2026-08-29

排版与防重叠基准案例：`R032_normal_3sigma_rule`

教学图基准：按“函数变化、几何构造、概率抽样、代数对应、边界极端、易混模型对比”等类型分别建立，不得以单一案例替代所有类型的教学图基准。

`R032_normal_3sigma_rule` 只用于 TikZ 间距、箭头走线、字号和 PDF 渲染验收，不作为所有二级结论的配图数量、图形类型或教学表达基准。

## 1. 规范地位与适用范围

本文件是本仓库“新增、整理、扩充、校订或重绘二级结论”任务的唯一质量基准。后续批量完善 500+ 个二级结论时，均按本文件执行。

本规范只约束二级结论相关工作，不自动授权发布、部署、提交 Git、修改服务器或批量覆盖现有内容。用户的明确要求优先于本规范。

处理任一二级结论前，必须先确认：

- 结论 ID、所属模块和目标目录；
- 当前工作树是否有用户尚未提交的修改；
- 结论目录、`meta.json`、TikZ 资源与现有 PDF 的状态；
- 本次任务是诊断、完善、重绘、编译，还是发布。未明确要求发布时，只做到本地源文件和 PDF 验收。

## 2. 总体目标

一个完善的二级结论必须同时满足七个目标：

1. **数学正确**：条件、结论、符号、边界、推导和数值均可核验。
2. **结构完整**：学生能按“是什么—为什么—怎么证—怎么用—哪里错—如何复盘”学习。
3. **教学有效**：解释和图形服务于理解，不只追求内容数量或视觉装饰。
4. **视觉建模**：凡是需要学生在脑中想象对象、位置、过程、变化、比较、分布或极端情形的内容，必须把关键结构画出来，并用图替代相应的重复文字。
5. **视觉清晰**：PDF 中没有重叠、裁切、拥挤、断页或难以辨认的图文。
6. **检索一致**：正文、公式、标题、别名、关键词和 `meta.json` 相互一致。
7. **可稳定发布**：源文件可重复编译，资源路径稳定，流水线可以提取内容和图片。

不得把“编译成功”“文件齐全”或“页数很多”视为完善完成。任何可见的文字压线、图形交叠、公式溢出或错误分页都属于验收失败。

不得把公式装进彩色方框、把正文改写成流程节点，或增加几个装饰图标，就视为已经完成教学可视化。一个没有主教学图、只有文字和公式的结论，除非满足本规范的“无图例外”，否则不得报告为“已完善”。

## 3. 文件与数据的职责

每个二级结论目录应以以下文件为正式内容源：

```text
<module>/<ID_slug>/
├─ 01_statement.tex
├─ 02_explanation.tex
├─ 03_proof.tex
├─ 04_examples.tex
├─ 05_traps.tex
├─ 06_summary.tex
├─ meta.json
├─ main.tex
└─ source.tex
```

图形资源统一放在：

```text
assets/tikz/<ID>_<descriptive_name>.tex
```

并在正文中使用 `\input{assets/tikz/...}` 引入。文件名必须包含结论 ID，使用稳定、可读的英文主题名。

职责约定：

- 模块下结论目录中的 `01`—`06` 文件、`meta.json` 和引用资源是完善后的正式内容。
- `12_pipeline/generated_sources/` 是生成产物，不作为人工精修的源文件，不直接在其中做长期修改。
- `12_pipeline/input/<ID>/source.tex` 是流水线输入材料，不能用它无条件覆盖已经精修的六段正文。
- 修改前保留用户已有更改；不得顺手重写同目录之外的无关结论。
- 复用图形前先确认它确实表达同一数学对象；仅颜色或标题相似不构成可复用理由。

## 4. 六段正文的统一标准

### 4.1 `01_statement.tex`：二级结论

目标是让读者准确知道“结论是什么、在什么条件下成立”。至少包含：

- 结论名称或一句话主题；
- 完整适用条件；
- 变量、参数、定义域或取值范围；
- 核心公式或等价表述；
- 必要的边界、等号成立条件、方向限制或退化情形。

要求：

- 主公式醒目，但不重复堆叠同一内容；
- “充分”“必要”“充要”“近似”“恒等”必须使用准确；
- 不把经验规律写成无条件定理；
- 不在结论页塞入长证明或大量例题。

### 4.2 `02_explanation.tex`：理解说明

目标是回答“为什么这样想、各量如何变化”。推荐顺序：

1. 一句话直觉；
2. 核心变量或结构的作用；
3. 关键变换、几何意义或概率意义；
4. 一幅真正有帮助的示意图；
5. 图后关键观察。

理解说明原则上必须包含至少一幅主教学图。教学图不是正文写完后的可选装饰，而是与文字、公式同等重要的正式教学内容。

只要内容出现以下任一情况，就必须配图：

1. 抽象参数需要与具体对象建立对应关系；
2. 学生需要想象位置、形状、方向、尺度、面积或运动；
3. 数量随参数、步骤或状态发生变化；
4. 两个公式相似，但机制、分布或适用条件不同；
5. 结论来自最大值、最小值、端点、极端配置或退化情形；
6. 涉及随机抽样、长期平均、概率分布或频率变化；
7. 证明依赖构造、分割、拼接、投影、对称或轨迹；
8. 常见错误源于两个模型在文字上相似、实际结构不同。

命中一个条件，至少设计一幅主教学图；命中三个及以上条件，通常应有 2—4 幅承担不同任务的主教学图。图的数量不机械统一，但零主教学图属于必须解释和复核的异常结果。

只有纯符号运算，并且不存在对象映射、变化过程、空间关系、模型比较、随机分布和边界结构的结论，才允许采用“无图例外”。无图例外必须在审计记录和交付说明中写明理由，不能只写“没有必要”。

抽象模型优先遵循“具体对象或数值情形 → 操作或变化 → 结果 → 一般符号和公式”的顺序。学生应先看见发生了什么，再阅读抽象结论。

理解说明页应先为主教学图预留空间，再编写围绕图片展开的精简文字。图与文字发生空间冲突时，优先删除重复说明、重写段落、拆图或增加页数，不得默认删除图片或缩小字号。

定义中承担位置、尺度、方向、斜率、周期、焦距、离心率等结构作用的关键变量，必须同时说明代数作用和几何含义。若改变该变量会产生可见的图形变化，应使用控制变量法：固定其他参数，选择 2—3 个有代表性的取值绘制对照图，并明确指出哪些特征改变、哪些特征保持不变。

### 4.3 `03_proof.tex`：证明或推导

目标是让结论可检查、可追溯。必须做到：

- 明确引用的定义、定理和前提；
- 推导链条完整，不用“显然”“易知”掩盖关键步骤；
- 分情况时覆盖所有分支，并说明分界原因；
- 证明末尾回扣原结论；
- 近似数值说明来源和舍入位置；
- 不能循环论证，也不能用待证结论证明自身。

简单结论可以短证，但不能省略决定正确性的步骤。

### 4.4 `04_examples.tex`：典型例题

例题数量由结论复杂度决定，通常 1—3 题，不机械追求数量。优先覆盖：

- 直接应用；
- 常见变式或逆向使用；
- 易混分支、边界情形或实际场景。

每题必须包含：题目、识别依据、完整计算或推理、最终答案、必要的回验。数值例子应可手算或可独立复核。

多个例题不得只是换数字；每题应承担不同教学任务。

若例题涉及具体对象、空间结构、参数变化、随机结果、分布比较或极端情形，至少选择一题使用图形呈现其关键结构。例题配图应帮助识别模型或看见推理过程，不能只复述题目背景。

### 4.5 `05_traps.tex`：易错提醒

优先检查以下错误类型：

- 忽略适用条件；
- 混淆定义、参数、单位或符号；
- 把充分条件当必要条件，或反过来；
- 漏掉等号、端点、正负号、分类讨论或退化情形；
- 把近似值当精确值；
- 套用相似但不等价的公式。

每个误区应写成“错误做法—错误原因—正确替代”的闭环。只有错误口号、没有纠正方法，不算完成。

若误区来自两个相似模型、两种图形状态或两个容易混淆的公式，优先使用“错误模型 vs 正确模型”的同尺度并列图。图中必须直接标出真正不同的结构，不能只在两边分别放红叉和绿勾。

### 4.6 `06_summary.tex`：复盘总结

总结页应帮助快速复习，推荐包含：

- 一句话核心；
- 2—4 条速查公式或等价关系；
- 必要时加入解题流程或分类判断图；
- 3—5 条最后检查。

总结不能只是复制前文，也不能新增前文未解释的新结论。流程图应突出决策顺序，而不是把所有正文压进图中。

## 5. 数学内容的硬性检查

每个结论至少检查以下项目：

- 变量是否首次出现即有定义；
- 定义域、参数范围、正负性和非零条件是否齐全；
- 公式两边量纲或单位是否一致；
- 严格不等号与非严格不等号是否正确；
- 区间端点、开闭性和连续情形是否说明；
- 等号成立条件是否完整；
- “任意”“存在”“唯一”等量词是否准确；
- 精确等式与近似等式是否区分；
- 数值精度和舍入是否统一；
- 特殊值、极端情形和退化情形是否与主结论相容；
- 例题答案能否代回或用另一方法验证；
- 结论是否超出高中数学的适用语境，若超出必须明确说明；
- 图中对象数量、颜色数量、位置关系、坐标、比例、标签和正文是否完全一致；
- 函数图、概率图和统计图是否由真实公式或数据生成，而不是凭视觉感觉手画；
- 同尺度比较图是否确实使用相同坐标范围、刻度和归一化方式；
- 示意图若不按比例绘制，是否明确标注“示意”并避免利用失真的比例支持结论。

数学错误优先级高于排版美化。发现错误时先修内容，再设计图形。

## 6. TikZ 与图表设计规范

### 6.1 教学图的语义门槛

主教学图必须让学生直接看见以下至少一种信息：

- 具体对象及其数量关系；
- 操作前后发生了什么变化；
- 两种状态、方法或模型的结构差异；
- 参数变化导致的图形、位置或数值变化；
- 最大值、最小值、端点或边界的来源；
- 随机结果的波动、分布或长期平均；
- 证明中的构造、分解、对应、投影或对称关系。

以下内容不能计入主教学图数量：

- 只把原文复制进彩色方框；
- 只有公式、箭头和装饰边框；
- 用流程图重复正文句子，却没有表现对象、状态或数量变化；
- 只有图标，没有表现数学关系；
- 与结论无关的照片、人物或装饰插画；
- 需要先读完一大段说明，才能知道图片在表达什么；
- 原文全部保留，图片只作为旁边的点缀；
- 公式截图、页面截图或未经重构的教材截图。

彩色文字框不等于直觉图，流程图也不是所有数学内容的通用图形。连续变化应优先使用函数图或连续状态图，随机性应优先使用抽样图、点图、直方图或分布图，几何关系应优先使用构造、分割、展开或拼接图。

每幅主教学图都必须通过“5 秒测试”：只看图、标题和直接标签，学生应能说出该图所表达的一个核心关系。若必须依赖图外长段文字才能理解，应重新设计。

### 6.2 一图一任务

每幅图只承担一个主要教学任务，例如：结构关系、变量映射、分类流程、函数变化、面积关系或易错对照。若一幅图需要同时承担三个以上独立任务，应拆图或改为“主图 + 文字说明”。

不得为了显得丰富而增加无教学作用的图，也不得把长段文字塞入节点。

参数对照图必须遵守“每次只改变一个关键变量”。曲线数量以能看出规律为准，通常使用 3 个代表值；图中要直接标明固定量、变化量以及不变量，不能只放多条曲线而不解释比较依据。

### 6.3 图文替代与版面优先级

设计每幅主教学图时，必须记录：

1. 它帮助学生回答哪个问题；
2. 它使哪个原本需要脑补的结构变得可见；
3. 它替代或缩短正文中的哪一段文字；
4. 图中哪些对象数量、位置、颜色、坐标、比例和标签需要核验。

如果加入图片以后，原来的整段文字仍然必须原样保留，说明该图大概率只是装饰，应重新设计。图完成后应删除与图重复的说明，只保留必要的观察引导、图中无法直接表达的条件，以及从具体图形推广到一般公式的关键一步。

理解说明页应先安排主教学图，再安排文字。主图通常应占理解说明页有效内容区域的 35%—55%，宽度通常不小于当前环境 `0.70\linewidth`；若需要展示并列比较，可使用接近 `\linewidth` 的完整宽度。不得把主图缩成角落里的小插图。

图、公式和文字发生空间冲突时，优先删除重复文字、改写段落、增加页面、拆分图片或调整页面结构，不得通过缩小主图、压缩标签或降低正文可读性解决。

### 6.4 字号与可读性

- 主要标题、节点和坐标名称原则上使用 `\small` 或更大字号；
- 主图中的核心标签原则上不小于正文有效字号；
- `\scriptsize` 只用于辅助标注、箭头说明和次要信息；
- 禁止用 `\tiny` 解决空间不足；
- 不通过整体缩小图形来掩盖布局问题，应优先增加空间、换行、拆分或重排。

### 6.5 安全间距

以下数值是最低设计目标，最终仍以 PDF 中字形的实际边界为准：

- 普通节点建议 `inner xsep >= 7pt`、`inner ysep >= 6pt`；
- 文字实际包围盒到容器边框至少留 `3mm`；
- 坐标刻度文字实际包围盒到面板边框至少留 `3mm`；
- 曲线峰值或图形主体到标题文字至少留 `4mm`；
- 箭头、折线与无关文字至少留 `3mm`；
- 相邻节点之间无标签时至少留 `6mm`，需要放箭头标签时至少留 `10mm`；
- 两个上下分区之间若放公式或变换说明，应预留独立变换带，不与任一分区边框共线。

坐标点到边框的距离不等于文字到边框的距离。含下伸字形的公式（如 `\mu-k\sigma`、分式、上下标）必须按完整字形高度预留空间。这是 R032 修改中最重要的视觉教训之一。

### 6.6 连线与箭头

- 只有在内容确实是步骤、决策或分支时才使用流程图；
- 流程图优先使用水平、垂直的正交连线；
- 每个分支尽量使用独立走线通道；
- 箭头不得穿过节点文字、标题、公式或其他箭头标签；
- 箭头应从明确的锚点进入节点，并保留箭头头部与文字的安全距离；
- 箭头标签默认使用单行，只保留动作、条件或分支所需的最短信息；若在正常字号和安全间距下仍无法单行容纳，说明流程结构过密，应拆分或重排流程，不得通过换行或缩字硬塞；
- 分支名称放在对应线段旁，不压在线上或边框上；
- 交叉线超过一次时，应重排节点而不是继续绕线；
- 入框方向应符合阅读方向，避免箭头横向撞入本应垂直进入的节点。

### 6.7 坐标图、曲线图与分区图

- 标题行、说明行、曲线区、坐标轴区、刻度标签区应分层设计；
- 坐标轴不能兼作面板边框；
- 刻度文字不能碰到或跨过面板边框；
- 曲线、阴影、辅助线和标签的层级必须清楚；
- 比较图使用一致的坐标尺度，若尺度不同必须明确标注；
- 公式映射或参数变换应放在独立空白带中，不挤在两幅曲线之间；
- 函数曲线、概率柱、统计图和数值标记必须由真实公式或数据生成；
- 若图中面积、长度、斜率或高度承担数学含义，不得为了美观任意改变其相对比例；
- 重要曲线和区域优先直接标注，避免让学生在图与远处图例之间来回查找。

### 6.8 画布、颜色、工具与复用

- 图宽按当前环境的 `\linewidth` 判断，不假定等于整页 `\textwidth`；
- 建议用 `\path[use as bounding box]` 明确画布，确保所有文字、箭头和边框均在范围内；
- 不允许图形、阴影或箭头超出包围盒后被裁切；
- 精确数学图优先使用 TikZ、PGFPlots 或由程序计算后导出的矢量图；
- AI 生图可用于构图草案或风格参考，但不得直接生成最终数学公式、坐标曲线、概率柱、对象数量或需要精确对应的教学图；
- 优先使用仓库已有语义色，如 `CStatement`、`CExplanation`、`CProof`、`CExample`、`CTrap`、`CSummary`；
- 同一数学对象在整份 PDF 中必须保持相同颜色和形状编码，例如目标个体、普通个体、原函数、变换后函数不能中途换色；
- 填充色应浅，文字和边框对比度必须足够；
- 三条或更多数据曲线必须优先使用跨色相类别的配色（如红、蓝、纯绿）；“高对比”指色相明显分离，不是只提高亮度、饱和度，或在同一色系中调深浅；禁止把深蓝与青蓝、红与紫红、红与橙等相邻近似色作为不同系列；图例、曲线、辅助线和对应标签必须保持同色；
- 若图形可能被黑白打印、低质量投影或面向色觉障碍读者使用，应在高对比色之外再增加实线、虚线、点线或标记点的区别；
- 同类图保持字号、线宽、圆角和颜色逻辑一致，但不得机械复制坐标。

### 6.9 认知问题与图形类型选择

| 认知问题 | 优先采用的图形 |
| --- | --- |
| 参数与对象对应 | 具体对象模型图、直接标注图 |
| 操作或状态变化 | 分步骤连续图、操作前后对照图 |
| 函数参数变化 | 同尺度控制变量对照图 |
| 两种模型易混 | 左右并列、相同尺度的比较图 |
| 最大值、最小值或边界 | 两个极端配置图、端点图 |
| 概率与期望 | 抽样模型图、重复试验图、点图、直方图或分布图 |
| 几何公式来源 | 分割、展开、拼接、投影或面积对应图 |
| 证明中的对应关系 | 构造图、映射图、局部高亮图 |
| 易错点 | 错误模型与正确模型并列图 |

选择图形类型时必须从认知问题出发，不能因为现成模板是流程图，就把所有内容都改造成流程图。

## 7. 页面与排版原则

- 页面留白是教学层级的一部分，不要以“填满一页”为目标；
- 主教学图应在页面草图阶段优先获得版面，不能等文字排满后再塞入剩余空间；
- 对于命中三个及以上强制配图条件的结论，可以合理增加页数，不得为了维持固定页数删除关键图片；
- 一个大框内只保留一个清晰主题，避免多个重型图表连续堆叠；
- 标题不得孤立在页底，图题不得与图分离；
- 大公式、长推导和 TikZ 图应避免跨页；
- 仅在确有独立教学单元或避免不良断页时使用 `\clearpage`；
- 页眉、页脚、页码、边框和正文之间必须保持稳定留白；
- 不用缩小字号解决分页，优先重写、分段、拆图或换页；
- 同一结论内部的六类彩色环境应保持现有语义配色，不随意互换。

## 8. `meta.json` 一致性标准

正文完善后必须复核 `meta.json`，至少保证：

- `id`、模块、标题与目录一致；
- `core.title`、`core.summary` 与 `01_statement.tex` 的主结论一致；
- 别名是真实常用叫法，不堆砌无意义近义词；
- 关键词、检索意图和查询模板覆盖学生真实搜索方式；
- `math.core_formula`、相关公式、变量说明、条件和结论与正文一致；
- `content` 中的直觉、推导和技巧没有保留旧版本错误；
- `interactive.has_diagram` 与实际情况一致；只有真正表现数学关系的主教学图或必要辅助图，才能据此标记为有图；
- `remarks` 中的图形数量、用途、路径、对应认知问题和所替代的文字说明准确；
- 若采用无图例外，`remarks` 或本轮审计记录中必须写明具体理由；
- 关联 ID 必须真实存在，不凭标题相似建立关系；
- 内容实质更新后同步维护 `updated_at`。

不得出现“正文已经修正，元数据仍描述旧结论”的双版本状态。

## 9. 标准工作流程

### 阶段 A：审计

1. 读取六段正文、`meta.json`、`main.tex`、`source.tex` 和所有引用资源；
2. 查看当前 PDF，记录结论错误、缺失内容、视觉缺陷和元数据不一致；
3. 列出学生理解本结论时需要在脑中完成的对象想象、过程模拟、图形变化、数量比较、随机波动和边界判断；
4. 为每个认知障碍标记“必须配图、建议配图或可用文字”，并写明建议图形类型；
5. 标记当前正文中可由图片替代或明显缩短的段落；
6. 给每个问题标注优先级：数学错误 > 条件缺失 > 教学结构缺口（包括缺少必须的教学图） > 例题不足 > 纯排版视觉问题 > 元数据优化；
7. 确认工作树中的已有修改，避免覆盖用户内容。

### 阶段 B：内容与图形共同设计

1. 先写清适用条件与核心结论；
2. 根据审计结果确定必须画出的主教学图，以及每幅图要回答的问题；
3. 为每幅主图选择正确的视觉类型，不能默认使用流程图；
4. 先制作页面草图，确定图、公式和文字的空间比例；
5. 先用具体对象、代表数值、前后状态或极端情形建立直觉，再过渡到一般符号和公式；
6. 编写围绕图片展开的精简解释，并删除将被图片替代的重复文字；
7. 补齐完整推导；
8. 设计承担不同任务的例题，必要时为关键例题配图；
9. 从真实错误路径整理易错点，必要时设计“错误模型 vs 正确模型”对照图；
10. 完成总结和 `meta.json` 对齐。

在进入实现前，至少形成以下视觉设计记录：

| 图形任务 | 学生的认知障碍 | 图形类型 | 替代或缩短的文字 | 目标页面 | 正确性检查点 |
| --- | --- | --- | --- | --- | --- |
| 示例 | 需要脑补的结构 | 对象图/对照图/分布图等 | 对应段落 | 01—06 | 数量、坐标、比例、标签等 |

### 阶段 C：实现

1. 按六段职责分别编辑，不把所有内容塞进一个文件；
2. 优先实现主教学图，再围绕最终图形调整正文长度和页面结构；
3. TikZ 独立成文件，以 `\input` 引用；
4. 复用仓库样式和颜色，但不能为了复用现成流程图模板而选择错误的图形类型；
5. 每次只修改当前结论所需文件；
6. 不直接修改生成产物来掩盖源文件问题。

### 阶段 D：编译与视觉验收

单个结论预览构建推荐使用：

```powershell
$ConclusionId = "R032"
python -B .\scripts\build_conclusion_pdfs.py $ConclusionId --overwrite --map-json ".\.tmp\${ConclusionId}_conclusion_pdf_map.preview.json"
```

将示例 ID 替换为当前结论 ID。如果任务明确要求生成纯 ID 文件名的 PDF，再添加：

```powershell
--pdf-name-mode id
```

使用独立预览映射文件，避免单条构建把正式 `build/conclusion_pdf_map.json` 改写成仅包含一个 ID 的映射。

编译后必须：

1. 用 `pdfinfo` 检查页数、纸张尺寸和文件状态；
2. 用 `pdftoppm` 将完整 PDF 以 180—220 DPI 渲染为 PNG；
3. 逐页检查正文、公式、边框、页眉、页脚和分页；
4. 对每幅 TikZ 图以 220—300 DPI 放大检查；
5. 逐图核对对象数量、颜色数量、位置关系、坐标、尺度、标签、公式和正文是否一致；
6. 对每幅主教学图执行“5 秒测试”，确认图本身能够表达预定的核心关系；
7. 检查主教学图是否真正替代或缩短了原有文字，而不是与正文重复；
8. 检查主图是否获得足够版面，没有被缩成角落里的小配图；
9. 任何实质布局修改后重新渲染受影响页；
10. 最终检查图中必须达到零重叠、零裁切、零压线、零难辨文字；
11. 清理本轮生成的临时渲染文件。

只查看 LaTeX 源码或只看到编译返回成功，不能替代 PDF 视觉检查。

### 阶段 E：数据与发布

只有用户明确要求同步前端或发布时，才运行规范内容构建、图片渲染、增量发布或远程部署。发布流程遵循 `scripts_docs/update_conclusion_pdf_and_deploy.md`，并在发布前再次确认本地 PDF 已通过本规范。

## 10. 完成定义（Definition of Done）

一个二级结论只有在以下项目全部通过后，才能报告“已完善”：

### 内容

- [ ] 结论准确，条件和变量完整；
- [ ] 核心公式、边界和等号条件正确；
- [ ] 直觉解释能说明关键结构；
- [ ] 证明或推导无关键跳步；
- [ ] 例题覆盖不同任务且答案可核验；
- [ ] 易错点包含原因和正确替代；
- [ ] 总结可独立用于快速复盘；
- [ ] 没有为了页数而重复内容。

### 图形与页面

- [ ] 已列出本结论的主要认知障碍，并完成视觉设计记录；
- [ ] 所有命中强制配图条件的认知障碍均有对应图片；
- [ ] 除正式记录的无图例外外，至少存在一幅主教学图；
- [ ] 命中三个及以上强制配图条件时，已检查是否需要 2—4 幅承担不同任务的主教学图；
- [ ] 每幅主教学图都明确替代或缩短了一段文字；
- [ ] 没有把彩色文字框、公式截图、页面截图或装饰图标冒充主教学图；
- [ ] 学生只看图、标题和直接标签，就能说出该图的一个核心关系；
- [ ] 抽象模型优先按照“具体对象或数值 → 操作或变化 → 结果 → 一般公式”呈现；
- [ ] 主教学图获得足够版面，没有缩在角落或依靠极小标签勉强放下；
- [ ] 对象数量、颜色数量、坐标、比例、尺度、标签和公式已经逐项核对；
- [ ] 函数图、概率图和统计图由真实公式或数据生成；
- [ ] 同一数学对象在整份 PDF 中保持一致的颜色和形状编码；
- [ ] 每幅图有唯一主要教学任务；
- [ ] 字号可读，没有使用极小字号救场；
- [ ] 文字、公式、箭头、曲线和边框无重叠；
- [ ] 刻度标签与边框有明确安全距离；
- [ ] 图形和公式未被裁切；
- [ ] 页眉、页脚、页码和分页正常；
- [ ] 全 PDF 已逐页渲染检查；
- [ ] 修改后的相关页已重新检查。

### 工程与数据

- [ ] 六段文件职责清晰并可编译；
- [ ] TikZ 路径和命名稳定；
- [ ] `meta.json` 与正文一致；
- [ ] 没有误改生成文件或无关结论；
- [ ] 最终 PDF 存在、页数合理、A4 输出正常；
- [ ] 临时检查文件已清理；
- [ ] 未经授权没有执行发布、部署或 Git 提交。

任何一项硬性视觉问题或数学问题未通过，都不得以“基本完成”交付。

## 11. 500+ 结论的批量推进原则

大规模整理必须采用“小批量、同标准、逐条验收”，不能一次性生成后只抽查少数 PDF。

推荐节奏：

1. **建立清单**：记录每个 ID 的内容完整度、数学风险、认知障碍、强制配图触发项、图形任务、无图例外、元数据状态和当前 PDF 状态；
2. **先做试点**：每个模块先选 3—5 个不同类型结论，验证模板和视觉策略；
3. **按模块小批推进**：每批建议 5—10 个，避免问题扩散到整个仓库；
4. **逐条质量门**：每个 ID 都要单独编译、渲染和检查，不因同批次其他结论通过而默认通过；
5. **发现系统性问题立即停批**：先修规范、模板或共用样式，再继续后续批次；
6. **沉淀可复用组件**：同类布局至少稳定使用三次后再抽成公共样式，避免过早抽象；
7. **保持内容差异**：结构统一不等于内容同质化，例题、图形和误区必须服从具体结论；
8. **零图预警**：批量清单中任何零主教学图的结论都必须自动进入复核队列，不能默认通过；
9. **按类型沉淀教学图基准**：函数、几何、概率、代数、边界和易混模型应分别保留通过验收的代表案例；
10. **阶段性复查**：每完成一个模块，随机复查数学、图形、检索和发布链路，但随机复查不能替代每条的基础验收。

自动化适合检查文件是否齐全、JSON 是否合法、公式是否存在和编译是否成功；数学正确性、教学价值与视觉重叠仍需逐条审查。

## 12. R032 提炼出的核心经验

R032 的修改形成以下长期原则：

1. **先分教学层级，再画图**：输入、变换、判断、分支和检查应分层，不能挤在一条线上。
2. **一条分支一条通道**：流程图箭头复用同一狭窄区域会造成穿字、误读和入框方向混乱。
3. **变换说明要有独立空间**：上下映射图之间必须留出公式和说明带，不能让它们压住曲线或边框。
4. **按字形包围盒留白**：坐标基线看似离边框足够，公式下伸字形仍可能压线；必须查看最终 PDF。
5. **空间不足时重排，不缩字**：扩大安全间距、降低曲线、重排或拆图优先于缩小字号；正文可以合理换行，箭头标签不得靠换行挤入狭窄通道。
6. **底部检查独立成层**：结论的限制条件和最终核对不应被分支箭头穿过。
7. **编译成功只是开始**：R032 的重叠问题只有在高分辨率渲染后才被准确发现，视觉验收是硬性步骤。
8. **图的数量服从认知障碍**：不能机械照搬 R032 的图数，但零主教学图属于异常结果；命中强制配图条件时，必须覆盖相应的认知障碍。
9. **先设计主图，再定稿文字**：若先把页面写满，图片最终通常只能成为缩小的附件；页面草图和主图必须早于最终正文定稿。
10. **先让学生看见对象和变化**：抽象公式之前，优先展示具体对象、代表数值、操作过程、前后状态或极端配置。
11. **布局正确不等于教学有效**：零重叠、零裁切只能证明图形可见；只有通过语义门槛、文字替代和 5 秒测试，才能证明它是合格教学图。

后续完善任何二级结论时，应复用这些排版原则，而不是机械复用 R032 的具体坐标、流程图结构、图数或页面长度。R032 不得替代函数、几何、概率、代数和边界类结论各自的教学图基准。

### 12.1 概率类教学图基准：超几何分布的期望

“超几何分布的期望”同时命中参数与对象对应、随机抽样、模型比较、非整数期望和边界极端等强制配图条件，属于高视觉需求结论。一个合格版本通常应覆盖以下视觉任务：

1. **总体—抽样—计数模型图**：用具体对象表现总体量 `N`、目标个体数 `M`、样本量 `n` 和样本中的目标数 `X`；
2. **指示变量图**：表现每个抽取位置贡献 `0` 或 `1`，以及 `X=X_1+\cdots+X_n`；
3. **超几何分布与二项分布对比图**：用同尺度概率图说明“期望相同不等于分布相同”；
4. **非整数期望图**：用多次整数结果的平均说明期望为什么可以是小数；
5. **取值范围的极端配置图**：分别画出尽量少取和尽量多取目标个体的情形，解释上下界公式的来源。

若该主题只有彩色文字框、公式流程图或文字节点，即使数学内容正确、排版无重叠，也不能通过教学图验收。本案例用于校准概率类结论的视觉质量，不要求其他概率结论机械照搬五幅图，而要求覆盖各自真实存在的认知障碍。

## 13. 规范维护

- 本文件保持为唯一正式版本，不在其他目录复制出内容分叉的“另一份规范”；
- 后续发现具有普遍性的数学、排版或流水线问题时，应先在少量结论中验证解决方案，再更新本文件；
- 更新规范时同步修改版本号和生效日期，并在本节追加一行简要变更说明；
- 不得为了提高批量速度而降低数学正确性、教学图覆盖、逐页渲染或零重叠这四项硬性质量门。

变更记录：

- **1.5（2026-08-29）**：把教学图从可选装饰提升为硬性教学内容；增加强制配图触发条件、无图例外、语义门槛、图文替代、视觉设计记录、零图预警、5 秒测试和多类型教学图基准。
- **1.4（2026-08-28）**：根据 R032 箭头标签复核，规定箭头文字默认单行并保持最短必要信息；单行无法容纳时拆分或重排流程。
- **1.3（2026-08-28）**：根据 R032 配色复核，明确多系列高对比必须以跨色相区分为主，禁止用同色系亮暗或饱和度差异充当系列区别。
- **1.2（2026-08-28）**：根据 R032 三曲线辨识问题增加多系列高对比配色与辅助线型要求。
- **1.1（2026-08-28）**：根据 R032 参数图补充“关键变量的代数作用与几何含义”和控制变量对照图要求。
- **1.0（2026-08-28）**：基于 R032 完善过程建立六段内容、TikZ 安全间距、PDF 视觉验收和 500+ 结论批量推进标准。
