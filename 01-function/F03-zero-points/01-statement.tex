\begin{statementbox}
\paragraph{结论（单调性判定定理）}

设函数 $f(x)$ 在区间 $I$ 上可导：
\begin{itemize}
  \item 若 $f'(x) > 0$ 在 $I$ 上恒成立，则 $f(x)$ 在 $I$ 上严格单调递增；
  \item 若 $f'(x) < 0$ 在 $I$ 上恒成立，则 $f(x)$ 在 $I$ 上严格单调递减；
  \item 若 $f'(x) \ge 0$（或 $\le 0$），则 $f(x)$ 在 $I$ 上单调不减（或不增）。
\end{itemize}
\end{statementbox}
