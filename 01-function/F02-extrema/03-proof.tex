\begin{proofbox}
\paragraph{证明（略述）}

由拉格朗日中值定理：  
对任意 $x_1 < x_2 \in I$，存在 $\xi \in (x_1,x_2)$，使得
\[
f(x_2)-f(x_1)=f'(\xi)(x_2-x_1).
\]

当 $f'(x)>0$ 时，右端恒为正，因此 $f(x_2)>f(x_1)$，
函数在 $I$ 上严格单调递增，其余情况类似。
\end{proofbox}
