\begin{statementbox}
有限集合 $A = \{a_1, a_2, \dots, a_n\}$（$n \in \mathbb{N}^*$）的子集个数、真子集个数等有以下核心结论：
\begin{enumerate}
    \item \textbf{子集个数}：$2^n$；
    \item \textbf{真子集个数}：$2^n - 1$；
    \item \textbf{非空子集个数}：$2^n - 1$；
    \item \textbf{非空真子集个数}：$2^n - 2$。
\end{enumerate}
\end{statementbox}