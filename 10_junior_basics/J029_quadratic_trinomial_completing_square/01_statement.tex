\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（二次项非零）：}}] $a \neq 0$。
		\item[\textbf{\textcolor{CStatement}{条件2（全体实数）：}}] $a,b,c,x \in \mathbb{R}$。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（配方恒等式）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			二次三项式化为完全平方与常数之和。
			\[
				\boxed{ax^{2}+bx+c = a\left(x+\frac{b}{2a}\right)^{2} + \frac{4ac-b^{2}}{4a}}.
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（顶点坐标）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			直接读出顶点坐标与对称轴。
			\[
				\left(-\frac{b}{2a},\; \frac{4ac-b^{2}}{4a}\right).
			\]
	\end{description}
\end{statementbox}
