\begin{proofbox}
	\textbf{\textcolor{CProof}{思路提示}}

	本题证明分为两步：先由内心定义确定等距，再利用面积分割导出半径公式。

	\medskip
	\textbf{\textcolor{CProof}{正式推导}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.55em]
		\item[\textbf{步骤一（等距性质）：}]
			由$I$是三条角平分线的交点，根据角平分线性质，$I$到各边距离相等。设该公共距离为$r$。
			\[
				d(I,BC)=d(I,CA)=d(I,AB)=r.
			\]
			\par\textbf{理由：}
			角平分线上的点到角的两边距离相等，$I$在三个角平分线上，故到三边等距。

		\item[\textbf{步骤二（面积分割）：}]
			连接$I$与三个顶点$A,B,C$，将$\triangle ABC$分成三个小三角形$\triangle IBC,\triangle ICA,\triangle IAB$。它们分别以$a,b,c$为底，高都是$r$。
			\[
				\begin{aligned}
					S_{\triangle IBC}&=\frac{1}{2} a r,\\
					S_{\triangle ICA}&=\frac{1}{2} b r,\\
					S_{\triangle IAB}&=\frac{1}{2} c r.
			\end{aligned}
			\]
			\par\textbf{理由：}
			小三角形的底分别为$a,b,c$，高为内心到对应边的距离$r$，面积等于底乘高的一半。

		\item[\textbf{步骤三（求和公式）：}]
			三个小三角形的面积之和等于原三角形的面积$S$，因此
			\[
				\frac{1}{2} r (a+b+c) = S,
			\]
			解得
			\[
				r = \frac{2S}{a+b+c}.
			\]
			\par\textbf{理由：}
			整体等于部分和；两边同乘2并除以$a+b+c$（$a+b+c>0$）得到$r$。
	\end{description}

	\medskip
	\textbf{\textcolor{CProof}{结论回扣}}

	以上推导表明：内心到三边的距离相等（都等于$r$），且该距离$r$正好满足$r = \dfrac{2S}{a+b+c}$，因此$r$就是内切圆的半径。\textbf{证毕}。
\end{proofbox}
