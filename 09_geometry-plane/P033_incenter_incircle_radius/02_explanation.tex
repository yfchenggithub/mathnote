\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	内心是三角形内切圆的圆心，因此它到三边的垂线段都是圆的半径，所以长度必然相等。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（定义基础）：}]内心是三条角平分线的交点，也是唯一到三边等距的点。
		\item[\textbf{要点二（等距结论）：}]该等距长度就是内切圆半径$r$。
		\item[\textbf{要点三（代数联系）：}]由面积分割$S = \frac{1}{2}r(a+b+c)$，可导出$r = \frac{2S}{a+b+c}$。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（图形解释）}}

	内心是内切圆圆心，故到三边距离相等且等于半径，这是内切圆定义的等价表述。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（周长面积）}}

	半径公式将几何量（距离）与代数量（边长、面积）联系起来，可用于计算。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（高考应用）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（直接求半径）：}已知三边长，先求面积（海伦公式），再代$r = \frac{2S}{a+b+c}$。
		\item \textbf{考法二（面积转化）：}利用$S = rp$，由半径和周长求面积。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	内心性质本质上就是内切圆的定义，公式只是面积分割的代数结果。不要与重心、垂心混淆。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（边长已知）：}已知三角形三边长，要求内切圆半径。
		\item \textbf{场景二（半径已知）：}已知内切圆半径和周长，要求面积。
	\end{itemize}
\end{explanationbox}
