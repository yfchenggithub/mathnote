\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（任意三角形）：}}] 在\(\triangle ABC\)中，\(D\)为\(BC\)边的中点，\(G\)为该三角形的重心。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（比例关系）：}}]
		      \textbf{\textcolor{CStatement}{直观描述：}}
		      顶点到重心的距离是重心到对边中点距离的两倍。
		      \[
			      \boxed{AG:GD = 2:1}
		      \]

		\item[\textbf{\textcolor{CStatement}{推广结论（长度公式）：}}]
		      \textbf{\textcolor{CStatement}{直观描述：}}
		      已知中线长可直接计算两部分长度。
		      \[
			      AG = \frac{2}{3} AD,
			      \qquad
			      GD = \frac{1}{3} AD
		      \]
	\end{description}
\end{statementbox}