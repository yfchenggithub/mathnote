\begin{proofbox}
设圆上任意一点为 $A$,
连接 $PA$、$OA$。

在 $\triangle OPA$ 中，
由三角形两边之和大于第三边，有：
\[
|OP-OA| \le PA \le OP+OA
\]

因为 $OA=R$,$OP=d$,
且 $d<R$，故
\[
R-d \le PA \le R+d
\]

当点 $A$ 在直线 $OP$ 上，
且位于 $O$ 的反向延长线方向时，
$PA=R-d$;

当点 $A$ 在直线 $OP$ 上，
且位于 $P$ 指向圆外方向时，
$PA=R+d$。

因此结论成立。
\end{proofbox}
