\begin{trapbox}
\begin{itemize}
    \item \textbf{图形对应：} 注意公式中减去的是"差对角线"（即连接两向量终点的线段）。在三角形中，这通常对应底边的长度。
    \item \textbf{中线形式：} 极化恒等式最常用的变体是中线模式：$\vec{AB} \cdot \vec{AC} = |\vec{AM}|^2 - |\vec{BM}|^2$（其中 $M$ 为 $BC$ 中点）。很多同学容易记错系数。
\end{itemize}
\end{trapbox}