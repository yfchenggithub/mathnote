\begin{statementbox}

\textbf{条件：}
\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
    \item[\textbf{条件 1（非负实数）：}] $a, b, c$ 均为非负实数。
\end{description}

\textbf{结论：}
\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
    \item[\textbf{结论一（立方和不等式）：}]
    \textbf{直观描述：} 三个非负数的立方和不小于三倍它们的乘积。
    \[
    a^3 + b^3 + c^3 \geq 3abc
    \]
\end{description}

\textbf{等号/取等条件：} 当且仅当 $a = b = c$。

\end{statementbox}
