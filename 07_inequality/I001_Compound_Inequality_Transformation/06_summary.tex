\begin{summarybox}
【一句话核心】
👉 “$N < f(x) < M$” 可以等价变形成乘积、绝对值、分式、倒数四种形式。

【使用条件】
1. 前提是 $M > N$。
2. $f(x)$ 是实数值函数。
3. 当原始不等式复杂时，考虑转化。

【关键提醒】
⚠️ 使用倒数形式前，必须确保 $f(x)-N > 0$（即 $f(x) > N$），否则倒数不等式方向可能出错。

【速记口诀】
夹中间 → 乘为负、距中小、分式正、倒反转。
\end{summarybox}
