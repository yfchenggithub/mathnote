\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（递推形式）：}}] 数列 $\{a_n\}$ 满足 $a_{n+1}=a_n+f(n)$（即 $a_{n+1}-a_n=f(n)$），其中 $f(n)$ 可求和。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（通项公式）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			将前 $n-1$ 个差依次累加得到第 $n$ 项。
			\[
				a_n = a_1 + \sum_{k=1}^{n-1} f(k)
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（一般累加）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			从第 $n$ 项累加到第 $m$ 项。
			\[
				a_m = a_n + \sum_{k=n}^{m-1} f(k),
				\qquad
				m>n
			\]
	\end{description}
\end{statementbox}
