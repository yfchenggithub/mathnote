\paragraph{典型例题}

\textbf{例 1}：判断函数 $f(x)=x^3-3x+1$ 的单调性。

\textbf{解}：
\[
f'(x)=3x^2-3=3(x-1)(x+1).
\]
当 $x<-1$ 或 $x>1$ 时，$f'(x)>0$；
当 $-1<x<1$ 时，$f'(x)<0$。

因此：
\begin{itemize}
  \item $(-\infty,-1)$ 与 $(1,+\infty)$ 上单调递增；
  \item $(-1,1)$ 上单调递减。
\end{itemize}
