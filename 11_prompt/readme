1. 识别 (OCR/Extraction) (结论识别层)
L1：只关心“看清楚”
将图像转换为原始文本和数学公式。大模型直接对图像进行复杂逻辑推理时，容易漏掉上下标或几何图形的隐含条件。先“数码化”是所有后续操作的物理基础。
2. 提炼与升级 (Refinement) (结论重构)
L2：只关心“讲明白”
“把原始表达 → 讲义级标准结论”
3. 评估 (Evaluation) (评估层)
L3：值不值（评估）
逻辑校对、公式推导验证及教学价值评级。这是质量闸门。数学结论最忌讳逻辑瑕疵。通过一个专门的“审稿人”角色，可以大幅降低最终 PDF 出现错误的概率。
是否值得进入讲义
难度 / 重要性 / 通用性评分
是否重复
4. LaTeX 生成 (6个文件) (结构生成层)
L4：变教材（6文件）
按预设模板（如：定义、证明、例题、变式等）填充内容。属于格式化输出阶段。如果 Prompt 足够长，可以与阶段 5 合并，但在复杂项目中，分拆有助于维护模板的稳定性。
5. meta.json 生成 (索引与系统层)
提取关键词、分类标签、索引权重等数据。对于你的“二级结论查询工具”小程序，这个文件决定了 Fuse.js 的搜索精度。必须确保 JSON 的 key 与小程序前端代码严格对齐。
“系统数据”，不是内容本身