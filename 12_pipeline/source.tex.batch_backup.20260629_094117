\textbf{结论名称：} A字型相似（平行截相似）

\textbf{适用条件：} 在 $\triangle ABC$ 中，$DE \parallel BC$，点 $D$ 在 $AB$ 上，点 $E$ 在 $AC$ 上，则所得小三角形与原三角形相似（A字型结构）。

\textbf{变量说明：} 设相似比 $k = \dfrac{AD}{AB} = \dfrac{AE}{AC} = \dfrac{DE}{BC}$，对应高、中线、角平分线的比均为 $k$，面积比为 $k^2$。

\textbf{核心公式：} 
\[
\boxed{\frac{AD}{AB} = \frac{AE}{AC} = \frac{DE}{BC}, \quad \frac{S_{\triangle ADE}}{S_{\triangle ABC}} = k^2 = \left(\frac{AD}{AB}\right)^2}
\]

\textbf{使用场景：} 解析几何中面积比速算，立体几何截面比例，平行线分线段成比例的快速推理。

\textbf{简单例子：} 在 $\triangle ABC$ 中，$DE \parallel BC$，已知 $AD = 2$，$DB = 3$，则 $AB = 5$，相似比 $k = \dfrac{2}{5}$，两三角形面积比 $\dfrac{S_{\triangle ADE}}{S_{\triangle ABC}} = \left(\dfrac{2}{5}\right)^2 = \dfrac{4}{25}$。