\begin{statementbox}
在 $\triangle ABD$ 中，$M$ 为 $BD$ 的中点，则极化恒等式可表示为：
\[ \vec{a} \cdot \vec{b} = |\vec{AM}|^2 - \frac{1}{4}|\vec{DB}|^2 \quad (\text{三角形模式}) \]

\textbf{几何意义：}
两个向量的数量积等于它们起点指向终点中点的向量模长的平方，减去终点之间距离一半的平方。
\end{statementbox}