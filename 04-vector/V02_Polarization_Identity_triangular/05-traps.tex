\begin{trapbox}
\begin{itemize}
    \item \textbf{系数陷阱：} 三角形模式中 $|\vec{DB}|^2$ 前面的系数是 $\frac{1}{4}$。若改用半边长 $|\vec{BM}|$，公式则为 $\vec{a} \cdot \vec{b} = |\vec{AM}|^2 - |\vec{BM}|^2$。
    \item \textbf{图形位置：} 务必确认 $M$ 是两个向量公共起点对面那条边的中点。
\end{itemize}
\end{trapbox}