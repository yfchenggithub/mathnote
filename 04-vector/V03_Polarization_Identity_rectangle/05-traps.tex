\begin{trapbox}
\begin{itemize}
    \item \textbf{图形性质限制：} 此结论严格依赖于 $OA=OB$ 这一特性，因此仅适用于**矩形**（含正方形）。对于普通的平行四边形，该等式不成立。
    \item \textbf{符号误区：} 在结论 ① 中，注意是对应顶点的平方和相等（$A$ 与 $C$ 对应，$B$ 与 $D$ 对应），不要记成邻边的平方和。
    \item \textbf{起点一致性：} 在结论 ② 中，四个向量的起点必须相同（均为点 $P$）。
\end{itemize}
\end{trapbox}