\begin{trapbox}
\begin{itemize}
    \item \textbf{起点顺序}：公式要求两个向量必须是**同起点**。如果是 $\vec{AB} \cdot \vec{CA}$，必须先转化为 $-\vec{AB} \cdot \vec{AC}$。
    \item \textbf{符号混淆}：公式分子中，减去的永远是**向量终点连线所对的边（即对边 $a$）**。
    \item \textbf{适用条件}：虽然该公式源于余弦定理，但在向量证明题中，直接使用该结论可以简化大题步骤。
\end{itemize}
\end{trapbox}