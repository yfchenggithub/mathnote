\begin{trapbox}
\begin{itemize}
    \item \textbf{基准错位}：使用定理前，必须明确哪条线是 $k=1$ 的基准线（通常是连接两基底向量终点的直线）。
    \item \textbf{比例正负}：当等和线与基准线在 $O$ 点异侧时，$k$ 为负值。学生常忽略负号。
    \item \textbf{向量起点}：所有向量 $\vec{OA}, \vec{OB}, \vec{OP}$ 必须始于同一点 $O$。
\end{itemize}
\end{trapbox}