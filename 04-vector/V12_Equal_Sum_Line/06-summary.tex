\begin{summarybox}
\textbf{等和线定理口诀}：
\begin{itemize}
    \item \textbf{一线上，和相等}：平行线上的点，系数和为定值。
    \item \textbf{过终点，和为一}：经过基底终点的直线，$k=1$。
    \item \textbf{远则大，近则小}：离 $O$ 越远，$|k|$ 越大；过 $O$ 点，$k=0$。
    \item \textbf{反向记，负数加}：在 $O$ 点另一侧，$k$ 取负数。
\end{itemize}
\end{summarybox}