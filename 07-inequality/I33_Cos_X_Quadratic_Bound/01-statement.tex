\begin{statementbox}
对于任意实数 $x \in \mathbb{R}$，余弦函数存在以下二阶放缩不等式：
\begin{enumerate}
    \item \textbf{核心形式}：$\cos x \geqslant 1 - \frac{x^2}{2}$
    \item \textbf{等号条件}：当且仅当 $x = 0$ 时，等号成立。
    \item \textbf{结构特征}：左侧为三角函数，右侧为开口向下的抛物线。在 $x=0$ 附近，该抛物线是余弦曲线的最佳二次逼近。
\end{enumerate}
\end{statementbox}