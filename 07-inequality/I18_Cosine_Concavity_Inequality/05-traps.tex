\begin{trapbox}
\begin{itemize}
    \item \textbf{区间越界}：如果 $x$ 或 $y$ 超过了 $90^\circ$（即钝角三角形），由于余弦函数在钝角区变为下凸，不等号方向会发生翻转！这是高考中最隐蔽的陷阱。
    \item \textbf{单位误用}：注意区别 `cos(1)`（弧度）和 `cos(1°)`（角度）。琴生不等式在两种单位下逻辑一致，但数值差异巨大。
\end{itemize}
\end{trapbox}