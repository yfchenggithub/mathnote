\begin{trapbox}
\begin{itemize}
    \item \textbf{分子分母错位}：注意是 $x^2$ 对应分母 $y$。如果题目是 $\frac{y^2}{x}$，在循环求和中结论相同，但如果是混合项（如 $\frac{x^2}{x+y}$）则需重新计算。
    \item \textbf{正数要求}：分母必须为正，否则不等号方向可能反转。
    \item \textbf{等号条件}：在有限制条件（如 $x+2y+3z=k$）的题目中，等号可能无法取得，此时该结论仅能作为放缩过程中的一步。
\end{itemize}
\end{trapbox}