\begin{trapbox}
\begin{itemize}
    \item \textbf{自变量范围}：该不等式在 $x < 0$ 时**不一定成立**（例如当 $x$ 极小时，二次项占优）。务必确认题目区间。
    \item \textbf{等号漏掉}：在写步骤时，必须交代 $x=0$ 处 $f(0)=0$ 的取到情况。
    \item \textbf{阶数匹配}：如果题目分母是 $x^3$，可能需要用到三阶展开 $1 + x + \frac{x^2}{2} + \frac{x^3}{6}$。
\end{itemize}
\end{trapbox}