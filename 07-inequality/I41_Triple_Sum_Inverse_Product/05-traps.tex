\begin{trapbox}
\begin{itemize}
    \item \textbf{忽略正数条件}：如果不限定 $a,b,c > 0$，该乘积可能为负数，不等号方向会反转或结论不成立。
    \item \textbf{"1"的代换误区}：在更复杂的题目中，如果常数和不是 1，记得先除以常数。
    \item \textbf{取等条件冲突}：若题目还伴随其他限制（如 $a=2b$），则此时 $a=b=c$ 无法达到，不能直接用 9 作为最小值。
\end{itemize}
\end{trapbox}