\begin{trapbox}
\begin{itemize}
    \item \textbf{方向记反}：这是最常见的错误。记住 $\ln x$ 图象像一把"扣在地上的伞"，弧线（函数值）在上面，弦线（均值）在下面。
    \item \textbf{定义域前提}：必须保证 $x, y > 0$。
    \item \textbf{与指数对比}：$e^x$ 是下凸（$\geqslant$），$\ln x$ 是上凸（$\leqslant$），两者互为反函数，凹凸性刚好相反。
\end{itemize}
\end{trapbox}