\begin{trapbox}
\begin{itemize}
    \item \textbf{范围敏感}：该结论仅在第一象限有效。进入第二象限后 $\tan x$ 为负，结论立即失效。
    \item \textbf{均值不等式误用}：证明过程中 $t + 1/t^2$ 的最小值不是 $2\sqrt{t \cdot 1/t^2}$，因为 $t$ 无法取到令等号成立的值，必须用单调性判定。
    \item \textbf{忽略初值}：在导数大题中，必须交代 $f(0)=0$ 这一关键点，否则单调性无法转化为大小关系。
\end{itemize}
\end{trapbox}