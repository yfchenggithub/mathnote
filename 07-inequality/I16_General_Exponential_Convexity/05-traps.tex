\begin{trapbox}
\begin{itemize}
    \item \textbf{底数陷阱}：注意 $a$ 必须大于 0。若底数为负数，指数函数在实数集无意义。
    \item \textbf{单调性误导}：初学者常误认为减函数（$0<a<1$）的放缩方向会改变。实际上，放缩方向由"凹凸性"决定，而非"单调性"。
    \item \textbf{等号意识}：在求最值时，必须确认 $x=y$ 是否在定义域或已知条件内。
\end{itemize}
\end{trapbox}