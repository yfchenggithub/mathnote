\begin{trapbox}
\begin{itemize}
    \item \textbf{拆项技巧不足}：如例1中，若直接使用二元均值 $\geqslant 2\sqrt{x^2 \cdot \frac{16}{x}} = 8\sqrt{x}$，结果仍含变量，无法求出极值。必须进行等量拆分。
    \item \textbf{忽略非负前提}：$a, b, c$ 必须为正数（或非负数）。如果题目涉及负数，需手动调整正负号。
    \item \textbf{等号验证}：在使用"和定积最大"等结论时，务必检查 $a=b=c$ 时是否符合已知约束。
\end{itemize}
\end{trapbox}