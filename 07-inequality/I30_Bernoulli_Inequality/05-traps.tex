\begin{trapbox}
\begin{itemize}
    \item \textbf{自变量范围}：必须保证 $x \geqslant -1$。若 $x < -1$，幂函数的基数变为负数，结论失效。
    \item \textbf{指数方向}：$n$ 的取值范围决定了不等号的方向。$0 < n < 1$ 时会"反号"，这是很多考生忽略的盲点。
    \item \textbf{精度损失}：伯努利放缩是非常"粗糙"的放缩。如果题目要求精度极高，可能需要结合泰勒展开的更高阶项。
\end{itemize}
\end{trapbox}