\begin{trapbox}
\begin{itemize}
    \item \textbf{忽视符号前提}：只有在 $a, b$ 同号时结论才成立。若一正一负，结论会反转。
    \item \textbf{等号不可达}：在使用结论前，务必检查 $a=b$ 这一条件在给定定义域内是否能够取到。
    \item \textbf{对勾函数单调性}：注意在区间 $(0, 1)$ 上，$x+1/x$ 是递减的；在 $(1, +\infty)$ 上是递增的。不能盲目套用最小值。
\end{itemize}
\end{trapbox}