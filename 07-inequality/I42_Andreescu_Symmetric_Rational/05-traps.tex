\begin{trapbox}
\begin{itemize}
    \item \textbf{分子次数匹配}：本结论要求分子是二次方。如果是 $a/(b+c)$，则结果常数是 $3/2$ 而非 $(a+b+c)/2$。
    \item \textbf{非对称干扰}：如果分母不是循环对称的（如 $b+2c$），则不能直接套用结论，需重新使用权方和公式。
    \item \textbf{取等检验}：在复杂背景下，若约束条件限制了 $a,b,c$ 无法相等，则该结论只能给出下确界，而非可达的最小值。
\end{itemize}
\end{trapbox}