\begin{trapbox}
\begin{itemize}
    \item \textbf{不等号方向}：切记"函数值的平均"在上方。如果记反了，会导致整个导数证明过程逻辑崩溃。
    \item \textbf{忽略基数}：本结论特指底数 $e > 1$ 的情形。如果底数在 $(0, 1)$ 之间，函数图象依然是下凸的，结论方向不变，但增减性不同，需注意区分。
    \item \textbf{推广条件}：在处理 $n$ 个变量时，必须保证每个变量都在函数的下凸区间内。
\end{itemize}
\end{trapbox}