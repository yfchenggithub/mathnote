\begin{statementbox}

	\textbf{\textcolor{CStatement}{条件}}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]

		\item[\textbf{\textcolor{CStatement}{条件1（椭圆与圆周方程）：}}]
		      给定椭圆
		      \[
			      E:\frac{x^2}{a^2}+\frac{y^2}{b^2}=1\quad (a>b>0)
		      \]
		      与圆周
		      \[
			      \Gamma:(x-x_0)^2+(y-y_0)^2=r^2\quad (r>0),
		      \]
		      圆心为 $M(x_0,y_0)$。

		      \textbf{注意：} 本结论中的“圆”均指圆周 $\Gamma$，不是圆面或圆盘。

		\item[\textbf{\textcolor{CStatement}{条件2（距离最值定义）：}}]
		      定义圆心 $M$ 到椭圆 $E$ 上点的距离最值为
		      \[
			      d_{\min}=\min_{P\in E}PM,\qquad d_{\max}=\max_{P\in E}PM.
		      \]
		      定义椭圆 $E$ 与圆周 $\Gamma$ 之间的最小距离和最大距离为
		      \[
			      D_{\min}=\min_{P\in E,\ Q\in\Gamma}PQ,\qquad
			      D_{\max}=\max_{P\in E,\ Q\in\Gamma}PQ.
		      \]

	\end{description}

	\textbf{\textcolor{CStatement}{结论}}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]

		\item[\textbf{\textcolor{CStatement}{结论一（一般距离公式）：}}]
		      \textbf{\textcolor{CStatement}{直观描述：椭圆到圆周的距离由圆心到椭圆的距离范围决定。}}

		      \[
			      D_{\min}
			      =\min_{P\in E}|PM-r|
			      =\min_{t\in[d_{\min},d_{\max}]}|t-r|,
			      \qquad
			      D_{\max}=d_{\max}+r.
		      \]

		\item[\textbf{\textcolor{CStatement}{结论二（最小距离完整分段）：}}]
		      \textbf{\textcolor{CStatement}{直观描述：比较半径 $r$ 与 $d_{\min},d_{\max}$ 的大小即可。}}

		      \[
			      D_{\min}=
			      \begin{cases}
				      d_{\min}-r, & r<d_{\min},                \\[2mm]
				      0,          & d_{\min}\le r\le d_{\max}, \\[2mm]
				      r-d_{\max}, & r>d_{\max}.
			      \end{cases}
		      \]

	\end{description}

	\textbf{\textcolor{CStatement}{位置说明：}}
	当 $r<d_{\min}$ 时，圆周与椭圆无公共点，但不能一概说“圆在椭圆外且外离”，圆周也可能在椭圆内部；
	当 $d_{\min}\le r\le d_{\max}$ 时，椭圆与圆周有公共点；
	当 $r>d_{\max}$ 时，椭圆整体位于圆周所围成的圆盘内部，但本文求的是椭圆到圆周的距离。

\end{statementbox}