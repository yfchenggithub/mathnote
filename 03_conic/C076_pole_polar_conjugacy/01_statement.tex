\begin{statementbox}

	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（曲线非退化）：}}] 二次曲线 $\Gamma: S(x,y)=0$ 为非退化（即 $S(x,y)$ 是二次多项式且无奇点）。
		\item[\textbf{\textcolor{CStatement}{条件 2（点非中心）：}}] 若 $\Gamma$ 为有心曲线（如椭圆、双曲线），点 $P(x_1,y_1), Q(x_2,y_2)$ 都不是曲线的中心。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（极线互换）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			点 $P$ 在点 $Q$ 的极线上等价于点 $Q$ 在点 $P$ 的极线上。
			\[
				P\in l_Q \iff Q\in l_P
			\]
		\item[\textbf{\textcolor{CStatement}{推广结论（代数判定）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			共轭关系等价于 $P$ 的坐标满足 $Q$ 的极线方程（即 $S_P(x_2,y_2)=0$）。
			\[
				P\in l_Q \iff S_P(x_2,y_2)=0
			\]
	\end{description}

	% 可选补充块
	\textbf{\textcolor{CStatement}{使用提醒：}}
	对于有心二次曲线，中心点没有极线，故本结论不适用于中心点。

\end{statementbox}
