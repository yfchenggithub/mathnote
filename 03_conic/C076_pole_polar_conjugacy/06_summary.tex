\begin{summarybox}

	\textbf{\textcolor{CSummary}{一句话核心}}

	极点极线共轭性：$P\in l_Q \iff Q\in l_P \iff S_P(x_2,y_2)=0$，体现二次曲线的几何对称性，是极点极线理论的基本性质。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（非退化曲线）：} 二次曲线必须非退化，即 $S(x,y)$ 为二次不可约多项式。
		\item \textbf{条件 2（点非中心）：} 对于有心二次曲线，点 $P,Q$ 不能是曲线的中心；抛物线（无心）无条件限制。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（中心除外）：} 有心曲线的中心无极线，应用共轭性前先判断点是否为中心点。
		\item \textbf{检查项（系数正确）：} 使用极线方程 $S_P(x,y)=0$ 时，注意交叉项系数为 $\frac{B}{2}$，一次项系数为 $\frac{D}{2},\frac{E}{2}$，不能漏半系数。
	\end{itemize}

\end{summarybox}
