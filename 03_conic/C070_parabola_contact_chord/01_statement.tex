\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（抛物线标准型）：}}] 抛物线 $C: y^2 = 2px$ ($p>0$)，点 $P(x_0, y_0)$ 在 $C$ 的外部。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（y平方型）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			切点弦方程为 $y_0 y = p(x + x_0)$，形式与切线方程类似。
			\[
				\boxed{y_0 y = p(x + x_0)}
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（x平方型）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			若抛物线开口向上，切点弦方程为 $x_0 x = p(y + y_0)$。
			\[
				\boxed{x_0 x = p(y + y_0)}
			\]
	\end{description}
\end{statementbox}
