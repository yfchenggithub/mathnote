\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	切点弦方程可直接用点坐标替换抛物线方程中的一次项得到。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（标准方程）：} 抛物线必须是标准形式 $y^2=2px$ 或 $x^2=2py$，且 $p>0$。
		\item \textbf{条件2（点位置）：} 点 $P(x_0,y_0)$ 必须在抛物线外部。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（区分开口）：} $y^2$ 型用 $y_0 y = p(x+x_0)$，$x^2$ 型用 $x_0 x = p(y+y_0)$。
		\item \textbf{检查项（验证外部）：} 使用前务必确认点在抛物线外部。
	\end{itemize}
\end{summarybox}
