\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	过抛物线外一点作两条切线，切点连线的方程可以直接将点坐标代入抛物线方程的一次项得到。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（切点弦定义）：}] 抛物线外一点与两个切点的连线叫做切点弦。
		\item[\textbf{要点二（公式记忆法）：}] 对比切线方程，将 $y^2$ 换成 $y_0 y$，将 $2px$ 换成 $p(x+x_0)$（或 $x^2$ 换成 $x_0 x$，$2py$ 换成 $p(y+y_0)$）。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（两切点连线）}}

	切点弦本质就是两个切点确定的直线。推导时，利用两切点处的切线方程，并结合点 $P$ 同时在两条切线上，得到两个等式，这两个等式恰好说明两个切点在同一条直线上。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（同构法）}}

	设两切点，利用切线方程代入点 $P$ 得到两个等式，这两个等式的形式完全一致，表明两个切点的坐标均满足直线方程 $y_0 y = p(x + x_0)$，从而直接得到切点弦方程。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（快速得分）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（选择填空）：} 直接套公式求切点弦，速度快。
		\item \textbf{考法二（解答题先证后用）：} 先写出结论再证明，节省思考时间。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	将切点弦方程与切线方程对比：切线 $y_1 y = p(x+x_1)$ 与切点弦 $y_0 y = p(x+x_0)$ 形式完全相同，只是将切点坐标换成了已知点坐标。这说明结论可由切线方程直接“转移”得到。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（求切点弦）：} 已知抛物线外一点，求切点弦方程。
		\item \textbf{场景二（反求点坐标）：} 已知切点弦方程，求对应的点。
	\end{itemize}
\end{explanationbox}
