\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（点椭圆外）：}}] 椭圆方程为 $\frac{x^2}{a^2}+\frac{y^2}{b^2}=1$（$a,b>0$），点 $P(x_0,y_0)$ 满足 $\frac{x_0^2}{a^2}+\frac{y_0^2}{b^2}>1$。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（切点弦方程）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			将椭圆方程中的 $x^2$ 换为 $x_0x$，$y^2$ 换为 $y_0y$，即得切点弦所在直线。
			\[
				\boxed{\frac{x_0x}{a^2}+\frac{y_0y}{b^2}=1}.
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（点在椭圆上）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			若点 $P$ 在椭圆上（即满足 $\frac{x_0^2}{a^2}+\frac{y_0^2}{b^2}=1$），则直线变为椭圆在点 $P$ 处的切线方程。
			\[
				\frac{x_0x}{a^2}+\frac{y_0y}{b^2}=1.
			\]
	\end{description}
\end{statementbox}
