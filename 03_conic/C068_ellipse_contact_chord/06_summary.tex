\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{过椭圆外一点$P(x_0,y_0)$的切点弦方程为$\frac{x_0x}{a^2}+\frac{y_0y}{b^2}=1$，相当于将椭圆方程中的平方项用乘积代替。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（点P在椭圆外）：} $\frac{x_0^2}{a^2}+\frac{y_0^2}{b^2}>1$，以保证存在两条切线。
		\item \textbf{条件2（椭圆标准式）：} 椭圆方程必须是$\frac{x^2}{a^2}+\frac{y^2}{b^2}=1$（$a,b>0$），且切线公式仅限于标准形式。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（区分点位置）：} 点$P$在椭圆上时，该方程变为切线方程；点$P$在椭圆内时，该方程不表示切点弦（没有切线），也不表示过点$P$的弦。
		\item \textbf{检查项（分母为平方）：} 公式中是$\frac{x_0x}{a^2}$而不是$\frac{x_0x}{a}$，务必确认分母为平方。
	\end{itemize}
\end{summarybox}
