\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知椭圆 $\frac{x^2}{4}+\frac{y^2}{3}=1$，点 $P(4,3)$ 在椭圆外。求过点 $P$ 的椭圆两条切线的切点弦所在直线方程。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别系数）：}] 确定椭圆方程的 $a^2=4$，$b^2=3$。
			\par\textbf{理由：} 标准形式 $\frac{x^2}{a^2}+\frac{y^2}{b^2}=1$。
		\item[\textbf{第二步（验证条件）：}] 计算 $\frac{4^2}{4}+\frac{3^2}{3}=4+3=7>1$，满足椭圆外的条件，故可应用切点弦公式。
			\par\textbf{理由：} 只有点 $P$ 在椭圆外时两条切线才存在。
		\item[\textbf{第三步（套用公式）：}] 将 $x_0=4$，$y_0=3$ 代入切点弦公式 $\frac{x_0x}{a^2}+\frac{y_0y}{b^2}=1$，得 $\frac{4x}{4}+\frac{3y}{3}=1$，化简为 $x+y=1$。
			\par\textbf{理由：} 公式直接给出切点弦方程。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{所求切点弦方程为 $\boxed{x+y=1}$。}

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知椭圆 $\frac{x^2}{9}+\frac{y^2}{4}=1$，点 $P(6,2)$。求过点 $P$ 的切点弦方程。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别系数）：}] $a^2=9$，$b^2=4$。
			\par\textbf{理由：} 标准形式。
		\item[\textbf{第二步（验证条件）：}] $\frac{6^2}{9}+\frac{2^2}{4}=4+1=5>1$，点 $P$ 在椭圆外。
			\par\textbf{理由：} 条件保证有两条切线。
		\item[\textbf{第三步（套用化简）：}] 代入公式 $\frac{x_0x}{a^2}+\frac{y_0y}{b^2}=1$ 得 $\frac{6x}{9}+\frac{2y}{4}=1$，即 $\frac{2x}{3}+\frac{y}{2}=1$。两边乘以 $6$ 得 $4x+3y=6$。
			\par\textbf{理由：} 公式代入后化简。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{切点弦方程为 $\boxed{4x+3y=6}$。}

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知椭圆 $\frac{x^2}{4}+y^2=1$，一条直线 $l$ 的方程为 $2x+y=1$，且 $l$ 是过椭圆外一点 $P$ 作椭圆两条切线的切点弦。求点 $P$ 的坐标。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别公式）：}] 椭圆 $\frac{x^2}{4}+y^2=1$ 即 $\frac{x^2}{4}+\frac{y^2}{1}=1$，所以 $a^2=4$，$b^2=1$。切点弦公式是 $\frac{x_0x}{a^2}+\frac{y_0y}{b^2}=1$，即 $\frac{x_0x}{4}+\frac{y_0y}{1}=1$，也就是 $\frac{x_0}{4}x + y_0y = 1$。
			\par\textbf{理由：} 由标准形式确定系数。
		\item[\textbf{第二步（比较系数）：}] 已知切点弦方程为 $2x+y=1$，与公式对应得 $\frac{x_0}{4}=2$，$y_0=1$。
			\par\textbf{理由：} 两个方程表示同一条直线，且常数项均为 $1$，所以对应系数相等。
		\item[\textbf{第三步（求解验证）：}] 解得 $x_0=8$，$y_0=1$。验证点 $P(8,1)$ 是否在椭圆外：$\frac{8^2}{4}+\frac{1^2}{1}=16+1=17>1$，满足条件。
			\par\textbf{理由：} 点 $P$ 必须在椭圆外才有两条切线。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{点 $P$ 的坐标为 $\boxed{(8,1)}$。}
\end{examplebox}
