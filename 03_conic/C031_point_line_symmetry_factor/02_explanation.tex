\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	点关于直线的对称点，等价于从点出发，沿法向量的单位方向移动 $-2d$，其中 $d$ 是点到直线的有向距离。

	\medskip

	\textbf{\textcolor{CExplanation}{核心拆解}}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（有向距离）：}]
		      首先计算点 $P$ 到直线 $l$ 的有向距离
		      \[
			      d = \dfrac{Ax_0+By_0+C}{\sqrt{A^2+B^2}}.
		      \]
		      对称因子为
		      \[
			      t = \dfrac{d}{\sqrt{A^2+B^2}}
			      = \dfrac{Ax_0+By_0+C}{A^2+B^2}.
		      \]
		      这里的 $d$ 是相对于所选一般式 $Ax+By+C=0$ 的有向距离；若直线方程整体乘以非零常数，有向距离的符号可能同步变化，但最终对称点坐标不变。

		\item[\textbf{要点二（法向量移动）：}]
		      设
		      \[
			      \mathbf n=\dfrac{(A,B)}{\sqrt{A^2+B^2}},
		      \]
		      则对称点满足
		      \[
			      P' = P - 2d\,\mathbf n.
		      \]
		      也就是
		      \[
			      P' = P - 2d\cdot\frac{(A,B)}{\sqrt{A^2+B^2}},
		      \]
		      整理即得
		      \[
			      x' = x_0 - 2At,\qquad y' = y_0 - 2Bt.
		      \]
	\end{description}

	\medskip

	\textbf{\textcolor{CExplanation}{几何本质（对称轴与法向量）}}

	直线 $l$ 的法向量 $(A,B)$ 决定了垂直于直线的方向。点 $P$ 与 $P'$ 的连线垂直于 $l$，且中点落在 $l$ 上；公式正是用法向量直接刻画了这一关系。

	\medskip

	\textbf{\textcolor{CExplanation}{代数意义（消元定比分点）}}

	若设 $P'(x',y')$，由中点在线 $l$ 上且 $PP'$ 垂直 $l$ 可列方程组；利用对称因子 $t$ 可避免解此方程组，直接给出坐标表达式。

	\medskip

	\textbf{\textcolor{CExplanation}{考点价值（高效计算）}}

	在解析几何中，遇到点关于直线的对称问题时，使用该公式可省去列方程组的过程，减少计算量，提高解题速度。

	\medskip

	\textbf{\textcolor{CExplanation}{顿悟点}}

	\textbf{本质上，$t$ 是带符号的法向修正因子。$2At,2Bt$ 是沿法向量方向分解出来的修正量，具体方向由 $Ax_0+By_0+C$ 的符号自动决定。}

	\medskip

	\textbf{\textcolor{CExplanation}{使用场景}}

	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（求对称点）：} 已知点 $P$ 和直线 $l$，直接代入公式求对称点 $P'$。
		\item \textbf{场景二（反射光线）：} 若光线从点 $A$ 经直线镜面 $l$ 反射后到点 $B$，可将 $A$ 或 $B$ 关于 $l$ 作对称，把反射路径问题转化为一条直线与 $l$ 的交点问题。
	\end{itemize}
\end{explanationbox}