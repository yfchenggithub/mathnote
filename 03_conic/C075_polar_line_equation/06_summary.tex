\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	\textbf{极线方程是将圆锥曲线方程中的坐标替换为点 $P$ 的坐标后得到的一次直线方程；它统一了切线和切点弦。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（非退化二次曲线）：} 曲线应为非退化的圆锥曲线（标准椭圆/双曲线/抛物线，或一般二次曲线）。
		\item \textbf{条件 2（已知点坐标）：} 需要知道点 $P$ 的坐标 $(x_0,y_0)$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（系数减半）：} 对一般二次曲线，$xy$ 项、$x$ 项、$y$ 项系数均需折半处理。
		\item \textbf{检查项（曲线非退化）：} 使用前确认曲线非退化，否则极线无定义。
	\end{itemize}

\end{summarybox}
