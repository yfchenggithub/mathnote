\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知椭圆 $\frac{x^2}{9}+\frac{y^2}{4}=1$，点 $P(3,1)$。求点 $P$ 关于椭圆的极线方程。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别曲线类型）：}]
			观察曲线为标准椭圆，适用公式 $\frac{x_0x}{a^2}+\frac{y_0y}{b^2}=1$。
			\par\textbf{理由：} 由结论一，椭圆 $\frac{x^2}{a^2}+\frac{y^2}{b^2}=1$ 对应极线 $\frac{x_0x}{a^2}+\frac{y_0y}{b^2}=1$。
		\item[\textbf{第二步（代入坐标与参数）：}]
			此处 $a^2=9,b^2=4$，$x_0=3,y_0=1$，代入得 $\frac{3x}{9}+\frac{1y}{4}=1$，即 $\frac{x}{3}+\frac{y}{4}=1$。
			\par\textbf{理由：} 将点坐标直接代入公式。
		\item[\textbf{第三步（化简得直线方程）：}]
			整理为 $4x+3y=12$。
			\par\textbf{理由：} 两边乘以12去分母。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{极线方程为 $4x+3y=12$。}

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知抛物线 $y^2=8x$ 上一点 $P(2,4)$。求点 $P$ 关于抛物线的极线方程。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（化为标准形式）：}]
			抛物线 $y^2=8x$ 对应 $2p=8$，即 $p=4$。
			\par\textbf{理由：} 标准抛物线 $y^2=2px$。
		\item[\textbf{第二步（代入极线公式）：}]
			根据结论二，极线方程为 $y_0y=p(x+x_0)$。代入 $y_0=4,p=4,x_0=2$ 得 $4y=4(x+2)$。
			\par\textbf{理由：} $P$ 在抛物线上，此即切线方程。
		\item[\textbf{第三步（化简求结果）：}]
			两边除以 $4$ 得 $y=x+2$。
			\par\textbf{理由：} 代数化简。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{切线方程为 $y=x+2$。}

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知圆锥曲线 $\Gamma: x^2+xy+y^2-2x-2y+1=0$，点 $P(2,0)$ 在 $\Gamma$ 外部。求点 $P$ 关于 $\Gamma$ 的极线方程。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（提取系数）：}]
			对照一般式 $Ax^2+Bxy+Cy^2+Dx+Ey+F=0$，得 $A=1,B=1,C=1,D=-2,E=-2,F=1$。
			\par\textbf{理由：} 正确识别系数以便代入。
		\item[\textbf{第二步（代入替换公式）：}]
			极线方程为 $A x_0 x+\frac{B}{2}(x_0 y+y_0 x)+C y_0 y+\frac{D}{2}(x+x_0)+\frac{E}{2}(y+y_0)+F=0$。代入 $x_0=2,y_0=0$，得
			\[
				1\cdot2\cdot x+\frac{1}{2}(2\cdot y+0\cdot x)+1\cdot0\cdot y+\frac{-2}{2}(x+2)+\frac{-2}{2}(y+0)+1=0,
			\]
			即 $2x + y - (x+2) - y + 1 = 0$。
			\par\textbf{理由：} 逐项带入，注意系数 $\frac{B}{2}$ 等。
		\item[\textbf{第三步（合并化简）：}]
			整理得 $2x+y-x-2-y+1=0$，即 $x-1=0$，故 $x=1$。
			\par\textbf{理由：} 合并同类项后得到。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{极线方程为 $x=1$。}
\end{examplebox}
