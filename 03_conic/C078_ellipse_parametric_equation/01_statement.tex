\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（椭圆标准形式）：}}]
			椭圆方程为 $\frac{x^2}{a^2}+\frac{y^2}{b^2}=1\;(a>b>0)$，中心在原点，焦点在 $x$ 轴上。
	\end{description}
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（参数方程表示）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			用离心角 $\theta$ 表示椭圆上点的坐标，形式统一。
			\[
				\begin{cases}
					x = a\cos\theta,\\ y = b\sin\theta,
			\end{cases}
				\quad \theta\in\mathbb{R}.
			\]
		\item[\textbf{\textcolor{CStatement}{推广结论（焦点在 y 轴）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			长轴在 $y$ 轴时，参数方程中 $a,b$ 位置互换。
			\[
				\begin{cases}
					x = b\cos\theta,\\ y = a\sin\theta,
			\end{cases}
				\quad \frac{x^2}{b^2}+\frac{y^2}{a^2}=1\;(a>b>0),\quad \theta\in\mathbb{R}.
			\]
	\end{description}

\end{statementbox}
