\begin{proofbox}
	\textbf{\textcolor{CProof}{思路提示}}
	直接代入椭圆方程，利用三角恒等式验证。

	\medskip
	\textbf{\textcolor{CProof}{正式推导}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.55em]
		\item[\textbf{步骤一（参数假设）：}]
			对任意 $\theta$，设点 $P(a\cos\theta,b\sin\theta)$。
			\par\textbf{理由：} 这是待验证的参数表示。
		\item[\textbf{步骤二（代入方程）：}]
			代入 $\frac{x^2}{a^2}+\frac{y^2}{b^2}$：
			\[
				\frac{(a\cos\theta)^2}{a^2}+\frac{(b\sin\theta)^2}{b^2}.
			\]
			\par\textbf{理由：} 检验是否满足椭圆方程。
		\item[\textbf{步骤三（化简结果）：}]
			化简得 $\cos^2\theta+\sin^2\theta=1$。
			\[
				\begin{aligned}
					\frac{a^2\cos^2\theta}{a^2}+\frac{b^2\sin^2\theta}{b^2} &= \cos^2\theta+\sin^2\theta \\ &= 1.
			\end{aligned}
			\]
			\par\textbf{理由：} 运用三角恒等式。
		\item[\textbf{步骤四（参遍性说明）：}]
			$\theta\in[0,2\pi)$ 时，$(a\cos\theta,b\sin\theta)$ 恰好取遍椭圆上所有点。
			\par\textbf{理由：} $\cos\theta,\sin\theta$ 有界且连续，覆盖整个椭圆。
	\end{description}

	\medskip
	\textbf{\textcolor{CProof}{结论回扣}}
	\textbf{因此参数方程 $
	\begin{cases}
		x=a\cos\theta\\ y=b\sin\theta
	\end{cases}
	$ 是椭圆 $\frac{x^2}{a^2}+\frac{y^2}{b^2}=1$ 的一种等价表示。}
\end{proofbox}
