\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	\textbf{抛物线 $y^2=2px$ 上两点 $A,B$，切线交点 $M$ 与弦中点 $N$ 纵坐标相同；$x^2=2py$ 时横坐标相同。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（标准形式）：} 抛物线方程必须是 $y^2=2px$ 或 $x^2=2py$（$p\neq0$）。
		\item \textbf{条件 2（两点不同）：} $A,B$ 为抛物线上两个不同的点。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（类型辨别）：} 分清抛物线类型，确保使用正确的坐标相等结论。
		\item \textbf{检查项（参数对应）：} 先将方程化为标准形式，正确找出 $p$ 的值。
	\end{itemize}

\end{summarybox}
