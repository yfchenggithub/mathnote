\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}
	对于抛物线 $y^2=2px$，过两点的切线交点 $M$ 与弦中点 $N$ 的纵坐标恰好相等，这意味着 $MN$ 水平。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（切线方程形式）：}]
			过点 $(x_1,y_1)$ 的切线方程为 $yy_1 = p(x+x_1)$，类似过点 $(x_2,y_2)$ 的切线方程为 $yy_2 = p(x+x_2)$。
		\item[\textbf{要点二（坐标相等推导）：}]
			联立两切线方程，相减并利用 $y_i^2=2px_i$ 消去 $x_i$，得 $y_M = \frac{y_1+y_2}{2}$；同时 $N$ 的纵坐标也是 $\frac{y_1+y_2}{2}$，故相等。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（轴对称特性）}}
	切线交点与弦中点的连线平行于抛物线的对称轴，这是抛物线区别于椭圆、双曲线的一个特殊性质。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（导数运算）}}
	使用隐函数求导得 $y' = \frac{p}{y}$，切线斜率为 $\frac{p}{y_1}$，代入点斜式可推出相同结论。代数化简中关键步骤是平方差消元。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（快速计算）}}
	利用此结论可快速得到切线交点的一个坐标，减少计算量。
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（求交点坐标）：} 已知两点坐标，直接得交点的一个坐标，再代入一条切线得另一个坐标。
		\item \textbf{考法二（判断平行）：} 已知弦中点，可推切线交点位置，或已知 $MN$ 方向判断线与对称轴关系。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	为什么纵坐标会相等？因为切线方程中 $x$ 与 $y$ 存在线性关系，相减后消去 $x$ 时利用抛物线方程将 $x_i$ 替换为 $\frac{y_i^2}{2p}$，从而出现 $(y_1^2-y_2^2)$ 因式分解得 $(y_1-y_2)(y_1+y_2)$，与分母 $(y_1-y_2)$ 约去后即得平均值。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（弦中点已知）：} 已知弦 $AB$ 的中点，求两切线交点坐标（只需再求一个坐标）。
		\item \textbf{场景二（切线交点已知）：} 已知两切线交点，可反推弦中点的坐标，进而得弦所在直线方程。
	\end{itemize}

\end{explanationbox}
