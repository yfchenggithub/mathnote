\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知抛物线 $y^2=4x$ 上两点 $A(1,2)$，$B(4,4)$。求弦 $AB$ 的中点 $N$，过 $A,B$ 的切线交点 $M$，并判断 $MN$ 的方向。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（弦中点）：}]
			由中点公式得
			\[
				N\left(\frac{1+4}{2},\frac{2+4}{2}\right) = \left(\frac{5}{2},3\right).
			\]
			\par\textbf{理由：} 中点坐标公式。
		\item[\textbf{第二步（切线交点）：}]
			抛物线 $y^2=4x$ 对应 $p=2$。过 $A$ 的切线为 $yy_1 = p(x+x_1)$，即 $2y = 2(x+1)$，化简得 $y=x+1$。过 $B$ 的切线为 $4y = 2(x+4)$，即 $y=\frac{1}{2}x+2$。联立
			\[
				\begin{cases}
					y = x+1,\\ y = \frac{1}{2}x+2,
			\end{cases}
			\]
			解得 $x=2$，$y=3$。故 $M(2,3)$。
			\par\textbf{理由：} 代入切线公式并解方程组。
		\item[\textbf{第三步（平行验证）：}]
			由于 $y_M = 3$，$y_N = 3$，故 $y_M = y_N$，直线 $MN$ 平行于 $x$ 轴。
			\par\textbf{理由：} 纵坐标相等时连线平行于 $x$ 轴。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	$M$ 与 $N$ 纵坐标相同，$MN\parallel x$ 轴。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知抛物线 $x^2=4y$ 上两点 $A(2,1)$，$B(-2,1)$。求弦 $AB$ 的中点 $N$，过 $A,B$ 的切线交点 $M$，并判断 $MN$ 的方向。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（弦中点）：}]
			$N\left(\frac{2+(-2)}{2},\frac{1+1}{2}\right) = (0,1)$。
			\par\textbf{理由：} 中点坐标公式。
		\item[\textbf{第二步（切线交点）：}]
			抛物线 $x^2=4y$ 对应 $p=2$。过 $A$ 的切线为 $xx_1 = p(y+y_1)$，即 $2x = 2(y+1)$，化简得 $x = y+1$。过 $B$ 的切线为 $-2x = 2(y+1)$，即 $x = -y-1$。联立
			\[
				\begin{cases}
					x = y+1,\\ x = -y-1,
			\end{cases}
			\]
			解得 $x=0$，$y=-1$。故 $M(0,-1)$。
			\par\textbf{理由：} 代入切线公式并解方程组。
		\item[\textbf{第三步（平行验证）：}]
			$x_M = 0$，$x_N = 0$，故 $x_M = x_N$，直线 $MN$ 平行于 $y$ 轴。
			\par\textbf{理由：} 横坐标相等时连线平行于 $y$ 轴。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	$M$ 与 $N$ 横坐标相同，$MN\parallel y$ 轴。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知抛物线 $y^2=2x$ 上两点 $A,B$，过 $A,B$ 的切线交于 $M$，弦 $AB$ 中点为 $N$。若直线 $AB$ 的斜率为 $1$，且 $|MN| = 2$，求直线 $AB$ 的方程。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（确认平行条件）：}]
			由结论知 $y_M=y_N$，故 $MN\parallel x$ 轴，从而 $|MN| = |x_M-x_N|$。
			\par\textbf{理由：} 结论指出切线交点与弦中点纵坐标相等，所以连线平行于 $x$ 轴。
		\item[\textbf{第二步（长度公式推导）：}]
			设 $A(\frac{y_1^2}{2},y_1), B(\frac{y_2^2}{2},y_2)$。切线方程 $yy_i = 1\cdot(x+x_i)$，结合 $y_M = \frac{y_1+y_2}{2}$ 可求得 $x_M = \frac{y_1y_2}{2}$。而 $x_N = \frac{x_1+x_2}{2} = \frac{y_1^2+y_2^2}{4}$。所以
			\[
				\begin{aligned}
					|MN| &= \left|\frac{y_1y_2}{2} - \frac{y_1^2+y_2^2}{4}\right| \\ &= \frac{(y_1-y_2)^2}{4}.
			\end{aligned}
			\]
			代入 $|MN|=2$ 得 $(y_1-y_2)^2=8$。
			\par\textbf{理由：} 导数法求切线，利用点在抛物线上代入化简可得 $x_M$ 表达式；中点坐标公式得 $x_N$。
		\item[\textbf{第三步（求解直线方程）：}]
			由 $k_{AB}=1$，即 $\frac{y_2-y_1}{x_2-x_1}=1$。而 $x_2-x_1 = \frac{y_2^2 - y_1^2}{2} = \frac{(y_2-y_1)(y_2+y_1)}{2}$，代入得 $1 = \frac{2}{y_1+y_2}$，解得 $y_1+y_2=2$。 联立 $y_1+y_2=2$ 与 $(y_1-y_2)^2=8$，可得 $y_1y_2 = -1$，故 $y_1,y_2$ 为方程 $t^2-2t-1=0$ 的根：$t=1\pm\sqrt{2}$。对应 $x_i = \frac{y_i^2}{2} = \frac{3\pm2\sqrt{2}}{2}$。 弦 $AB$ 中点 $N(\frac{x_1+x_2}{2},\frac{y_1+y_2}{2}) = (\frac{3}{2},1)$，又斜率 $k=1$，所以 $AB$ 方程为 $y-1 = x-\frac{3}{2}$，即 $y = x-\frac{1}{2}$。
			\par\textbf{理由：} 斜率公式转化，根与系数关系，点斜式求直线。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	当 $k=1$，$|MN|=2$ 时，直线 $AB$ 为 $y = x-\frac{1}{2}$。
\end{examplebox}
