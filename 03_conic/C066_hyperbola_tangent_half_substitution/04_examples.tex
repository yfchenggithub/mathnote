\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知双曲线 $\dfrac{x^{2}}{4}-\dfrac{y^{2}}{3}=1$，点 $P(4,3)$ 在该双曲线上，求过点 $P$ 的切线方程。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（验证点在线上）：}] 计算 $\dfrac{4^{2}}{4}-\dfrac{3^{2}}{3}=4-3=1$，故点 $P$ 在双曲线上。
			\par\textbf{理由：} 切线公式要求切点在曲线上。
		\item[\textbf{第二步（套用切线公式）：}] 根据结论 $\dfrac{x_0x}{a^{2}}-\dfrac{y_0y}{b^{2}}=1$，代入 $a^{2}=4$，$b^{2}=3$，$x_0=4$，$y_0=3$，得
			\[
				\frac{4x}{4}-\frac{3y}{3}=1.
			\]
			\par\textbf{理由：} 双曲线切线方程的直接代换。
		\item[\textbf{第三步（化简方程）：}] 化简得 $x - y = 1$。
			\par\textbf{理由：} 系数约分。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{切线方程为 $x - y = 1$。}

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知双曲线 $\dfrac{y^{2}}{5}-\dfrac{x^{2}}{4}=1$，点 $P(2,\sqrt{10})$ 在该双曲线上，求过点 $P$ 的切线方程。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（验证点在线上）：}] 计算 $\dfrac{(\sqrt{10})^{2}}{5}-\dfrac{2^{2}}{4}=\dfrac{10}{5}-\dfrac{4}{4}=2-1=1$，点 $P$ 在双曲线上。
			\par\textbf{理由：} 满足曲线方程。
		\item[\textbf{第二步（判断双曲线类型）：}] 该双曲线焦点在 $y$ 轴，标准形式为 $\dfrac{y^{2}}{a^{2}}-\dfrac{x^{2}}{b^{2}}=1$，因此对应切线公式为 $\dfrac{y_0y}{a^{2}}-\dfrac{x_0x}{b^{2}}=1$。
			\par\textbf{理由：} 焦点在 $y$ 轴时 $x$ 与 $y$ 的位置互换。
		\item[\textbf{第三步（代入数据）：}] $a^{2}=5$，$b^{2}=4$，$x_0=2$，$y_0=\sqrt{10}$，代入得
			\[
				\frac{\sqrt{10}\,y}{5}-\frac{2x}{4}=1.
			\]
			\par\textbf{理由：} 直接代换公式。
		\item[\textbf{第四步（化简方程）：}] 化简为 $\dfrac{\sqrt{10}}{5}y - \dfrac{1}{2}x = 1$，或两边乘以10得 $2\sqrt{10}y - 5x = 10$。
			\par\textbf{理由：} 去分母。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{切线方程为 $2\sqrt{10}y - 5x = 10$。}

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知双曲线 $\dfrac{x^{2}}{2} - y^{2} = 1$，一条直线 $l$ 与双曲线切于点 $P$，且直线 $l$ 过点 $A(1,2)$，求切点 $P$ 的坐标。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（设切点坐标）：}] 设切点 $P(x_0,y_0)$，则 $\dfrac{x_0^{2}}{2} - y_0^{2} = 1$。
			\par\textbf{理由：} 点 $P$ 在双曲线上。
		\item[\textbf{第二步（写出切线方程）：}] 由切线公式，切线方程为 $\dfrac{x_0x}{2} - y_0y = 1$。
			\par\textbf{理由：} 此处 $a^{2}=2$，$b^{2}=1$。
		\item[\textbf{第三步（切线过定点）：}] 切线过 $A(1,2)$，代入得 $\dfrac{x_0}{2} - 2y_0 = 1$。
			\par\textbf{理由：} 点 $A$ 在切线上。
		\item[\textbf{第四步（联立方程组）：}] 联立
			\[
				\begin{cases}
					\dfrac{x_0^{2}}{2} - y_0^{2} = 1,\\\\[6pt]
					\dfrac{x_0}{2} - 2y_0 = 1.
			\end{cases}
			\]
			\par\textbf{理由:} 切点满足曲线和切线条件.
		\item[\textbf{第五步(解方程组):}] 由第二式得 $x_0 = 2(1+2y_0)$,代入第一式:
			\[
				\begin{aligned}
					\frac{[2(1+2y_0)]^{2}}{2} - y_0^{2} &= 1 \\\\
					\frac{4(1+4y_0+4y_0^{2})}{2} - y_0^{2} &= 1 \\\\
					2(1+4y_0+4y_0^{2}) - y_0^{2} &= 1 \\\\
					2+8y_0+8y_0^{2} - y_0^{2} &= 1 \\\\
					7y_0^{2} + 8y_0 + 1 &= 0.
			\end{aligned}
			\]
			解得 $y_0 = -1$ 或 $y_0 = -\dfrac{1}{7}$.
			\par\textbf{理由:} 代入消元,解二次方程.
		\item[\textbf{第六步(求对应 $x_0$):}] 当 $y_0=-1$ 时,$x_0 = 2(1-2) = -2$;当 $y_0=-\dfrac{1}{7}$ 时,$x_0 = 2\left(1-\dfrac{2}{7}\right) = \dfrac{10}{7}$.故切点为 $(-2,-1)$ 或 $(\dfrac{10}{7},-\dfrac{1}{7})$.
			\par\textbf{理由:} 代回 $x_0$ 表达式.
	\end{description}

	\textbf{\textcolor{CExample}{关键结论:}}
	\textbf{切点坐标为 $\left(-2,-1\right)$ 或 $\left(\dfrac{10}{7},-\dfrac{1}{7}\right)$.}
\end{examplebox}
