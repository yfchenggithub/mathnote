\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}
	椭圆外一点 $P$ 如果向椭圆引两条互相垂直的切线，那么 $P$ 一定在固定圆 $x^2+y^2=a^2+b^2$ 上；反过来，圆上任意一点向椭圆引两条切线，它们必然垂直。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（切线存在条件）：}]
			设过 $P(x_0,y_0)$ 的切线方程为 $y-y_0 = k(x-x_0)$，与椭圆联立后令判别式 $\Delta=0$，得到关于斜率 $k$ 的二次方程；根的存在性保证 $k$ 有两个值，对应两条切线。
		\item[\textbf{要点二（垂直条件推导）：}]
			两条切线斜率 $k_1,k_2$ 满足 $k_1k_2=-1$。利用上一步得到的 $k_1k_2$ 表达式（由 $x_0,y_0$ 表示），代入 $k_1k_2=-1$ 即可消去 $k$，得到 $x_0^2+y_0^2=a^2+b^2$。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（对称性解析）}}
	蒙日圆的圆心是椭圆的中心，半径平方 $a^2+b^2$ 恰好是椭圆长半轴与短半轴的平方和。这反映了椭圆对称轴方向上的等量关系：当两条切线垂直时，它们的倾斜角相差 $90^\circ$，几何上对应着切点弦的某种正交性质。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（消参求轨迹）}}
	通过“设切线——联立——判别式——垂直条件”四步，最终消去参数斜率，得到仅含 $P$ 点坐标的关系式。本质上是一个“参数消去”的典型范例，也是解析几何中“交轨法”求轨迹的体现。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（运算优化）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（轨迹快速判断）：} 见到椭圆外一点引两条垂直切线，直接说明该点在蒙日圆上，立即写出轨迹方程，省去联立推导。
		\item \textbf{考法二（垂直性质应用）：} 已知点 $P$ 在蒙日圆上，则过 $P$ 的椭圆切线互相垂直，可借此构建等量关系解决后续问题。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	\textbf{垂直条件 $k_1k_2=-1$ 是连接点坐标与切线斜率的关键桥梁；最终消参时不需要分别求出 $k_1,k_2$，而是直接使用韦达定理得到 $k_1k_2$，简化计算。}

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（垂直验证）：} 给定椭圆外一点，问它引出的两条切线是否垂直？只要代入蒙日圆方程检验即可。
		\item \textbf{场景二（定值计算）：} 涉及椭圆切线上某点到原点的距离或垂直关系时，可利用蒙日圆快速确定变量关系。
	\end{itemize}

\end{explanationbox}
