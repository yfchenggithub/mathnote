\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	\textbf{椭圆外一点引两条互相垂直的切线的交点轨迹是圆 $x^2+y^2=a^2+b^2$，反之亦然。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（椭圆标准型）：} 已知椭圆的标准方程 $\frac{x^2}{a^2}+\frac{y^2}{b^2}=1$，且 $a>b>0$。
		\item \textbf{条件 2（垂直前提）：} 点 $P$ 在椭圆外部，且过 $P$ 的两条椭圆切线互相垂直。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（忽视外部条件）：} 必须确保点 $P$ 在椭圆外，否则无法作两条实切线。
		\item \textbf{检查项（参数识别）：} 在应用蒙日圆公式时，正确识别椭圆的 $a^2$ 和 $b^2$，避免混淆。
	\end{itemize}

\end{summarybox}
