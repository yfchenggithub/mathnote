\begin{statementbox}

	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（标准抛物线）：}}] 已知抛物线 $C: y^2=2px\ (p>0)$，焦点 $F\bigl(\frac{p}{2},0\bigr)$．过 $F$ 的直线交 $C$ 于 $A(x_1,y_1)$，$B(x_2,y_2)$．
	\end{description}

	\medskip

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（横坐标积）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			焦点弦两端点横坐标之积为定值，仅与 $p$ 有关．
			\[
				x_1x_2 = \frac{p^2}{4}.
			\]

		\item[\textbf{\textcolor{CStatement}{结论二（纵坐标积）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			焦点弦两端点纵坐标之积为负定值 $-p^2$．
			\[
				y_1y_2 = -p^2.
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（切线垂直）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			过 $A,B$ 两点的切线互相垂直，即斜率积为 $-1$．
			\[
				k_A k_B = -1,
			\]
			其中 $k_A,k_B$ 分别为 $A,B$ 处切线斜率．
	\end{description}

	\medskip

	\textbf{\textcolor{CStatement}{参数限制（标准形式）：}}
	结论仅适用于抛物线 $y^2=2px\ (p>0)$．若抛物线为 $y^2=-2px$ 或 $x^2=2py$，结论形式会发生改变，不可直接套用．
\end{statementbox}
