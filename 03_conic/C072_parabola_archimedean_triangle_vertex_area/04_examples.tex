\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知抛物线 $x^2=4y$（$p=2$）上两点 $A(2,1)$, $B(4,4)$，求阿基米德三角形 $\triangle PAB$ 的顶点 $P$ 坐标和三角形面积。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求 $P$ 坐标）:}] 由 $P$ 坐标公式 $P\!\left(\dfrac{x_1+x_2}{2},\,\dfrac{x_1x_2}{2p}\right)$，代入 $x_1=2,x_2=4,p=2$ 得
			\[
				P\!\left(\frac{2+4}{2},\,\frac{2\times 4}{2\times 2}\right) = (3,2)
			\]
			\par\textbf{理由：}直接套用结论（1）。
		\item[\textbf{第二步（求三角形面积）:}] 利用面积公式 $S_{\triangle PAB}=\dfrac{|x_1-x_2|^3}{8p}$，代入得
			\[
				S = \frac{|2-4|^3}{8\times 2} = \frac{8}{16} = \frac{1}{2}
			\]
			\par\textbf{理由：}直接套用结论（2）。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$P(3,2)$，$S_{\triangle PAB}=\dfrac12$。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知抛物线 $x^2=2y$（$p=1$）的某阿基米德三角形面积为 $1$，求两个切点的横坐标之差的绝对值 $|x_1-x_2|$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（套用面积公式）:}] 面积公式 $S=\dfrac{|x_1-x_2|^3}{8p}$，此处 $p=1,S=1$，代入得
			\[
				1 = \frac{|x_1-x_2|^3}{8}
			\]
			\par\textbf{理由：}已知条件中的面积对应结论（2）。
		\item[\textbf{第二步（解绝对值方程）:}] 两边乘以 $8$ 得 $|x_1-x_2|^3 = 8$，开立方（实数立方唯一）得 $|x_1-x_2| = 2$。
			\par\textbf{理由：}正实数立方根唯一。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$|x_1-x_2| = 2$。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知抛物线 $x^2=4y$（$p=2$）外一点 $Q(4,1)$，过 $Q$ 引抛物线的两条切线，切点为 $A,B$，求 $\triangle QAB$ 的面积。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（判断顶点条件）:}] 由切线定义，$Q$ 即为两条切线的交点 $P$，所以 $Q$ 是阿基米德三角形 $\triangle PAB$ 的顶点 $P$。设 $Q(x_0,y_0)=(4,1)$。
			\par\textbf{理由：}阿基米德三角形定义。
		\item[\textbf{第二步（坐标公式逆用）:}] 由 $P$ 的坐标公式得
			\[
				\frac{x_1+x_2}{2}=4,
				\qquad
				\frac{x_1x_2}{2p}=1
			\]
			代入 $p=2$ 得 $x_1+x_2=8$，$x_1x_2 = 4$。
			\par\textbf{理由：}结论（1）的逆用。
		\item[\textbf{第三步（计算差值面积）:}]
			\[
				|x_1-x_2| = \sqrt{(x_1+x_2)^2-4x_1x_2} = \sqrt{8^2-4\times4} = \sqrt{64-16}= \sqrt{48}=4\sqrt{3}
			\]
			代入面积公式 $S_{\triangle PAB}=\dfrac{|x_1-x_2|^3}{8p}$ 得
			\[
				S = \frac{(4\sqrt{3})^3}{8\times 2} = \frac{64\times 3\sqrt{3}}{16} = 12\sqrt{3}
			\]
			\par\textbf{理由：}结论（2）求面积。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$\triangle QAB$ 的面积为 $12\sqrt{3}$。
\end{examplebox}
