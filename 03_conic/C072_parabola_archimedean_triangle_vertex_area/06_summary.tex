\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	阿基米德三角形的顶点坐标 $P$ 和面积 $S$ 均可由两切点的横坐标 $(x_1,x_2)$ 直接表达，不必求出具体切点位置。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（标准抛物线形式）:} 抛物线必须为 $x^2=2py$ 的标准形式，$p>0$。
		\item \textbf{条件 2（切点条件）:} $A,B$ 是抛物线上两个不同的点，且各自切线交于 $P$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（记错系数）:} 面积公式的分母是 $8p$ 而非 $8p^2$ 或 $4p$，纵坐标分母是 $2p$。
		\item \textbf{检查项（适用前提）:} 确认抛物线开口方向与对称轴，公式仅适用于 $x^2=2py$。
	\end{itemize}
\end{summarybox}
