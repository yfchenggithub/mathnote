\begin{statementbox}
\textbf{P03 圆上一点到圆的距离极值结论}

设圆 $O$ 的半径为 $R$，
点 $P$ 在圆上，
即 $OP=R$。

则点 $P$ 到圆上各点距离的：

\begin{itemize}
  \item 最小值为 $0$；
  \item 最大值为 $2R$。
\end{itemize}

最小距离在 $P$ 点本身取得，
最大距离在圆上与 $P$ 直径相对的点处取得。
\end{statementbox}
