\begin{trapbox}
\begin{itemize}
    \item \textbf{斜率不存在讨论}: 当切线为 $x = \pm a$ 时, 焦点到切线距离分别为 $a-c$ 与 $a+c$, 乘积为 $a^2-c^2=b^2$, 结论依然成立.
    \item \textbf{距离方向}: 距离始终为正值. 计算中若涉及坐标, 务必保留绝对值符号.
    \item \textbf{符号规范}: 确保所有英文半角符号的使用. 严禁在非数学环境直接书写 "$b^2$" 或 "$d_1$", 必须包裹在数学符号内.
\end{itemize}
\end{trapbox}