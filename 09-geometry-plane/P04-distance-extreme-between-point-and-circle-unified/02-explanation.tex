\begin{explanationbox}
无论点 $P$ 在哪里，
距离问题都可以统一转化为
$\triangle OPA$ 中的三角形不等式。

区别只在于：
\begin{itemize}
  \item $d<R$（圆内）；
  \item $d=R$（圆上）；
  \item $d>R$（圆外）。
\end{itemize}

模型不变，位置决定结果。
\end{explanationbox}
