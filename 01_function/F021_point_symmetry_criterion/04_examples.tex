\begin{examplebox}

	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)$ 的定义域为 $\mathbb{R}$，且对任意 $x\in\mathbb{R}$ 满足 $f(2+x)+f(2-x)=6$。试判断 $f(x)$ 的图象是否为中心对称图形，若是，求对称中心。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别特征结构）：}] 已知等式符合 $f(a+x)+f(a-x)=2b$ 的形式，其中 $a=2$，$2b=6$ 即 $b=3$。
			\par\textbf{理由：}
			比较已知等式与定理形式。
		\item[\textbf{第二步（验证定义域对称）：}] 定义域 $\mathbb{R}$ 关于 $x=2$ 对称（对任意 $x$，$2\pm x$ 均为实数）。
			\par\textbf{理由：}
			这是定义域对称的条件。
		\item[\textbf{第三步（应用结论）：}] 由充要条件，$f(x)$ 的图象关于点 $(2,3)$ 中心对称。
			\par\textbf{理由：}
			满足定理条件，直接得出结论。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{对称中心为 $(2,3)$。}

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)$ 定义域为 $\mathbb{R}$，且满足 $f(1+x)+f(1-x)=4$，当 $x>1$ 时 $f(x)=\log_2 x$，求 $f(-1)$ 的值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（确定对称中心）：}] 由条件知 $a=1$，$2b=4$ 即 $b=2$，故对称中心为 $(1,2)$。
			\par\textbf{理由：}
			与定理对照。
		\item[\textbf{第二步（建立对称关系）：}] 取 $x=2$（此时 $1+x=3>1$，$1-x=-1$），则 $f(3)+f(-1)=4$。
			\par\textbf{理由：}
			对称点函数值之和等于 $2b$。
		\item[\textbf{第三步（代入已知计算）：}] 因为 $f(3)=\log_2 3$，所以 $f(-1)=4-\log_2 3$。
			\par\textbf{理由：}
			解方程得结果。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f(-1)=4-\log_2 3$。}

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)$ 定义域为 $\mathbb{R}$，且满足 $f(2+x)+f(2-x)=4$，且 $f(x)$ 在 $[2,+\infty)$ 上单调递增。判断 $f(x)$ 在 $(-\infty,2]$ 上的单调性，并证明。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（对称关系转化）：}] 由定理知 $f(x)$ 关于点 $(2,2)$ 中心对称，即对任意 $x$ 有 $f(2+x)+f(2-x)=4$，从而 $f(x)=4-f(4-x)$。
			\par\textbf{理由：}
			对称性公式变形。
		\item[\textbf{第二步（任取两点比较）：}] 任取 $x_1<x_2\leq2$，则 $4-x_1>4-x_2\geq2$。
			\par\textbf{理由：}
			作差比较定义。
		\item[\textbf{第三步（利用已知单调性）：}] 因为 $f$ 在 $[2,+\infty)$ 上递增，所以 $f(4-x_1)>f(4-x_2)$，从而
			\[
				\begin{aligned}
					f(x_1) &= 4 - f(4-x_1) \\
					&< 4 - f(4-x_2) \\
					&= f(x_2),
			\end{aligned}
			\]
			故 $f(x)$ 在 $(-\infty,2]$ 上单调递增。
			\par\textbf{理由：}
			函数值的符号反转导致单调性一致。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f(x)$ 在 $(-\infty,2]$ 上单调递增（与右侧相同）。}

\end{examplebox}
