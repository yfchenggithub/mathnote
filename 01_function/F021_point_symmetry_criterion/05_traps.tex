\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（定义域忽略）}}] 学生直接套用 $f(a+x)+f(a-x)=2b$ 判断对称性，未检查定义域是否关于 $x=a$ 对称。
		\item[\textbf{\textcolor{CTrap}{正确理解（必须对称）：}}] 使用前必须先验证定义域对称性，否则结论不成立。
		\item[\textbf{\textcolor{CTrap}{错因分析（条件缺失）：}}] 充要条件要求定义域对称，遗漏条件会导致错误判断。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（公式混淆）}}] 将中心对称条件误记为 $f(a+x)=f(a-x)$（轴对称条件）。
		\item[\textbf{\textcolor{CTrap}{正确理解（和式常数）：}}] 中心对称的条件是 $f(a+x)+f(a-x)=2b$，不是相等。
		\item[\textbf{\textcolor{CTrap}{错因分析（对称类型混淆）：}}] 中心对称对应函数值和为常数，轴对称对应函数值相等。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（中心坐标错误）}}] 从 $f(a+x)+f(a-x)=2b$ 误认为对称中心是 $(a,b)$ 但写错符号或忘记除2。
		\item[\textbf{\textcolor{CTrap}{正确理解（直接对应）：}}] 对称中心为 $(a,b)$，其中 $a$ 是常数项，$b$ 等于函数值和的一半。
		\item[\textbf{\textcolor{CTrap}{错因分析（系数误解）：}}] 不明确 $2b$ 中 $b$ 的几何意义，误将 $2b$ 当作纵坐标。
	\end{description}
\end{trapbox}
