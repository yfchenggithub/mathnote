\begin{summarybox}

	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{函数 $f(x)$ 的图象关于点 $(a,b)$ 中心对称 $\iff$ $f(a+x)+f(a-x)=2b$ 对定义域内所有 $x$ 成立。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（定义域对称）：} 定义域关于 $x=a$ 对称，即 $x\in D$ 时 $2a-x\in D$。
		\item \textbf{条件 2（等式普适）：} 对定义域内任意 $x$ 均有 $f(a+x)+f(a-x)=2b$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（符号混淆）：} 注意是 $+$ 号，不是 $-$ 号；右侧是 $2b$，不是 $b$。
		\item \textbf{检查项（定义域验证）：} 使用前务必确认定义域对称，否则结论不适用。
	\end{itemize}
\end{summarybox}
