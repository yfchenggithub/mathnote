\begin{statementbox}

	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（定义域对称）：}}] 函数 $f(x)$ 的定义域关于 $x=a$ 对称，即对任意 $x$，若 $a+x$ 属于定义域，则 $a-x$ 也属于定义域。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（充要等式）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			自变量关于 $a$ 对称的点，函数值之和为常数 $2b$。
			\[
				f(a+x) + f(a-x) = 2b.
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（平移奇函数）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			令 $g(x)=f(a+x)-b$，则 $g(x)$ 为奇函数。
			\[
				g(x) = f(a+x)-b,\quad g(-x) = -g(x).
			\]
	\end{description}

\end{statementbox}
