\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（原始函数）：}}]
			给定函数$y=f(x)$，其图像定义在$x$的允许取值范围内。
		\item[\textbf{\textcolor{CStatement}{条件2（平移方式）：}}]
			将图像沿$y$轴方向平移，平移量为$b$（$b>0$）。向上平移$b$个单位，或向下平移$b$个单位。
	\end{description}

	\medskip
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（上加下减）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			图像向上平移$b$个单位$(b>0)$，解析式变为$y=f(x)+b$；向下平移$b$个单位，解析式变为$y=f(x)-b$。
			\[
				y = f(x) + b,
				\qquad
				y = f(x) - b.
			\]
		\item[\textbf{\textcolor{CStatement}{推广结论（通式表达）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			沿$y$轴平移$t$个单位（$t>0$向上，$t<0$向下），新函数解析式为$y=f(x)+t$。
			\[
				y = f(x) + t,\quad t\in\mathbb{R}.
			\]
	\end{description}

\end{statementbox}
