\begin{trapbox}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（方向混淆）}}]
			将“上加下减”用于$x$轴平移。例如，函数$y=\sin x$向左平移1个单位，误写成$y=\sin x+1$。
		\item[\textbf{\textcolor{CTrap}{正确理解（区分坐标轴）：}}]
			$x$轴平移应遵循“左加右减”，向左平移1个单位应为$y=\sin(x+1)$。$y$轴平移才是“上加下减”。
		\item[\textbf{\textcolor{CTrap}{错因分析（口诀混用）：}}]
			未区分平移对象：$y$轴平移直接作用在函数值上，$x$轴平移作用在自变量上。虽口诀类似但位置不同。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（正负误读）}}]
			题目中说“向下平移$-2$个单位”，学生直接套用$y=f(x)-b$，代入$b=-2$得$y=f(x)+2$，得到向上平移的效果，导致错误。
		\item[\textbf{\textcolor{CTrap}{正确理解（规范表述）：}}]
			通常平移量用正数表示，若出现负数应理解为逆向平移。向下平移$-2$个单位实为向上平移2个单位，应写为$y=f(x)+2$。
		\item[\textbf{\textcolor{CTrap}{错因分析（符号混淆）：}}]
			混淆了平移方向与平移量的符号。标准方法是：若平移量为$b>0$，上加下减；若$b<0$，按反向处理。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（定义域误判）}}]
			平移后求函数性质时，错误地认为定义域也发生了平移。例如$y=\sqrt{x}$向上平移1个单位，误认为新函数为$y=\sqrt{x+1}+1$。
		\item[\textbf{\textcolor{CTrap}{正确理解（定义域不变）：}}]
			沿$y$轴平移只改变函数值，不改变自变量$x$的取值范围。$y=\sqrt{x}+1$的定义域仍是$x\ge0$。
		\item[\textbf{\textcolor{CTrap}{错因分析（惯性思维）：}}]
			将$y$轴平移与$x$轴平移混淆，以为平移必然改变$x$的条件。实际上$y$轴平移不影响$x$满足的条件。
	\end{description}

\end{trapbox}
