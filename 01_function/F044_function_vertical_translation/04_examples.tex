\begin{examplebox}
	\textbf{\textcolor{CExample}{例1（基础）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知函数$y=2x+1$，将其图像向上平移3个单位，求平移后的函数解析式。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（明确平移量）：}]
			由题意，向上平移$b=3$个单位。
			\par\textbf{理由：} 题目直接给出平移方向和距离。
		\item[\textbf{第二步（套用规则）：}]
			向上平移后解析式为$y=f(x)+3$。
			\par\textbf{理由：} 根据“上加下减”，向上平移后函数值加$b$。
		\item[\textbf{第三步（代入函数）：}]
			代入$f(x)=2x+1$，得$y=2x+1+3=2x+4$。
			\par\textbf{理由：} 进行代数加法。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	平移后的解析式为\textbf{$y=2x+4$}。

	\medskip
	\textbf{\textcolor{CExample}{例2（稍变形）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知函数$y=x^2$的图像经过平移后得到$y=x^2-2$，请说明平移的方向和距离。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（对比解析式）：}]
			原函数$f(x)=x^2$，新函数$g(x)=x^2-2$。
			\par\textbf{理由：} 通过对比发现差别为减2。
		\item[\textbf{第二步（识别平移量）：}]
			$g(x)=f(x)-2$，由“上加下减”可知，向下平移2个单位。
			\par\textbf{理由：} 减号对应向下平移，2为平移量。
		\item[\textbf{第三步（写出结论）：}]
			将原图像向下平移2个单位即可得到新图像。
			\par\textbf{理由：} 推理完毕，直接作答。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	平移方向为向下，平移距离为\textbf{$2$}个单位。

	\medskip
	\textbf{\textcolor{CExample}{例3（综合应用）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知函数$y=-x^2$，将其先向上平移1个单位，再向下平移3个单位，求最终函数解析式，并指出图像的顶点坐标变化。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（第一次平移）：}]
			向上平移1个单位：$y_1 = -x^2 + 1$。
			\par\textbf{理由：} 向上平移加1。
		\item[\textbf{第二步（第二次平移）：}]
			将$y_1$再向下平移3个单位：$y_2 = y_1 - 3 = -x^2 + 1 - 3 = -x^2 - 2$。
			\par\textbf{理由：} 向下平移减3。
		\item[\textbf{第三步（顶点坐标）：}]
			原函数顶点为$(0,0)$，第一次平移后顶点为$(0,1)$，第二次后顶点为$(0,-2)$。
			\par\textbf{理由：} 纵坐标随平移增减，横坐标始终为0。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	最终解析式为\textbf{$y = -x^2 - 2$}，顶点从\textbf{$(0,0)$}变为\textbf{$(0,-2)$}，即向下平移了2个单位。
\end{examplebox}
