\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	\textbf{函数图像沿$y$轴平移时，解析式遵循“上加下减”：向上平移$b$个单位得$y=f(x)+b$，向下平移$b$个单位得$y=f(x)-b$。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（函数确定）：} 已知函数$y=f(x)$且其图像存在可平移范围（即定义域无特殊限制）。
		\item \textbf{条件2（平移量正数）：} 平移量$b>0$，方向明确为向上或向下。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（区分坐标轴）：} 与$x$轴平移的“左加右减”区分，两者加减作用的对象不同。
		\item \textbf{检查项（平移量符号）：} 若遇到“平移$-b$个单位”等表述，应转化为反向平移标准处理。
	\end{itemize}

\end{summarybox}
