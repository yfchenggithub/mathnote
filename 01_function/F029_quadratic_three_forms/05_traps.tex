\begin{trapbox}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（忽略二次项）}}]
			易错将 $a=0$ 的情况也当作二次函数处理，导致错误。
		\item[\textbf{\textcolor{CTrap}{正确理解（定义前提）：}}]
			二次函数必须 $a\neq0$，否则退化为一次函数或常数。
		\item[\textbf{\textcolor{CTrap}{错因分析（惯性思维）：}}]
			看到 $x^2$ 项就默认二次，未检查系数是否为零。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（判别式条件）}}]
			在 $\Delta<0$ 时仍然尝试将二次函数写成两根式。
		\item[\textbf{\textcolor{CTrap}{正确理解（判别式非负）：}}]
			两根式只在 $\Delta\ge0$ 时有实数意义。
		\item[\textbf{\textcolor{CTrap}{错因分析（机械记忆）：}}]
			只记形式，忽略成立条件。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（公式记错）}}]
			记错顶点纵坐标 $k=\frac{b^2-4ac}{4a}$ 或 $k=-\frac{b^2-4ac}{4a}$。
		\item[\textbf{\textcolor{CTrap}{正确理解（正确公式）：}}]
			$k=\frac{4ac-b^2}{4a}$，可通过配方验证。
		\item[\textbf{\textcolor{CTrap}{错因分析（死记硬背）：}}]
			缺乏推导理解，符号混淆。
	\end{description}

\end{trapbox}
