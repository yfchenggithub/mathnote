\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}
	二次函数的三种形式就像是同一个人的身份证、全身照和指纹，从不同侧面刻画自身，方便不同场景下的使用。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（配方法）：}]
			通过配方将一般式转为顶点式，关键操作是 $ax^2+bx = a(x+\frac{b}{2a})^2 - \frac{b^2}{4a}$。
		\item[\textbf{要点二（因式分解）：}]
			利用求根公式 $x_{1,2}=\frac{-b\pm\sqrt{\Delta}}{2a}$ 分解为两根式，前提是 $\Delta\ge0$。
		\item[\textbf{要点三（参数映射）：}]
			一般式系数 $a,b,c$ 与顶点坐标 $(h,k)$、两根和积 $x_1+x_2, x_1x_2$ 之间存在对应关系，可以互相推导。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（图形信息）}}
	顶点式直接给出抛物线顶点位置和开口方向，两根式直接显示与 $x$ 轴的交点，体现图象的几何特征。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（形式转换）}}
	三种形式本质上是同一个二次多项式的不同代数表示，通过代数变换（配方、因式分解）相互转化，体现了代数等价的灵活性。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（解题选择）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（设顶点式）：} 已知顶点或对称轴时设顶点式 $f(x)=a(x-h)^2+k$。
		\item \textbf{考法二（设两根式）：} 已知与 $x$ 轴交点时设两根式 $f(x)=a(x-x_1)(x-x_2)$。
		\item \textbf{考法三（设一般式）：} 已知任意三点时设一般式 $f(x)=ax^2+bx+c$ 解方程组。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	配方法与求根公式是转化的核心工具，熟练掌握配方法可快速求得顶点坐标，求根公式则用于分解因式。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（已知顶点）：} 直接设顶点式，待定 $a$ 通过其他条件求出。
		\item \textbf{场景二（已知交点）：} 直接设两根式，待定 $a$ 通过其他条件求出。
	\end{itemize}

\end{explanationbox}
