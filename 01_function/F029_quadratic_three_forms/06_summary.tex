\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	\textbf{二次函数有三种等价形式，顶点式给出顶点坐标 $h=-\frac{b}{2a}, k=\frac{4ac-b^2}{4a}$；两根式给出零点与根和积 $x_1+x_2=-\frac{b}{a}, x_1x_2=\frac{c}{a}$。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（二次项非零）：} $a\neq0$。
		\item \textbf{条件2（判别式非负）：} 两根式需要 $\Delta\ge0$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（公式记忆）：} 顶点坐标分母 $2a$，$k$ 分子勿混淆符号；和积注意负号。
		\item \textbf{检查项（前提检验）：} 使用两根式前检验 $\Delta$ 是否非负。
	\end{itemize}

\end{summarybox}
