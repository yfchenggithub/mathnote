\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（系数非零）：}}]
			二次项系数 $a\neq 0$。
	\end{description}

	\medskip
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（顶点式变换）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			通过配方将一般式化为顶点式，直接得到顶点坐标及对称轴。
			\[
				f(x)=a(x-h)^2+k,
				\qquad
				h=-\frac{b}{2a},\; k=\frac{4ac-b^2}{4a}.
			\]
		\item[\textbf{\textcolor{CStatement}{结论二（两根式分解）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			当判别式非负时，函数可分解为两个一次因式，直接显示零点与根的和积。
			\[
				f(x)=a(x-x_1)(x-x_2),\quad x_1+x_2=-\frac{b}{a},\; x_1x_2=\frac{c}{a}.
			\]
		\item[\textbf{\textcolor{CStatement}{推广结论（判别式与零点）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			判别式 $\Delta=b^2-4ac$ 的符号决定零点个数：$\Delta>0$ 两个不同实零点，$\Delta=0$ 一个重零点，$\Delta<0$ 无实零点。
			\[
				\Delta = b^2-4ac.
			\]
	\end{description}

\end{statementbox}
