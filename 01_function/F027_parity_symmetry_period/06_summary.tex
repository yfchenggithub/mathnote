\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	当函数既是奇函数（或偶函数）又有相应的对称性时，它的一个周期为 $4|a|$。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（奇函数加线对称）：} $f(x)$ 是奇函数，且图像关于 $x=a$ 对称，$a\neq0$。
		\item \textbf{条件 2（偶函数加点对称）：} $f(x)$ 是偶函数，且图像关于 $(a,0)$ 对称，$a\neq0$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（组合固定）：} 不是所有奇偶性与对称性组合都产生周期 $4|a|$，只有两个固定组合成立。
		\item \textbf{检查项（定义域对称）：} 需确保定义域关于原点对称，否则奇偶性失效。
	\end{itemize}
\end{summarybox}
