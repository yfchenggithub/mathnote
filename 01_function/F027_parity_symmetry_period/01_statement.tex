\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（奇函数加线对称）：}}] 函数 $f(x)$ 是奇函数，且图像关于直线 $x=a$ 对称（$a\neq 0$）。
		\item[\textbf{\textcolor{CStatement}{条件 2（偶函数加点对称）：}}] 函数 $f(x)$ 是偶函数，且图像关于点 $(a,0)$ 中心对称（$a\neq 0$）。
	\end{description}
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（奇函数情形）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			奇函数关于直线 $x=a$ 对称时，周期为 $4|a|$。
			\[
				T = 4|a|.
			\]
		\item[\textbf{\textcolor{CStatement}{结论二（偶函数情形）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			偶函数关于点 $(a,0)$ 对称时，周期也为 $4|a|$。
			\[
				T = 4|a|.
			\]
		\item[\textbf{\textcolor{CStatement}{推广结论（周期统一形式）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			两种条件下函数的最小正周期相同，均为 $4|a|$。
			\[
				T_{\min}=4|a|.
			\]
	\end{description}
	\textbf{\textcolor{CStatement}{参数限制：}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item $a\neq 0$，否则对称轴或对称中心与原点重合，无法产生周期。
		\item $f(x)$ 的定义域必须关于原点对称，否则奇偶性无意义。
	\end{itemize}
\end{statementbox}
