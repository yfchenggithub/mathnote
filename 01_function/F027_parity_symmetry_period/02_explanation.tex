\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	函数同时具有两种对称性时，连续进行四次对称变换即可回到自身，从而产生固定周期。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（对称链构成）：}] 每个对称操作（奇函数或偶函数给出一种对称，对称轴或对称中心给出另一种）复合后产生平移效果。
		\item[\textbf{要点二（递推公式建立）：}] 将对称性与奇偶性写成函数方程，代入变量递推得到 $f(x+4a)=f(x)$。
		\item[\textbf{要点三（最小周期判定）：}] 需验证 $4|a|$ 是最小正周期，即不存在更小的正数 $T$ 满足周期定义。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（对称叠加）}}

	一个对称中心和一个对称轴距离 $|a|$，连续两次关于对称中心和对称轴变换的组合相当于平移 $2|a|$，四次后恰好回到原位置，周期为 $4|a|$。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（方程推导）}}

	奇函数满足 $f(-x)=-f(x)$，关于 $x=a$ 对称满足 $f(a+x)=f(a-x)$。联立可得 $f(x+4a)=f(x)$。偶函数类似。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（周期判定）}}

	高考中常用于：（1）已知抽象函数的对称性，快速求周期；（2）结合周期与对称性求函数值；（3）判断函数图像性质。

	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（直接求周期）：}给出奇偶性和对称性，直接套用 $T=4|a|$。
		\item \textbf{考法二（综合求值）：}利用周期将未知自变量转化到已知区间求值。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	构造周期：$f(x+2a)=f(-x)$（奇函数情形）和 $f(x+2a)=-f(-x)$ 等中间关系，连续代入可得 $f(x+4a)=f(x)$。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（抽象函数题）：}题中给出函数奇偶性和对称性，求周期。
		\item \textbf{场景二（图像变换题）：}判断函数是否具有周期性并确定周期。
	\end{itemize}
\end{explanationbox}
