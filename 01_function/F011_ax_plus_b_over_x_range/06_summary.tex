\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	对勾函数 $f(x)=Ax+\frac{B}{x}$（$A,B>0$）的值域为 $(-\infty,-2\sqrt{AB}]\cup[2\sqrt{AB},+\infty)$，在 $x>0$ 取得最小值 $2\sqrt{AB}$，$x<0$ 取得最大值 $-2\sqrt{AB}$，取等条件分别为 $x=\pm\sqrt{\frac{B}{A}}$.

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（参数正性）：} $A>0$, $B>0$.
		\item \textbf{条件2（正负分区）：} 必须按 $x>0$ 和 $x<0$ 分类应用结论.
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（不等号方向）：} $x<0$ 时 $f(x)\le -2\sqrt{AB}$，勿写成 $\ge$.
		\item \textbf{检查项（取等条件）：} 确认 $x=\pm\sqrt{\frac{B}{A}}$ 属于定义域，才能写闭区间.
	\end{itemize}
\end{summarybox}
