\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	对勾函数 $f(x)=Ax+\frac{B}{x}\ (A,B>0)$ 的图像分为两支，形似“对勾”。在正值区间上，函数有最小值；在负值区间上，函数有最大值，整体值域为两段区间。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（结构对称性）：}] 函数由正比例项 $Ax$ 与反比例项 $\frac{B}{x}$ 组成。两项互为“乘积恒定”，导致当 $x>0$ 与 $x<0$ 时，函数单调性与最值相反。
		\item[\textbf{要点二（最值触发条件）：}] 利用基本不等式，当 $Ax=\frac{B}{x}$ 时两项相等，此时取得最值。解出 $x=\pm\sqrt{\frac{B}{A}}$，最值为 $\pm2\sqrt{AB}$.
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（对勾形态）}}

	图像关于原点对称，两支分别位于第一、第三象限。在第一象限，曲线先减后增，最低点对应最小值；在第三象限，曲线先增后减，最高点对应最大值。那个转折点正是最值点。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（均值不等式）}}

	直接运用均值不等式 $Ax+\frac{B}{x}\ge 2\sqrt{AB}$ 时，要求 $Ax$ 与 $\frac{B}{x}$ 均为正数，所以只能用于 $x>0$；对于 $x<0$，先提取负号化为正数再用不等式，得到 $f(x)\le -2\sqrt{AB}$.

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（工具性迁移）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（直接求值域）：} 给定具体 $A,B$，直接套用结论得出函数值域，无需每一步推导。
		\item \textbf{考法二（换元转化）：} 对于形如 $\frac{dx^2+ex+f}{gx+h}$ 的函数，通过代数换元 $t=gx+h$ 可化为 $At+\frac{B}{t}+C$ 形式，从而使用对勾函数结论。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	对勾函数求最值的核心是让两项相等。不论系数如何，最值点始终是 $x=\sqrt{\frac{B}{A}}$（正）或 $x=-\sqrt{\frac{B}{A}}$（负），最值大小仅由 $AB$ 乘积决定。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（基础求值域）：} 直接求 $y=2x+\frac{8}{x}$ 的值域。
		\item \textbf{场景二（分子二次分式）：} 求 $y=\frac{x^2+3x+5}{x+1}$ 的值域，先通过多项式除法并换元化为对勾函数。
	\end{itemize}
\end{explanationbox}
