\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{识别根式类型，选择对应换元，消去根号后转化为常规函数求值域。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（根式结构清晰）：} 函数表达式含有 $\sqrt{ax+b}$、$\sqrt{a^2-x^2}$、$\sqrt{x^2+a^2}$ 或 $\sqrt{x^2-a^2}$ 标准形式。
		\item \textbf{条件 2（换元后处理可行）：} 换元后所得新的代数函数或三角函数能通过常规方法（配方法、单调性、有界性）求值域。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（忽略新变量范围）：} 换元后必须同步确定新变量范围，否则值域结果无效。
		\item \textbf{检查项（三角恒等式符号）：} 三角换元后开方时注意符号，保证根式结果与原式一致。
	\end{itemize}
\end{summarybox}
