\begin{statementbox}

	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（含根式求值域）：}}] 函数表达式含有根式，且直接求解值域困难。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（换元化整式）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			通过变量替换，将根号下整体设为新变量，消去根号。
			\begin{enumerate}
			\item 若含 $\sqrt{ax+b}$（$a\neq 0$），令 $t=\sqrt{ax+b}\ (t\ge 0)$，则 $x=\frac{t^2-b}{a}$。
		\end{enumerate}

		\item[\textbf{\textcolor{CStatement}{结论二（三角换元）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			利用三角恒等式设角，将根式转化为三角函数形式。
			\begin{enumerate}
			\item 若含 $\sqrt{a^2-x^2}$，令 $x=a\sin\theta$，$\theta\in\bigl[-\frac{\pi}{2},\frac{\pi}{2}\bigr]$，则 $\sqrt{a^2-x^2}=a\cos\theta$。
			\item 若含 $\sqrt{x^2+a^2}$，令 $x=a\tan\theta$。
			\item 若含 $\sqrt{x^2-a^2}$，令 $x=a\sec\theta$。
		\end{enumerate}

		\item[\textbf{\textcolor{CStatement}{常见变形（换元后求值域）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			换元后函数转化为整式或三角函数，使用常规方法求值域。
	\end{description}

	\textbf{\textcolor{CStatement}{适用范围：}}
	适用于形如 $\sqrt{ax+b}$、$\sqrt{a^2-x^2}$、$\sqrt{x^2+a^2}$、$\sqrt{x^2-a^2}$ 的根式函数求值域问题。

\end{statementbox}
