\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件一（可导及弧度）：}}] 每个函数在其定义域内可导；自变量$x$以弧度为单位.
		\item[\textbf{\textcolor{CStatement}{条件二（底数范围）：}}] 指数函数底数$a>0$且$a\neq 1$；对数函数底数$a>0$且$a\neq 1$.
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（基本导数公式）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			八个求导公式是计算导数的基础.
			\[
				\begin{aligned}
					(x^\alpha)' &= \alpha x^{\alpha-1},\\[2pt]
					(a^x)' &= a^x\ln a,\quad (e^x)' = e^x,\\[2pt]
					(\log_a x)' &= \frac{1}{x\ln a},\quad (\ln x)' = \frac{1}{x},\\[2pt]
					(\sin x)' &= \cos x,\\[2pt]
					(\cos x)' &= -\sin x.
			\end{aligned}
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论(线性组合求导):}}]
			\textbf{\textcolor{CStatement}{直观描述:}}
			对基本初等函数的线性组合求导时,可逐项求导再组合.
			\[
				(c_1 f_1 + c_2 f_2)' = c_1 f_1' + c_2 f_2'.
			\]
			例如,$f(x)=x^3+2e^x-3\ln x+\sin x$ 的导数为 $f'(x)=3x^2+2e^x-\frac{3}{x}+\cos x$.
	\end{description}
\end{statementbox}
