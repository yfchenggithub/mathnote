\begin{proofbox}
	\textbf{\textcolor{CProof}{思路提示}}

	函数图像对称变换的证明基于点的对称关系。设原曲线上一点 $(x,y)$，根据对称类型求对称点坐标，代入原方程即得新方程。

	\medskip
	\textbf{\textcolor{CProof}{正式推导}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.55em]
		\item[\textbf{步骤一（横轴对称）：}]
			设原曲线上一点 $(x, y)$ 满足 $y=f(x)$。其关于 $x$ 轴的对称点为 $(x, -y)$。该点在新曲线上，因此满足：
			\[
				-y = f(x).
			\]
			整理得 $y = -f(x)$，即新函数解析式。
			\par\textbf{理由：} 对称点横坐标不变，纵坐标取相反数，代入原函数即得。

		\item[\textbf{步骤二（纵轴对称）：}]
			设原曲线上一点 $(x, y)$ 满足 $y=f(x)$。其关于 $y$ 轴的对称点为 $(-x, y)$。该点在新曲线上，因此满足：
			\[
				y = f(-x).
			\]
			即新函数解析式为 $y = f(-x)$。
			\par\textbf{理由：} 对称点纵坐标不变，横坐标取相反数，代入原函数。

		\item[\textbf{步骤三（中心对称）：}]
			设原曲线上一点 $(x, y)$ 满足 $y=f(x)$。其关于原点的对称点为 $(-x, -y)$。该点在新曲线上，因此满足：
			\[
				-y = f(-x).
			\]
			整理得 $y = -f(-x)$，即新函数解析式。
			\par\textbf{理由：} 对称点横、纵坐标均取相反数，代入原函数。

		\item[\textbf{步骤四（反函数对称）：}]
			设原曲线上一点 $(x, y)$ 满足 $y=f(x)$。其关于直线 $y=x$ 的对称点为 $(y, x)$。该点在新曲线上，因此满足：
			\[
				x = f(y).
			\]
			若 $f$ 存在反函数 $f^{-1}$，则可由 $x = f(y)$ 解出 $y = f^{-1}(x)$，即新函数解析式。
			\par\textbf{理由：} 对称点横纵坐标互换，代入原函数得到关于 $x,y$ 的关系，再借助反函数求解。
	\end{description}

	\medskip
	\textbf{\textcolor{CProof}{结论回扣}}

	以上四种对称变换分别得到对应的函数解析式：关于 $x$ 轴对称得 $y=-f(x)$；关于 $y$ 轴对称得 $y=f(-x)$；关于原点对称得 $y=-f(-x)$；关于直线 $y=x$ 对称（若 $f$ 可逆）得 $y=f^{-1}(x)$。
\end{proofbox}
