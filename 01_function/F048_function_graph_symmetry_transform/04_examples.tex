\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知 $f(x)=3x+2$，求函数图像关于 $x$ 轴对称后对应的函数 $g(x)$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（原函数确认）：}]
			\[
				f(x)=3x+2.
			\]
			\par\textbf{理由：} 题目已知。

		\item[\textbf{第二步（对称公式）：}]
			\[
				\begin{aligned}
					g(x) &= -f(x) \\
					&= -(3x+2) \\
					&= -3x-2.
			\end{aligned}
			\]
			\par\textbf{理由：} 应用结论 $y=-f(x)$。

		\item[\textbf{第三步（结论表述）：}]
			\[
				g(x)=-3x-2.
			\]
			\par\textbf{理由：} 由第二步计算所得。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$g(x)=-3x-2$}

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知 $f(x)=x^2-4x+5$，求关于 $y$ 轴对称的函数 $g(x)$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（原函数确认）：}]
			\[
				f(x)=x^2-4x+5.
			\]
			\par\textbf{理由：} 题目已知。

		\item[\textbf{第二步（对称公式）：}]
			\[
				\begin{aligned}
					g(x) &= f(-x) \\
					&= (-x)^2 -4(-x) +5 \\
					&= x^2 +4x +5.
			\end{aligned}
			\]
			\par\textbf{理由：} 应用结论 $y=f(-x)$。

		\item[\textbf{第三步（结论表述）：}]
			\[
				g(x)=x^2+4x+5.
			\]
			\par\textbf{理由：} 由第二步计算所得。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$g(x)=x^2+4x+5$}

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知 $f(x)=2x-1$，求关于原点对称的函数 $g(x)$ 以及关于直线 $y=x$ 对称的函数 $h(x)$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（原点对称）：}]
			\[
				\begin{aligned}
					g(x) &= -f(-x) \\
					&= -\bigl(2(-x)-1\bigr) \\
					&= -(-2x-1) \\
					&= 2x+1.
			\end{aligned}
			\]
			\par\textbf{理由：} 应用结论 $y=-f(-x)$。

		\item[\textbf{第二步（对称方程）：}]
			由对称关系得：
			\[
				x = 2y-1.
			\]
			\par\textbf{理由：} 点 $(x,y)$ 关于 $y=x$ 对称得到 $(y,x)$，代入 $y=2x-1$ 即得。

		\item[\textbf{第三步（解出函数）：}]
			解方程 $x = 2y-1$ 得：
			\[
				y = \frac{x+1}{2}.
			\]
			即 $h(x)=\dfrac{x+1}{2}$。
			\par\textbf{理由：} 将隐函数显化，得到反函数。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$g(x)=2x+1$，$h(x)=\dfrac{x+1}{2}$}
\end{examplebox}
