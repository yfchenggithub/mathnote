\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（定义域非零）：}}] 函数$f(x)$在定义域内满足$f(x)\neq 0$，且存在常数$a\neq 0$，使得对任意$x$，有$f(x+a)=\frac{1}{f(x)}$ \textbf{或} $f(x+a)=-\frac{1}{f(x)}$成立。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（倒数型周期）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			$f(x)$以$2a$为周期，图像每隔$2a$重复。
			\[
				f(x+2a) = f(x)
			\]
			对任意$x$成立。

		\item[\textbf{\textcolor{CStatement}{推广结论（周期链推广）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			所有$2a$的整数倍都是其周期，周期不唯一。
			\[
				f(x+2ka) = f(x),\quad k\in\mathbb{Z},\ k\neq 0.
			\]
	\end{description}

\end{statementbox}
