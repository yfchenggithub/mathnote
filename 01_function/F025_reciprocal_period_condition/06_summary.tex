\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{倒数型周期：$f(x+a)=\pm\frac{1}{f(x)}$（$f(x)\neq0$）$\Rightarrow$ 周期$T=2a$。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（函数非零）：} 对定义域内所有$x$，$f(x)\neq 0$。
		\item \textbf{条件2（平移常数）：} $a\neq 0$。
		\item \textbf{条件3（倒数关系）：} 对任意$x$，$f(x+a)=\frac{1}{f(x)}$ 或 $f(x+a)=-\frac{1}{f(x)}$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（非零与符号）：} 必须确认$f(x)\neq0$，处理负倒数时注意符号抵消。
		\item \textbf{检查项（迭代两次）：} 使用前自己迭代$f(x+2a)$验证，确保无误。
	\end{itemize}
\end{summarybox}
