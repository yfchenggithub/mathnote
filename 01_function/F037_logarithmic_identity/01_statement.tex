\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（底数要求）：}}]
			$a>0$，$a\neq 1$。
		\item[\textbf{\textcolor{CStatement}{条件2（真数要求）：}}]
			$N>0$。
	\end{description}
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（指对恒等）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			指数运算与对数运算相互抵消，结果等于真数。
			\[
				a^{\log_a N}=N
			\]
		\item[\textbf{\textcolor{CStatement}{推广结论（指对互化）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			根据恒等式可得对数与指数之间的互化形式。
			\[
				\log_a (a^x)=x
			\]
	\end{description}

\end{statementbox}
