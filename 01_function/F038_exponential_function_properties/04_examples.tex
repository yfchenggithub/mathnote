\begin{examplebox}

	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	比较 $1.7^{0.3}$ 与 $1.7^{0.5}$ 的大小。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别底数）：}]
			\[
				a=1.7>1.
			\]
			\par\textbf{理由：} 由指数函数性质，当 $a>1$ 时 $y=a^x$ 在 $\mathbb{R}$ 上单调递增。

		\item[\textbf{第二步（比较自变量）：}]
			\[
				0.3<0.5.
			\]
			\par\textbf{理由：} 直接比较小数大小。

		\item[\textbf{第三步（应用单调性）：}]
			\[
				1.7^{0.3} < 1.7^{0.5}.
			\]
			\par\textbf{理由：} 单调递增时，自变量大则函数值大。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$1.7^{0.3} < 1.7^{0.5}$}。
	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知 $a\in(0,1)$，比较 $a^{0.8}$ 与 $a^{1.2}$ 的大小。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（判断底数）：}]
			\[
				a\in(0,1).
			\]
			\par\textbf{理由：} 底数在0到1之间，指数函数 $y=a^x$ 在 $\mathbb{R}$ 上单调递减。

		\item[\textbf{第二步（比较自变量）：}]
			\[
				0.8 < 1.2.
			\]
			\par\textbf{理由：} 直接比较。

		\item[\textbf{第三步（应用单调性）：}]
			\[
				a^{0.8} > a^{1.2}.
			\]
			\par\textbf{理由：} 单调递减时，自变量大的函数值反而小。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$a^{0.8} > a^{1.2}$}。
	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	解不等式 $2^{x} > 4^{x-1}$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（化为同底）：}]
			\[
				4^{x-1} = (2^2)^{x-1} = 2^{2(x-1)} = 2^{2x-2}.
			\]
			\par\textbf{理由：} 幂的乘方运算法则。

		\item[\textbf{第二步（利用单调性）：}]
			\[
				\begin{aligned}
					2^x &> 2^{2x-2}\\    &\Rightarrow x > 2x-2.    \end{aligned}
				\]
				\par\textbf{理由：} 底数 $2>1$，指数函数单调递增，故原不等式等价于 $x > 2x-2$。

			\item[\textbf{第三步（解代数不等式）：}]
				\[
					\begin{aligned}
						x &> 2x-2\\    &\Rightarrow -x > -2\\    &\Rightarrow x < 2.    \end{aligned}
					\]
					\par\textbf{理由：} 移项，两边除以 $-1$ 注意不等号方向反转。
			\end{description}

			\textbf{\textcolor{CExample}{关键结论：}}
			\textbf{不等式的解集为 $(-\infty, 2)$}。

		\end{examplebox}
