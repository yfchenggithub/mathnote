\begin{summarybox}

	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{指数函数 $y=a^x$ 的性质完全由底数 $a$ 与1的大小关系决定：$a>1$ 时递增，$a\in(0,1)$ 时递减；图像恒过 $(0,1)$，值域为 $(0,+\infty)$，以 $y=0$ 为渐近线。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（底数范围）：} $a>0$ 且 $a\neq 1$，否则失去指数函数性质。
		\item \textbf{条件 2（定义域）：} $x\in\mathbb{R}$，函数在全体实数上有定义。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（底数与单调性）：} 使用单调性时务必先判断底数 $a>1$ 还是 $a\in(0,1)$，这决定不等号方向是否反转。
		\item \textbf{检查项（定点与渐近线）：} 解题时可利用 $x=0$ 时 $y=1$ 检验函数，并注意 $y>0$ 恒成立。
	\end{itemize}

\end{summarybox}
