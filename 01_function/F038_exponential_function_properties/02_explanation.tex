\begin{explanationbox}

	\textbf{\textcolor{CExplanation}{一句话直觉}}

	指数函数的值永远为正，且底数大于1时随$x$增大而快速增大；底数在0到1之间时随$x$增大而快速趋近于0，但始终不取负值。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（增减性）：}] 当 $a>1$ 时，$a^x$ 在 $\mathbb{R}$ 上严格单调递增；当 $a\in(0,1)$ 时，$a^x$ 在 $\mathbb{R}$ 上严格单调递减。增减性完全由底数是否大于1决定。
		\item[\textbf{要点二（分界点值）：}] $x=0$ 时 $a^0=1$，因此图像恒过 $(0,1)$；$x>0$ 时 $y$ 与1的大小关系取决于底数：$a>1$ 时 $y>1$，$a\in(0,1)$ 时 $y<1$；$x<0$ 时结论相反。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（图像特性）}}

	指数函数的图像是一条经过点 $(0,1)$、以 $x$ 轴为水平渐近线的平滑曲线。$a>1$ 时曲线从左到右上升；$a\in(0,1)$ 时曲线从左到右下降。图像整体在 $x$ 轴上方（$y>0$），永不接触 $x$ 轴。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（单调性与保序性）}}

	指数运算保持单调性：当 $a>1$ 时，$a^x$ 是 $\mathbb{R}$ 上的严格增函数，因此不等号方向不变；当 $a\in(0,1)$ 时是严格减函数，不等号方向逆转。这一性质常用于比较指数式的大小或解指数不等式。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（高考常见题）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（比较大小）：} 利用单调性只需比较指数（底数相同）；若底数不同，常借助中间量（如0、1）或图像法。
		\item \textbf{考法二（解不等式）：} 化为同底后利用单调性去掉底数，注意不等号方向是否反转。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	\textbf{抓住底数 $a$ 与1的关系：$a>1$ 时递增、$a\in(0,1)$ 时递减。任何问题都优先明确底数范围。}

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（识别图像）：} 给定指数函数解析式或图像，判断底数范围、单调性、定点等。
		\item \textbf{场景二（大小比较）：} 比较 $a^m$ 与 $a^n$ 的大小或解 $a^x>b$ 型不等式。
	\end{itemize}

\end{explanationbox}
