\begin{statementbox}

	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件一（底数范围）：}}] 设指数函数 $f(x)=a^x$，其中 $a>0$ 且 $a\neq 1$。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{结论一（值域与定点）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			图像恒过点 $(0,1)$，值域为全体正实数，以 $x$ 轴为渐近线。
			\[
				\begin{aligned}
					&f(x)=a^x\\
					&a^x\in(0,+\infty)\\
					&f(0)=1\\
					&y=0.
			\end{aligned}
			\]

		\item[\textbf{\textcolor{CStatement}{结论二（单调与分界）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			当 $a>1$ 时递增且 $x$ 正负决定 $y$ 与 $1$ 的大小，当 $a\in(0,1)$ 时递减。
			\[
				\begin{aligned}
					&a>1:\\
					&\qquad x_1<x_2\\
					&\qquad \Rightarrow a^{x_1}<a^{x_2}\\
					&\qquad x<0\\
					&\qquad \Rightarrow 0<a^x\\
					&\qquad \Rightarrow a^x<1\\
					&\qquad x>0\\
					&\qquad \Rightarrow a^x>1\\
					&a\in(0,1):\\
					&\qquad x_1<x_2\\
					&\qquad \Rightarrow a^{x_1}>a^{x_2}\\
					&\qquad x<0\\
					&\qquad \Rightarrow a^x>1\\
					&\qquad x>0\\
					&\qquad \Rightarrow 0<a^x\\
					&\qquad \Rightarrow a^x<1.
			\end{aligned}
			\]
	\end{description}

	\textbf{\textcolor{CStatement}{推广结论（图像对称性）}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{推广结论（图像对称性）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			$y=a^x$ 与 $y=(\frac1a)^x$ 的图像关于 $y$ 轴对称。
			\[
				\begin{aligned}
					&f(x)=a^x\\
					&g(x)=\left(\frac{1}{a}\right)^x\\
					&g(x)=f(-x).
			\end{aligned}
			\]
	\end{description}

\end{statementbox}
