\begin{trapbox}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（底数与不等号方向）}}] 解形如 $a^x > 1$ 的不等式时，不考虑底数 $a$ 是否大于1，直接得出 $x>0$。例如，对 $(0.5)^x > 1$ 错误地得到 $x>0$。
		\item[\textbf{\textcolor{CTrap}{正确理解（单调性决定不等号）：}}] 当 $a>1$ 时不等式 $a^x > 1$ 的解为 $x>0$；当 $a\in(0,1)$ 时解为 $x<0$。必须先判断底数与1的关系。
		\item[\textbf{\textcolor{CTrap}{错因分析（未判断底数与1关系）：}}] 错误认为指数函数总是递增，忽略了底数在 $(0,1)$ 时函数递减，导致不等号方向错误。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（底数等于1误用）}}] 将 $1^x$ 视为指数函数，认为它也满足值域 $(0,+\infty)$、单调性等性质。
		\item[\textbf{\textcolor{CTrap}{正确理解（指数函数底数范围）：}}] 指数函数定义要求 $a>0$ 且 $a\neq 1$。$1^x=1$ 是常数函数，不满足单调性，值域为 $\{1\}$，图像不是指数函数。
		\item[\textbf{\textcolor{CTrap}{错因分析（忽略底数≠1）：}}] 未注意底数不能为1，直接把常数函数当作指数函数使用。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（定点记忆错误）}}] 错误认为指数函数恒过点 $(1,1)$ 或其他点，例如代入 $x=1$ 得 $a^1=a$，以为 $(1,a)$ 是定点。
		\item[\textbf{\textcolor{CTrap}{正确理解（恒过点(0,1)）：}}] 由 $a^0=1$ 知，无论底数 $a$ 取何值（$a>0,a\neq 1$），$x=0$ 时 $y=1$，所以图像恒过 $(0,1)$。
		\item[\textbf{\textcolor{CTrap}{错因分析（混淆定点与可变点）：}}] 只看了一个特殊点，但忽略了 $x=1$ 时 $y$ 随 $a$ 变化，不是定点。
	\end{description}

\end{trapbox}
