\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（忽略定义域）}}] 直接套用“内偶则偶”而不检查内函数定义域是否关于原点对称。
		\item[\textbf{\textcolor{CTrap}{正确理解（定义域前提）：}}] 使用口诀前必须先验证$g(x)$的定义域关于原点对称，否则结论不成立。
		\item[\textbf{\textcolor{CTrap}{错因分析（前提缺失）：}}] 奇偶性定义以定义域对称为前提，忽略这个条件会导致错误判断。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（内奇必奇误解）}}] 误认为只要内函数是奇函数，复合函数就是奇函数。
		\item[\textbf{\textcolor{CTrap}{正确理解（外奇则奇）：}}] 当内函数为奇时，复合函数的奇偶性与外函数相同。若外函数是偶函数，复合函数反而为偶。
		\item[\textbf{\textcolor{CTrap}{错因分析（口诀记忆片面）：}}] 只记住“内奇”却忽略了“同外”，未考虑外层的奇偶性。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（充分必要混淆）}}] 认为复合函数为偶函数时，内层必为偶函数。
		\item[\textbf{\textcolor{CTrap}{正确理解（多种可能）：}}] 复合函数为偶函数有两种情况：内层偶（外层任意）；或内层奇且外层偶。故不能反推内层必偶。
		\item[\textbf{\textcolor{CTrap}{错因分析（逻辑颠倒）：}}] 混淆了充分条件与必要条件，默认原命题的逆命题成立。
	\end{description}
\end{trapbox}
