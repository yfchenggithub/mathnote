\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	判断函数 $F(x)=2^{\cos x}$ 的奇偶性。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（分解函数）：}] 记 $g(x)=\cos x$，$f(u)=2^{u}$，则 $F(x)=f(g(x))$。
			\par\textbf{理由：} 复合函数结构。
		\item[\textbf{第二步（判断内偶）：}] 因为 $\cos(-x)=\cos x$，所以 $g(x)$ 为偶函数。
			\par\textbf{理由：} 偶函数定义。
		\item[\textbf{第三步（应用结论）：}] 由“内偶则偶”得，$F(x)$ 为偶函数。验证：$F(-x)=2^{\cos(-x)}$，因为$\cos(-x)=\cos x$，所以$2^{\cos(-x)}=2^{\cos x}$，故$F(-x)=F(x)$。
			\par\textbf{理由：} 复合函数奇偶性口诀。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$F(x)$ 是偶函数。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	判断函数 $F(x)=\sin(x^{3})$ 的奇偶性。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（分解函数）：}] 记 $g(x)=x^{3}$，$f(u)=\sin u$，则 $F(x)=f(g(x))$。
			\par\textbf{理由：} 复合函数结构。
		\item[\textbf{第二步（判断内奇）：}] 因为 $(-x)^{3}=-x^{3}$，所以 $g(x)$ 为奇函数。
			\par\textbf{理由：} 奇函数定义。
		\item[\textbf{第三步（判断外奇）：}] $f(u)=\sin u$ 为奇函数（$\sin(-u)=-\sin u$）。由“内奇同外”得，$F(x)$ 为奇函数。验证：$F(-x)=\sin((-x)^{3})$，因为$(-x)^{3}=-x^{3}$，故$\sin((-x)^{3})=\sin(-x^{3})$；由$f$奇得$\sin(-x^{3})=-\sin(x^{3})$；而$\sin(x^{3})=F(x)$，所以$F(-x)=-F(x)$。
			\par\textbf{理由：} 内奇同外（外奇则奇）。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$F(x)$ 是奇函数。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知 $F(x)=f(x^{2}+1)$ 是偶函数，能否唯一确定 $f(x)$ 的奇偶性？请说明理由。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（分解函数）：}] 内函数 $g(x)=x^{2}+1$，外函数 $f(u)$ 未知，满足 $F(x)=f(g(x))$。
			\par\textbf{理由：} 复合函数关系。
		\item[\textbf{第二步（判断内偶）：}] 计算 $g(-x)=(-x)^{2}+1=x^{2}+1=g(x)$，所以 $g(x)$ 为偶函数。
			\par\textbf{理由：} 偶函数定义。
		\item[\textbf{第三步（分析结论）：}] 由“内偶则偶”知，无论 $f$ 具有何种奇偶性（只要定义域满足），$F$ 均为偶函数。因此 $F$ 为偶函数不能反推出 $f$ 的奇偶性。例如，取 $f(u)=u$（奇函数），此时 $F(x)=x^{2}+1$ 仍是偶函数。所以 $f$ 的奇偶性无法唯一确定。
			\par\textbf{理由：} 内偶则偶是充分条件而非必要条件。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	不能唯一确定；内偶则偶只能保证复合函数为偶，不能逆推外层的奇偶性。
\end{examplebox}
