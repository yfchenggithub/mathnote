\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（定义域对称）：}}] 函数$g(x)$的定义域关于原点对称。
		\item[\textbf{\textcolor{CStatement}{条件2（值域包含）：}}] 外函数$f(u)$的定义域包含内函数$g(x)$的值域。
	\end{description}
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（内偶则偶）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			当内函数$g$为偶函数时，复合函数$F(x)=f(g(x))$必为偶函数。
			\[
				F(-x)=F(x)
			\]
		\item[\textbf{\textcolor{CStatement}{结论二（内奇同外）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			当内函数$g$为奇函数时，复合函数$F$的奇偶性与外函数$f$相同（$f$奇则$F$奇，$f$偶则$F$偶）。
			\[
				\begin{aligned}
					f \text{ 奇 } &\Rightarrow F(-x) = -F(x),\\
					f \text{ 偶 } &\Rightarrow F(-x) = F(x).
			\end{aligned}
			\]
		\item[\textbf{\textcolor{CStatement}{推广结论（外偶总偶）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			若外函数$f$为偶函数，则无论内函数$g$是奇还是偶，复合函数$F$必为偶函数。
			\[
				F(-x)=F(x)
			\]
	\end{description}
\end{statementbox}
