\begin{examplebox}
	\textbf{\textcolor{CExample}{例1（基础）}}
	\textbf{\textcolor{CExample}{题目：}}
	判断函数 $f(x)=|x-2|$ 的图象是否关于直线 $x=2$ 对称？
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（检验定义域）：}]
			定义域 $\mathbb{R}$ 关于 $x=2$ 对称。
			\par\textbf{理由：} 全体实数关于任何点对称。
		\item[\textbf{第二步（计算 $f(2+x)$ 和 $f(2-x)$）：}]
			$f(2+x)=|(2+x)-2|$，而 $|(2+x)-2|=|x|$，所以 $f(2+x)=|x|$；同样，$f(2-x)=|(2-x)-2|$，$|(2-x)-2|=|-x|$，且 $|-x|=|x|$，故 $f(2-x)=|x|$。因此 $f(2+x)=f(2-x)$。
			\par\textbf{理由：} 代入解析式。
		\item[\textbf{第三步（利用结论）：}]
			由充要条件，图象关于 $x=2$ 对称。
			\par\textbf{理由：} 等式恒成立且定义域对称。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f(x)=|x-2|$ 的图象关于 $x=2$ 对称。}

	\medskip
	\textbf{\textcolor{CExample}{例2（稍变形）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)=x^2+mx+1$ 的图象关于直线 $x=1$ 对称，求实数 $m$ 的值。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（写出对称条件）：}]
			由充要条件，对任意 $x$，$f(1+x)=f(1-x)$ 恒成立。
			\par\textbf{理由：} 图象关于 $x=1$ 对称。
		\item[\textbf{第二步（代入并化简）：}]
			由对称条件得
			\[
				(1+x)^2+m(1+x)+1=(1-x)^2+m(1-x)+1
			\]
			展开两边：
			\[
				1+2x+x^2+m+mx+1 = 1-2x+x^2+m-mx+1
			\]
			化简得
			\[
				(4+2m)x = 0
			\]
			\par\textbf{理由：} 多项式展开，合并同类项。
		\item[\textbf{第三步（利用恒等条件）：}]
			由于上式对任意 $x$ 成立，故系数 $4+2m=0$，解得 $m=-2$。
			\par\textbf{理由：} 多项式恒为零则各项系数为零。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$m=-2$。}

	\medskip
	\textbf{\textcolor{CExample}{例3（综合应用）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)$ 在 $[2,+\infty)$ 上的解析式为 $f(x)=2^x$，且 $f(x)$ 的图象关于直线 $x=2$ 对称，求 $f(x)$ 在 $(-\infty,2)$ 上的解析式。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（对称等式）：}]
			由图象关于 $x=2$ 对称，有 $f(2+x)=f(2-x)$ 对任意 $x$ 恒成立。
			\par\textbf{理由：} 对称充要条件。
		\item[\textbf{第二步（换元转化）：}]
			当 $x<2$ 时，令 $t=2-x>0$，则 $x=2-t$，于是 $f(x)=f(2-t)=f(2+t)$。
			\par\textbf{理由：} 利用对称等式将自变量转化为已知区间。
		\item[\textbf{第三步（代入已知解析式）：}]
			因为 $2+t>2$，所以 $f(2+t)=2^{2+t}$，又 $2^{2+t}=2^{4-x}$，因此 $f(x)=2^{4-x}$。
			\par\textbf{理由：} 代入 $x$ 表达式。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f(x)=2^{4-x}\;(x<2)$。}
\end{examplebox}
