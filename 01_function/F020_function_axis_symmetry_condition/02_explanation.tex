\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}
	函数图象沿直线 $x=a$ 折叠后左右重合，等价于对称点函数值相等。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（对称定义转化）：}]
			将几何对称转化为代数等式 $f(a+x)=f(a-x)$，验证等式是否成立即可判断对称性。
		\item[\textbf{要点二（充要双向验证）：}]
			定义域对称是前提，等式成立则图象对称，图象对称则等式必成立。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（轴反射对称）}}
	图象上任意点关于直线 $x=a$ 的对称点仍在图象上。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（平移后偶函数）}}
	令 $g(t)=f(a+t)$，则 $g(-t)=f(a-t)$，条件化为 $g(t)=g(-t)$，即 $g$ 为偶函数。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（应试方向）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（判断对称轴）：} 给出解析式，检验 $f(a+x)=f(a-x)$ 是否恒成立，判断是否关于 $x=a$ 对称。
		\item \textbf{考法二（利用对称求式）：} 已知函数在某区间解析式及对称轴，求对称区间解析式。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	核心是抓住自变量之和为 $2a$ 时函数值相等，即 $f(x)=f(2a-x)$。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（性质判定）：} 判断函数是否具有轴对称性，如二次函数、含绝对值函数。
		\item \textbf{场景二（解析扩充）：} 由对称性将局部解析式延拓到整体。
	\end{itemize}

\end{explanationbox}
