\begin{proofbox}
	\textbf{\textcolor{CProof}{思路提示}}
	利用对称点坐标关系，分别证明充分性和必要性。

	\medskip
	\textbf{\textcolor{CProof}{正式推导}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.55em]
		\item[\textbf{步骤一（对称点坐标）：}]
			设 $P(x,f(x))$ 是图象上任意一点，则它关于直线 $x=a$ 的对称点 $P'$ 的坐标为 $(2a-x,f(x))$。
			\[
				P'(2a-x,f(x))
			\]
			\par\textbf{理由：} 对称点横坐标满足 $(x+x')/2=a$，纵坐标相同。
		\item[\textbf{步骤二（必要性证明）：}]
			若图象关于 $x=a$ 对称，则 $P'$ 在图象上，故 $f(2a-x)=f(x)$。
			\[
				f(2a-x)=f(x)
			\]
			令 $t=x-a$，得 $f(a-t)=f(a+t)$，即
			\[
				f(a+x)=f(a-x)
			\]
			对任意偏移量 $x$ 成立。
			\par\textbf{理由：} 对称定义；变量代换 $x=a+t$。
		\item[\textbf{步骤三（充分性证明）：}]
			若 $f(a+x)=f(a-x)$ 恒成立，则对任意 $x$，
			\[
				f(2a-x) = f(a-(x-a))
			\]
			\[
				f(a-(x-a)) = f(a+(x-a))
			\]
			\[
				f(a+(x-a)) = f(x)
			\]
			因此 $f(2a-x)=f(x)$，点 $(2a-x,f(x))$ 在图象上，即 $P'$ 在图象上。由 $P$ 的任意性知图象关于 $x=a$ 对称。
			\par\textbf{理由：} 条件恒等式代入；对称点定义。
		\item[\textbf{步骤四（结论整合）：}]
			在定义域关于 $x=a$ 对称的前提下，$f(a+x)=f(a-x)$ 恒成立是图象关于 $x=a$ 对称的充要条件。
			\par\textbf{理由：} 必要性、充分性均已证明。
	\end{description}

	\medskip
	\textbf{\textcolor{CProof}{结论回扣}}
	\textbf{函数图象关于直线 $x=a$ 对称 $\Longleftrightarrow$ $f(a+x)=f(a-x)$ 对定义域内任意允许的 $x$ 恒成立。}
\end{proofbox}
