\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{构造异侧点 $A(m,-a)$、$B(n,b)$，将函数化为 $|PA|+|PB|$，由“两点间线段最短”得最小值 $\sqrt{(m-n)^2+(a+b)^2}$，值域为 $[\sqrt{(m-n)^2+(a+b)^2},+\infty)$．}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（根式结构）：} $f(x)=\sqrt{(x-m)^2+a^2}+\sqrt{(x-n)^2+b^2}$，其中 $a,b>0$，$m,n\in\mathbb{R}$．
		\item \textbf{条件 2（几何对应）：} 能通过配方将根号内写成平方和形式，且 $x$ 为自变量．
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（异侧设定）：} 必须设 $A(m,-a)$ 而非 $A(m,a)$，否则 $A,B$ 同侧，最小值求法不同．
		\item \textbf{检查项（共线条件）：} 取最小值时 $P$ 在线段 $AB$ 上，即 $x$ 在 $m,n$ 之间；需验证解出的 $x$ 是否满足区间．
	\end{itemize}
\end{summarybox}
