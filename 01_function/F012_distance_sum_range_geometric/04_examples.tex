\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	求函数 $f(x)=\sqrt{(x-1)^2+4}+\sqrt{(x-3)^2+9}$ 的最小值，并求出此时 $x$ 的值．

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别模型）：}] 识别 $f(x)$ 形如 $\sqrt{(x-m)^2+a^2}+\sqrt{(x-n)^2+b^2}$，其中 $m=1$, $n=3$, $a=2$, $b=3$（因为 $4=2^2$, $9=3^2$）．
			\par\textbf{理由：} 直接观察根号内形式，与标准模型匹配．
		\item[\textbf{第二步（构造点）：}] 令 $A(1,-2)$、$B(3,3)$、$P(x,0)$，则 $f(x)=|PA|+|PB|$．
			\par\textbf{理由：} 距离公式验证：$|PA|=\sqrt{(x-1)^2+2^2}$，$|PB|=\sqrt{(x-3)^2+3^2}$．
		\item[\textbf{第三步（求解计算）：}] 由几何意义，$f(x)\ge|AB|$，计算 $|AB|=\sqrt{(1-3)^2+(2+3)^2}=\sqrt{4+25}=\sqrt{29}$；由 $A,P,B$ 共线得 $\frac{x-1}{2}=\frac{3-x}{3}$，解得 $x=\frac{9}{5}$．故最小值为 $\sqrt{29}$，此时 $x=\frac{9}{5}$．
			\par\textbf{理由：} 两点间线段最短，共线条件解出 $x$．
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	最小值为 $\sqrt{29}$，取得最小值时 $x=\frac{9}{5}$．

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	求函数 $f(x)=\sqrt{x^2-4x+5}+\sqrt{x^2+2x+5}$ 的最小值．

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（配方识别）：}] $x^2-4x+5=(x-2)^2+1^2$，$x^2+2x+5=(x+1)^2+2^2$，故对应 $m=2$, $n=-1$, $a=1$, $b=2$．
			\par\textbf{理由：} 配方得到标准形式，确定参数．
		\item[\textbf{第二步（构造点）：}] 令 $A(2,-1)$、$B(-1,2)$、$P(x,0)$，则 $f(x)=|PA|+|PB|$．
			\par\textbf{理由：} 距离公式等价．
		\item[\textbf{第三步（求解计算）：}] $|AB|=\sqrt{(2-(-1))^2+(1+2)^2}=\sqrt{9+9}=3\sqrt{2}$；由共线得 $\frac{x-2}{1}=\frac{-1-x}{2}$，解得 $x=1$．故最小值为 $3\sqrt{2}$．
			\par\textbf{理由：} 两点间线段最短，共线条件．
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	最小值为 $3\sqrt{2}$，此时 $x=1$．

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知 $k>0$，函数 $f(x)=\sqrt{(x-2)^2+k^2}+\sqrt{(x-5)^2+9}$ 的最小值为 $5$，求 $k$ 的值及此时 $x$ 的值．

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（套用模型）：}] 根据函数形式，对应 $m=2$, $n=5$, $a=k$, $b=3$（因为 $9=3^2$）．
			\par\textbf{理由：} 与标准形式对比得出参数．
		\item[\textbf{第二步（列方程）：}] 由结论最小值为 $\sqrt{(m-n)^2+(a+b)^2}=\sqrt{(2-5)^2+(k+3)^2}=\sqrt{9+(k+3)^2}$．由题意得 $\sqrt{9+(k+3)^2}=5$，两边平方得 $9+(k+3)^2=25$，$(k+3)^2=16$，$k+3=\pm4$．因为 $k>0$，取 $k+3=4$，得 $k=1$．
			\par\textbf{理由：} 利用结论直接得到最小值表达式，并解方程．
		\item[\textbf{第三步（求对应 $x$）：}] 当 $A(2,-k),B(5,3),P(x,0)$ 共线时取等，由 $\frac{x-2}{k}=\frac{5-x}{3}$，代入 $k=1$ 得 $\frac{x-2}{1}=\frac{5-x}{3}$，解得 $3(x-2)=5-x$，$3x-6=5-x$，$4x=11$，$x=\frac{11}{4}$．
			\par\textbf{理由：} 共线条件解出 $x$．
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$k=1$，此时 $x=\frac{11}{4}$．
\end{examplebox}
