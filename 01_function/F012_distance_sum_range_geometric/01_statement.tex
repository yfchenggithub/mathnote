\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（参数范围）：}}] $a>0$, $b>0$, $m,n\in\mathbb{R}$，函数 $f(x)=\sqrt{(x-m)^2+a^2}+\sqrt{(x-n)^2+b^2}$．
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（距离最小值）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			将函数值转化为两段线段长度之和，通过坐标几何求最小值．
			\[
				f(x) = |PA| + |PB|,\quad
				\text{值域}\ \bigl[\sqrt{(m-n)^2+(a+b)^2},+\infty\bigr),
			\]
			其中 $A(m,-a)$, $B(n,b)$, $P(x,0)$．
			当 $A,P,B$ 三点共线时 $f(x)$ 取最小值 $|AB|=\sqrt{(m-n)^2+(a+b)^2}$，此时 $x$ 介于 $m$ 与 $n$ 之间．

		\item[\textbf{\textcolor{CStatement}{推广结论（斜率型）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			分式型函数的值域可转化为定点与动点连线的斜率范围．
			对于函数 $y=\dfrac{u(x)-y_0}{v(x)-x_0}$（$v(x)\neq x_0$），其值域等于定点 $(x_0,y_0)$ 与动点 $(v(x),u(x))$ 所连直线的斜率取值范围．
	\end{description}
\end{statementbox}
