\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（参数限定）：}}]
			原函数 $y=f(x)$ 已给定，且参数 $\omega>0$ 且 $\omega\neq 1$。
	\end{description}
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（横坐标变换）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			横坐标变为原来的 $\dfrac{1}{\omega}$，纵坐标不变，新函数解析式为 $y=f(\omega x)$。
			\[
				y = f(\omega x)
			\]
		\item[\textbf{\textcolor{CStatement}{推广结论（参数互换）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			横坐标变为原来的 $k$ 倍（$k>0,k\neq1$），新解析式为 $y=f\!\left(\dfrac{x}{k}\right)$。
			\[
				y = f\!\left(\frac{x}{k}\right)
			\]
	\end{description}
	\textbf{\textcolor{CStatement}{边界情形（恒等变换）：}}
	当 $\omega=1$（或 $k=1$）时，横坐标不变，图像与原图像重合，解析式不变。
\end{statementbox}
