\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	\textbf{横坐标伸缩变换的本质：横坐标缩放倍数 $\dfrac{1}{\omega}$ 与自变量系数 $\omega$ 互为倒数，新函数为 $y=f(\omega x)$。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（参数正数）：} 伸缩系数 $\omega>0$ 且 $\omega\neq 1$，保证变换非退化。
		\item \textbf{条件2（纵坐标不变）：} 变换过程仅改变横坐标，纵坐标保持不变。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（参数对应）：} 横坐标变为原来的 $k$ 倍时，$\omega=\dfrac{1}{k}$，新函数为 $y=f\!\left(\dfrac{x}{k}\right)$，不是 $y=f(kx)$。
		\item \textbf{检查项（方向判断）：} 当 $\omega>1$ 时横坐标压缩，当 $0<\omega<1$ 时横坐标拉伸。
	\end{itemize}

\end{summarybox}
