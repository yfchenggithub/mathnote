\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}
	横坐标缩放改变的是图像在水平方向的宽度，纵坐标不变，因此函数表达式中的自变量$x$需要乘以因子$\omega$来匹配新坐标。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（坐标缩放）：}]
			原图像上任意点$(x_0, f(x_0))$经变换后变为$\bigl(\frac{x_0}{\omega}, f(x_0)\bigr)$，纵坐标保持不变。
		\item[\textbf{要点二（解析推导）：}]
			设新图像上点$(X, Y)$，则$X = \frac{x_0}{\omega}$，$x_0 = \omega X$，因此$Y = f(x_0)$，即 $Y = f(\omega X)$，故新函数为$y = f(\omega x)$。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（水平缩放）}}
	图像在水平方向按比例$\frac{1}{\omega}$缩放，缩放中心为$y$轴（纵轴），图像整体在$x$轴方向被压缩或拉长。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（参数倒置）}}
	横坐标伸缩因子$\omega$与函数自变量$x$的系数$\omega$相同，表明$\omega$越大，自变量被放大越多，图像反而在水平方向收缩（因为$\frac{1}{\omega}$变小）。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（图像变换）}}
	横坐标伸缩是函数图像变换的基础操作，常与平移、对称、纵坐标伸缩结合出现，解题时需注意变换顺序对解析式的影响。

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	记住“反直觉”：横坐标缩放因子与自变量系数互为倒数——横坐标放大（$0<\omega<1$）对应系数变小，图像拉伸；横坐标缩小（$\omega>1$）对应系数变大，图像压缩。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（正向求解析式）：} 已知原函数$y=f(x)$和横坐标伸缩因子$\omega$，直接代入公式$y=f(\omega x)$得到新函数。
		\item \textbf{场景二（逆向求因子）：} 已知变换前后的解析式，比较自变量系数，确定横坐标伸缩因子$\omega$。
	\end{itemize}

\end{explanationbox}
