\begin{examplebox}
	\textbf{\textcolor{CExample}{例1（基础）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知函数$y=f(x)=x^2$，将图象上所有点的横坐标变为原来的$\dfrac{1}{3}$（纵坐标不变），求新函数的解析式，并说明图象变化。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（参数确定）：}]
			横坐标变为原来的$\dfrac{1}{3}$，即$\dfrac{1}{\omega}=\dfrac{1}{3}$，解得$\omega=3$。
			\[
				\omega = 3.
			\]
			\par\textbf{理由：} 变换规则$\dfrac{1}{\omega}$等于给定的缩放比例。
		\item[\textbf{第二步（套用公式）：}]
			代入变换公式$y=f(\omega x)$，得新函数
			\[
				\begin{aligned}
					y &= f(3x) \\ &= (3x)^2 \\ &= 9x^2.
			\end{aligned}
			\]
			\par\textbf{理由：} 直接使用横坐标伸缩变换结论。
		\item[\textbf{第三步（变化描述）：}]
			因为$\omega=3>1$，所以图象横向压缩为原来的$\dfrac{1}{3}$。
			\par\textbf{理由：} 当$\omega>1$时图象横向压缩。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{新函数为$y=9x^2$，图象横向压缩为原来的$\dfrac{1}{3}$。}

	\medskip
	\textbf{\textcolor{CExample}{例2（稍变形）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知函数$y=\ln x$，将图象上所有点的横坐标扩大到原来的$2$倍（纵坐标不变），求新函数的解析式，并说明图象变化。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（比例转化）：}]
			扩大到原来的2倍即横坐标变为原来的$2$倍，所以$\dfrac{1}{\omega}=2$，得$\omega=\dfrac{1}{2}$。
			\[
				\omega = \frac{1}{2}.
			\]
			\par\textbf{理由：} 将文字表述转化为数学比例。
		\item[\textbf{第二步（套用公式）：}]
			代入$y=f(\omega x)$得$y=\ln\!\left(\dfrac{1}{2}x\right)$。
			\[
				y = \ln\!\left(\frac{1}{2}x\right).
			\]
			\par\textbf{理由：} 使用横坐标伸缩变换结论。
		\item[\textbf{第三步（变化描述）：}]
			因为$\omega=\dfrac{1}{2}<1$，所以图象横向拉伸为原来的$2$倍（即$\dfrac{1}{\omega}=2$）。
			\par\textbf{理由：} 当$0<\omega<1$时图象横向拉伸。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{新函数为$y=\ln\!\left(\dfrac{1}{2}x\right)$，图象横向拉伸为原来的$2$倍。}

	\medskip
	\textbf{\textcolor{CExample}{例3（综合应用）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知函数$y=\sin x$，先将其图象上所有点的横坐标压缩为原来的$\dfrac{1}{2}$（纵坐标不变），再向上平移$1$个单位，求最终函数的解析式。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（横坐标压缩）：}]
			横坐标变为原来的$\dfrac{1}{2}$，所以$\dfrac{1}{\omega}=\dfrac{1}{2}$，$\omega=2$；由伸缩变换得$y=\sin(2x)$。
			\[
				y = \sin(2x).
			\]
			\par\textbf{理由：} 代入横坐标伸缩变换公式。
		\item[\textbf{第二步（向上平移）：}]
			将$y=\sin(2x)$向上平移$1$个单位，由平移规则得$y=\sin(2x)+1$。
			\[
				y = \sin(2x) + 1.
			\]
			\par\textbf{理由：} 纵坐标平移：上加下减。
		\item[\textbf{第三步（综合确认）：}]
			最终函数解析式为
			\[
				y = \sin(2x) + 1.
			\]
			\par\textbf{理由：} 先压缩后平移依次应用变换规则得到。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{最终函数为$y=\sin(2x)+1$。}
\end{examplebox}
