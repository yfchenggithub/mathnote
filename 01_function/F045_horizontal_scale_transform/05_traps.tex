\begin{trapbox}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（参数对应错误）}}]
			误认为横坐标变为原来的$k$倍时，新函数为$y=f(kx)$。
		\item[\textbf{\textcolor{CTrap}{正确理解（参数对应）：}}]
			横坐标变为原来的$k$倍，即$\dfrac{1}{\omega}=k$，故$\omega=\dfrac{1}{k}$，新函数应为$y=f\!\left(\dfrac{x}{k}\right)$。
		\item[\textbf{\textcolor{CTrap}{错因分析（混淆因子）：}}]
			混淆了变换比例$k$与自变量系数$\omega$的关系，未正确建立坐标关系。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（方向颠倒）}}]
			错误认为$\omega>1$时图象横向拉伸，$0<\omega<1$时图象横向压缩。
		\item[\textbf{\textcolor{CTrap}{正确理解（方向判别）：}}]
			$\omega>1$时横坐标变为$\dfrac{1}{\omega}<1$，图象压缩；$0<\omega<1$时横坐标变为$\dfrac{1}{\omega}>1$，图象拉伸。
		\item[\textbf{\textcolor{CTrap}{错因分析（习惯定势）：}}]
			只关注$\omega$大小，忽略了实际缩放倍数为$\dfrac{1}{\omega}$，导致方向判断错误。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（忽略特例）}}]
			忽视$\omega=1$的情况，认为任意正数$\omega$都适用。
		\item[\textbf{\textcolor{CTrap}{正确理解（特例说明）：}}]
			当$\omega=1$时横坐标不变，图象不变，该变换为恒等变换，不属于本结论讨论的伸缩变换。
		\item[\textbf{\textcolor{CTrap}{错因分析（条件遗漏）：}}]
			未注意结论中$\omega\neq1$的限制，或对恒等变换缺乏认识。
	\end{description}

\end{trapbox}
