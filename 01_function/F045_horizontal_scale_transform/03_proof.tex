\begin{proofbox}
	\textbf{\textcolor{CProof}{思路提示}}
	从函数图象上的任意点出发，利用坐标变换关系，将新点坐标代入原函数表达式中，消去中间变量即可得到新函数解析式。

	\medskip
	\textbf{\textcolor{CProof}{正式推导}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.55em]
		\item[\textbf{步骤一（设定任意点）：}]
			在原函数 $y=f(x)$ 的图象上任取一点 $P(x_0, y_0)$，则有 $y_0 = f(x_0)$。
			\[
				y_0 = f(x_0)
			\]
			\par\textbf{理由：} 点在原函数图象上，满足函数关系。
		\item[\textbf{步骤二（坐标变换）：}]
			经过横坐标伸缩变换后，点 $P$ 对应到新图象上的点 $P'(X, Y)$，满足
			\[
				X = \frac{x_0}{\omega},
				\qquad
				Y = y_0.
			\]
			\par\textbf{理由：} 横坐标变为原来的 $\dfrac{1}{\omega}$，纵坐标不变。
		\item[\textbf{步骤三（代入消元）：}]
			由 $Y = y_0$ 和 $y_0 = f(x_0)$ 得 $Y = f(x_0)$；再将 $x_0 = \omega X$ 代入，得
			\[
				Y = f(\omega X).
			\]
			\par\textbf{理由：} 利用坐标关系 $x_0 = \omega X$ 替换原自变量。
		\item[\textbf{步骤四（解析式总结）：}]
			由于 $P'(X, Y)$ 是新图象上任意一点，故新图象上所有点的坐标都满足 $Y = f(\omega X)$。将变量名改写为 $(x, y)$，即得新函数解析式
			\[
				y = f(\omega x).
			\]
			\par\textbf{理由：} 点的任意性保证函数关系对整个图象成立。
	\end{description}

	\medskip
	\textbf{\textcolor{CProof}{结论回扣}}
	由此可知，横坐标变为原来的 $\dfrac{1}{\omega}$ 后，函数解析式变为 $y = f(\omega x)$。当 $\omega > 1$ 时，$\dfrac{1}{\omega} < 1$，横坐标缩短，图象横向压缩；当 $0 < \omega < 1$ 时，$\dfrac{1}{\omega} > 1$，横坐标伸长，图象横向拉伸。
\end{proofbox}
