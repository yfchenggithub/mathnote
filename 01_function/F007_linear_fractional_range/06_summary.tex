\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	一次分式函数 $y=\dfrac{ax+b}{{cx+d}}$（$c\neq0, ad\neq bc$）通过分离常数可化为 $y=\dfrac{a}{c}+\dfrac{bc-ad}{{c(cx+d)}}$，其值域为全体实数除去 $\dfrac{a}{c}$。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（系数条件）：} $c\neq0$ 且 $ad\neq bc$（保证函数不是常数，且为真分式）。
		\item \textbf{条件 2（定义域条件）：} $x\neq -\dfrac{d}{c}$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（条件缺省）：} 使用值域结论前必须确认 $ad\neq bc$，否则函数为常数，值域是单点。
		\item \textbf{检查项（分离常数）：} 分离后反比例部分分子是 $bc-ad$，注意符号，避免写成 $b$ 或 $ad-bc$。
	\end{itemize}

\end{summarybox}
