\begin{trapbox}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（渐近线误解）}}]
			认为 $y=\dfrac{a}{c}$ 能被取到，或以为值域为 $\mathbb{R}$。
		\item[\textbf{\textcolor{CTrap}{正确理解（水平渐近线）：}}]
			$y=\dfrac{a}{c}$ 是函数的水平渐近线，函数值无限接近但永远不取，故值域排除 $\dfrac{a}{c}$。
		\item[\textbf{\textcolor{CTrap}{错因分析（未分离常数）：}}]
			没有通过分离常数得到 $y=\dfrac{a}{c}+$非零反比例形式，误以为分子分母都是线性因此可取所有实数。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（漏用条件）}}]
			忽略 $ad\neq bc$ 条件，直接在 $ad=bc$ 时也使用值域结论。
		\item[\textbf{\textcolor{CTrap}{正确理解（常数区分）：}}]
			当 $ad=bc$ 时，函数化为常数 $y=\dfrac{a}{c}$，值域为单点集，而不是排除该点。
		\item[\textbf{\textcolor{CTrap}{错因分析（适用范围不明）：}}]
			结论推导过程中要求 $ad\neq bc$ 以保证分式不是常数，否则分离常数后反比例部分系数为零。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（分离错误）}}]
			分离常数时配凑出错，比如将 $y=\frac{ax+b}{{cx+d}}$ 错误化为 $y=\frac{a}{c}+\frac{b}{{cx+d}}$。
		\item[\textbf{\textcolor{CTrap}{正确理解（正确分离形式）：}}]
			正确结果应为 $y=\frac{a}{c}+\frac{bc-ad}{{c(cx+d)}}$，余项分子是 $bc-ad$ 而非 $b$。
		\item[\textbf{\textcolor{CTrap}{错因分析（计算粗心）：}}]
			配凑时未将分子按分母倍数展开，忽略了常数项对齐。
	\end{description}

\end{trapbox}
