\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（前提条件）：}}]
			函数为 $y=\dfrac{ax+b}{{cx+d}}$，其中 $a,b,c,d\in\mathbb{R}$，$c\neq0$ 且 $ad\neq bc$。
		\item[\textbf{\textcolor{CStatement}{条件 2（定义域限制）：}}]
			分母不为零，即 $x\neq -\dfrac{d}{c}$。
	\end{description}
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（分离常数）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			将分式化为常数与反比例函数之和。
			\[
				y = \frac{a}{c} + \frac{bc-ad}{{c(cx+d)}}.
			\]
		\item[\textbf{\textcolor{CStatement}{结论二（值域）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			函数值可取除 $\dfrac{a}{c}$ 外的一切实数。
			\[
				\{y\in\mathbb{R}\mid y\neq\frac{a}{c}\}.
			\]
		\item[\textbf{\textcolor{CStatement}{常见变形（对称中心）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			函数图像关于点 $\left(-\dfrac{d}{c},\dfrac{a}{c}\right)$ 对称。
			\[
				\left(-\frac{d}{c},\frac{a}{c}\right).
			\]
	\end{description}

\end{statementbox}
