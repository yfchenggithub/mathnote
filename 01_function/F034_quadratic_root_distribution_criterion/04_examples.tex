\begin{examplebox}
	\textbf{\textcolor{CExample}{例1（基础）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知二次函数$f(x)=x^2+2x+m$在区间$(-2,1)$内有两个不同的零点，求实数$m$的取值范围。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（判别式条件）：}]
			$\Delta=4-4m>0$，解得$m<1$。
			\par\textbf{理由：} 两不等实根的必要条件。
		\item[\textbf{第二步（对称轴条件）：}]
			对称轴$x_0=-1$，满足$-2<-1<1$，自动成立。
			\par\textbf{理由：} 对称轴必在区间内。
		\item[\textbf{第三步（端点同号条件）：}]
			$f(-2)=m>0$且$f(1)=3+m>0$，得$m>0$。
			\par\textbf{理由：} $a=1>0$，端点函数值需为正。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	$m\in(0,1)$。

	\medskip
	\textbf{\textcolor{CExample}{例2（稍变形）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知二次函数$f(x)=x^2-2kx+4$在区间$(-1,1)$内恰有一个零点（端点不是零点），求实数$k$的取值范围。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（应用唯一根条件）：}]
			根据结论二，$f(-1)f(1)<0$。计算$f(-1)=1+2k+4=2k+5$，$f(1)=1-2k+4=5-2k$，得$(2k+5)(5-2k)<0$。
			\par\textbf{理由：} 开区间内恰有一个零点的充要条件。
		\item[\textbf{第二步（解不等式）：}]
			$(2k+5)(5-2k)<0$化为$(2k+5)(2k-5)>0$，解得$k<-\frac{5}{2}$或$k>\frac{5}{2}$。
			\par\textbf{理由：} 二次不等式求解。
		\item[\textbf{第三步（检查端点非根）：}]
			当$k=-\frac{5}{2}$时$f(-1)=0$，当$k=\frac{5}{2}$时$f(1)=0$，均不满足“端点不是零点”，故舍去。
			\par\textbf{理由：} 原结论要求端点非根。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	$k\in(-\infty,-\frac{5}{2})\cup(\frac{5}{2},+\infty)$。

	\medskip
	\textbf{\textcolor{CExample}{例3（综合应用）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知二次函数$f(x)=x^2+mx+4$在区间$(1,3)$内恰有一个零点，求实数$m$的取值范围。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（端点异号情形）：}]
			若$f(1)f(3)<0$，则区间内恰有一个零点（非端点）。$f(1)=m+5$，$f(3)=3m+13$，得$(m+5)(3m+13)<0$，解得$-5<m<-\frac{13}{3}$。
			\par\textbf{理由：} 对应唯一根条件。
		\item[\textbf{第二步（重根情形）：}]
			若$\Delta=m^2-16=0$且对称轴$x_0=-\frac{m}{2}$在$(1,3)$内，则重根在区间内。由$m^2=16$得$m=\pm4$，对称轴条件$1<-\frac{m}{2}<3$得$-6<m<-2$。同时满足的只有$m=-4$。
			\par\textbf{理由：} 重根视为一个零点，但端点符号条件不同，需单独处理。
		\item[\textbf{第三步（合并与检查）：}]
			将两种情形的结果取并集：$-5<m<-\frac{13}{3}$或$m=-4$。验证端点非根：$m=-4$时$f(1)=1\neq0$，$f(3)=1\neq0$，符合。
			\par\textbf{理由：} 最终范围需满足题目条件。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	$m\in(-5,-\frac{13}{3})\cup\{-4\}$。
\end{examplebox}
