\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}
	二次函数在某个开区间内与$x$轴交点的个数，完全由判别式、对称轴和端点函数值的符号决定。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（数形结合）：}]
			画出抛物线草图，确定开口方向、对称轴、区间位置；根据交点个数调整条件。
		\item[\textbf{要点二（充要条件逻辑）：}]
			两实根条件同时控制判别式、对称轴和端点符号；一个实根只需端点符号异号。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（抛物线穿轴次数）}}
	二次函数在开区间内与$x$轴的交点个数，等价于抛物线穿过$x$轴的次数。两根对应穿过两次，一根对应穿过一次。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（判别式符号约束）}}
	$\Delta>0$保证两个不同实根；$m<x_0<n$保证对称轴在区间内，从而两根分布在区间内部；$af(m)>0,af(n)>0$保证区间端点位于两根外侧（与$a$同号）。对于单根，$f(m)f(n)<0$由介值性保证存在一根。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（高考高频考点）}}
	常出现在含参数二次方程根的分布问题中，是导数、解析几何的基础。
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（已知根分布求参）：} 根据区间内根的个数，转化为不等式组求参数范围。
		\item \textbf{考法二（参数不等式组）：} 将根分布条件转化为关于参数的不等式组，解出参数。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	图像是根本！画出草图，所有条件可直观读出：两要“同号且对称轴在内”，一要“异号”。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（区间内两根问题）：} 求证或求解两根都在给定区间内。
		\item \textbf{场景二（区间内单根问题）：} 证明或求解恰有一根在区间内。
	\end{itemize}

\end{explanationbox}
