\begin{proofbox}
	\textbf{\textcolor{CProof}{思路提示}}
	从图像入手，结合判别式、对称轴、端点符号逐步导出充要条件。

	\medskip
	\textbf{\textcolor{CProof}{正式推导}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.55em]
		\item[\textbf{步骤一（两不等根：必要性）：}]
			设$f(x)=0$在$(m,n)$内有两个不等实根$x_1,x_2$，则$\Delta>0$；两根关于$x_0$对称，故$m<x_0<n$；因为两根在$(m,n)$内，故端点$m,n$在两根之外，且$a>0$时$f(m)>0,f(n)>0$，$a<0$时$f(m)<0,f(n)<0$，统一写为$af(m)>0,af(n)>0$。
			\par\textbf{理由：} 根在区间内$\Rightarrow$区间端点必在两根之外，故函数值与$a$同号。
		\item[\textbf{步骤二（两不等根：充分性）：}]
			若$\Delta>0$，设两根$x_1<x_2$。由$m<x_0<n$，得$x_1<x_0<x_2$。由$af(m)>0$，得$f(m)$与$a$同号。对于$a>0$，$f(m)>0$说明$m$不在区间$(x_1,x_2)$内（否则$f(m)<0$）。结合$m<x_0$，可得$m<x_1$。同理$f(n)>0$且$x_0<n$得$x_2<n$。故$x_1,x_2\in(m,n)$。$a<0$时类似。
			\par\textbf{理由：} 利用二次函数在两根之间与两侧的符号规律。
		\item[\textbf{步骤三（唯一根：必要性）：}]
			设$(m,n)$内恰有一个实根$c$（非端点）。若$f(m)f(n)>0$，则区间两端同号，此时二次函数在区间内要么无实根，要么有两个实根（可能相等），不可能恰有一个单根。因此必有$f(m)f(n)<0$。
			\par\textbf{理由：} 二次函数在两端同号时，实根个数为偶数（0个或2个，重根视为两个）；单根个数为奇数，故两端必异号。
		\item[\textbf{步骤四（唯一根：充分性）：}]
			若$f(m)f(n)<0$，由零点存在定理（图像连续）可知存在$c\in(m,n)$使得$f(c)=0$。若还有另一根$d\neq c$，则$d$或$c$必有一根在$(m,n)$外（否则两根均在内，故$f(m),f(n)$同号），因而$(m,n)$内恰有一根。
			\par\textbf{理由：} 二次函数最多两实根，端点异号限制另一根不能在区间内。
	\end{description}

	\medskip
	\textbf{\textcolor{CProof}{结论回扣}}
	上述推导完整论证了二次方程在开区间内根分布的两个充要条件，体现了数形结合的数学思想。
\end{proofbox}
