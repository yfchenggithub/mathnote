\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	二次函数在开区间内的根分布由判别式、对称轴和端点符号共同决定。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（区间内两不等根）：} 需要$\Delta>0$，对称轴在区间内，端点与$a$同号（$af(m)>0,af(n)>0$）。
		\item \textbf{条件2（区间内单根）：} 需要端点函数值异号（$f(m)f(n)<0$）。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（判别式符号）：} 两不等根务必保证$\Delta>0$，不能忽略。
		\item \textbf{检查项（区间端点非根）：} 使用单根条件时自动满足，但两不等根条件也隐含端点非根（$af(m)>0$）。
	\end{itemize}

\end{summarybox}
