\begin{explanationbox}

	\textbf{\textcolor{CExplanation}{一句话直觉}}

	二次函数的值恒为正数，等价于抛物线全部位于 $x$ 轴上方，即开口向上且顶点在 $x$ 轴以上。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（开口方向）：}] 当 $a>0$ 时，抛物线开口向上，$x\to\pm\infty$ 时 $f(x)\to+\infty$，保证函数值不会趋向负无穷。
		\item[\textbf{要点二（判别式符号）：}] $\Delta<0$ 意味着二次方程 $f(x)=0$ 无实根，二次函数的图像与 $x$ 轴没有交点，因此所有函数值同号。结合 $a>0$，同为正号。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（图像特征）}}

	抛物线开口向上且位于 $x$ 轴上方，与 $x$ 轴无公共点。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（判别式作用）}}

	$\Delta<0$ 表明二次函数的二次项系数与函数值符号一致（$a>0$ 时恒正，$a<0$ 时恒负）。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（命题角度）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（参数范围）：} 已知二次函数在 $\mathbb{R}$ 上恒正，求解析式中参数的取值范围。
		\item \textbf{考法二（不等式迁移）：} 将未定型不等式通过移项、配方转化为二次函数恒正问题。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	把抽象的不等式恒成立转化为具体的抛物线图像状态，视觉化判断。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（直接判断）：} 已知二次函数解析式，判断其是否恒正。
		\item \textbf{场景二（参数求解）：} 已知二次函数恒正，求所含参数的取值区间。
	\end{itemize}
\end{explanationbox}
