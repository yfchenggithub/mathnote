\begin{trapbox}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（忽略开口）：}}] 只由 $\Delta<0$ 推出参数范围，而没有检查二次项系数 $a>0$。例如认为 $f(x)=-x^2+2x-2$（$\Delta<0$）恒正，实际上 $-x^2+2x-2$ 恒负。
		\item[\textbf{\textcolor{CTrap}{正确理解（缺一不可）：}}] 充要条件是 $a>0$ 且 $\Delta<0$，两个条件必须同时满足。
		\item[\textbf{\textcolor{CTrap}{错因分析（图像缺失）：}}] 只关注判别式，忘记了抛物线开口方向对函数值符号的决定作用。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（混淆等号）：}}] 误将 $\Delta\le0$ 当作恒正的条件，导致 $\Delta=0$ 时误判。例如 $f(x)=x^2-2x+1$（$\Delta=0$），实际 $f(x)\ge0$，但存在 $x=1$ 使 $f(1)=0$，不满足 $>0$。
		\item[\textbf{\textcolor{CTrap}{正确理解（严格不等）：}}] 恒正必须 $\Delta<0$，不能取等号；$\Delta\le0$ 对应的是恒非负。
		\item[\textbf{\textcolor{CTrap}{错因分析（等号流失）：}}] 忽视“$\Delta=0$ 时存在零点”这一事实，将“非负”与“正”等价。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（系数为零）：}}] 当二次项系数含有参数时，直接套用 $\Delta<0$，而没有先讨论参数是否使 $a=0$。例如 $f(x)=(k-2)x^2+2x+1$，若 $k=2$ 则变成一次函数，不适用本结论。
		\item[\textbf{\textcolor{CTrap}{正确理解（首项非零）：}}] 使用本充要条件的前提是函数为二次函数，即 $a\neq0$。若参数可能使 $a=0$，应首先分类讨论。
		\item[\textbf{\textcolor{CTrap}{错因分析（前提遗忘）：}}] 忽略结论的适用条件（$a\neq0$），默认为二次函数而导致漏解或错解。
	\end{description}

\end{trapbox}
