\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)=2x^2-4x+1$，$x\in[-1,3]$，求 $f(x)$ 的最大值和最小值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求对称轴）：}] $x_0=-\frac{-4}{2\times2}=1$。
			\par\textbf{理由：} 对称轴公式 $x_0=-\frac{b}{2a}$。
		\item[\textbf{第二步（判断开口和位置）：}] $a=2>0$，开口向上；$x_0=1\in[-1,3]$。
			\par\textbf{理由：} 开口方向和对称轴与区间的关系确定最值候选。
		\item[\textbf{第三步（套用规则求最值）：}] 最小值 $f(1)=2-4+1=-1$；最大值计算 $f(-1)=2+4+1=7$，$f(3)=18-12+1=7$，故 $f_{\max}=7$。
			\par\textbf{理由：} $a>0$ 且对称轴在区间内，最小值为顶点值，最大值为端点较大者。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f_{\min}=-1$，$f_{\max}=7$。}

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)=-x^2+2x+3$，$x\in[0,3]$，求 $f(x)$ 的最大值和最小值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求对称轴）：}] $x_0=-\frac{2}{2\times(-1)}=1$。
			\par\textbf{理由：} 对称轴公式。
		\item[\textbf{第二步（判断开口和位置）：}] $a=-1<0$，开口向下；$x_0=1\in[0,3]$。
			\par\textbf{理由：} 开口方向与区间包含对称轴的情况。
		\item[\textbf{第三步（套用规则求最值）：}] 最大值 $f(1)=-1+2+3=4$；最小值计算 $f(0)=3$，$f(3)=-9+6+3=0$，取较小者 $f_{\min}=0$。
			\par\textbf{理由：} $a<0$ 且对称轴在区间内，最大值为顶点值，最小值为端点较小者。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f_{\max}=4$，$f_{\min}=0$。}

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)=x^2-2x+3$ 在区间 $[0,m]$ 上的最小值为 $2$，求实数 $m$ 的取值范围。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求对称轴）：}] $f(x)=(x-1)^2+2$，对称轴 $x_0=1$，开口向上。
			\par\textbf{理由：} 顶点式可直接得出对称轴和顶点值。
		\item[\textbf{第二步（分类讨论）：}] 根据区间 $[0,m]$ 与对称轴 $x=1$ 的位置分类：
			\begin{itemize}[leftmargin=*, itemsep=0.3em]
			\item 若 $m<1$，则区间在对称轴左侧，函数递减，最小值在右端点 $x=m$ 处。由 $f(m)=m^2-2m+3=2$，得 $m^2-2m+1=0$，即 $(m-1)^2=0$，解得 $m=1$，与 $m<1$ 矛盾，故无解。
			\item 若 $m\geq1$，则对称轴在区间内，最小值在顶点 $x=1$ 处，$f(1)=2$ 恒成立，故 $m\geq1$ 满足条件。
		\end{itemize}
			\par\textbf{理由：} 开口向上时，最小值在顶点（对称轴在区间内）或离对称轴最近的端点（对称轴在区间外）。
		\item[\textbf{第三步（综合结论）：}] 综上，$m$ 的取值范围为 $m\geq1$，即 $m\in[1,+\infty)$。
			\par\textbf{理由：} $m<1$ 时无解，$m\geq1$ 时恒成立，故取并集得 $m\geq1$。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$m\in[1,+\infty)$}。
\end{examplebox}
