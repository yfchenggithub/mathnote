\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{二次函数在闭区间上的最值完全由开口方向 $a>0$ 或 $a<0$ 以及对称轴 $x_0$ 相对于区间 $[m,n]$ 的位置决定。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（二次函数）：} 函数 $f(x)=ax^2+bx+c$，其中 $a\neq0$。
		\item \textbf{条件 2（固定区间）：} 闭区间 $[m,n]$，$m<n$，且对称轴 $x_0=-\frac{b}{2a}$ 与区间均为已知常数。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（区分开口）：} 务必先确认 $a>0$ 还是 $a<0$，然后套用对应的最值规则。
		\item \textbf{检查项（验证位置）：} 必须计算 $x_0$ 是否满足 $m\leq x_0\leq n$，只有满足时顶点才参与最值比较。
	\end{itemize}
\end{summarybox}
