\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（混淆开口方向）}}] 无论二次项系数 $a$ 是正还是负，一律认为顶点是最小值。
		\item[\textbf{\textcolor{CTrap}{正确理解（区分最值类型）}}] 当 $a>0$ 时，顶点是最小值；当 $a<0$ 时，顶点是最大值。
		\item[\textbf{\textcolor{CTrap}{错因分析（开口决定）}}] 开口向上时抛物线有最低点，开口向下时有最高点；忽略了 $a$ 的符号对最值类型的决定作用。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（对称轴位置误判）}}] 未核实对称轴是否落在区间 $[m,n]$ 内，想当然认为顶点值一定在区间内可取得。
		\item[\textbf{\textcolor{CTrap}{正确理解（必须比较）}}] 必须计算 $x_0=-\frac{b}{2a}$ 并检查是否满足 $m\leq x_0\leq n$，只有满足时顶点才参与最值。
		\item[\textbf{\textcolor{CTrap}{错因分析（条件遗漏）}}] 顶点是最值候选之一，但前提是顶点在区间内；不在区间内时最值完全取决于端点。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（端点值遗漏）}}] 求最值时只计算了一个端点的函数值，或忘记比较两个端点。
		\item[\textbf{\textcolor{CTrap}{正确理解（两端点皆需比较）}}] 无论对称轴是否在区间内，两个端点值 $f(m),f(n)$ 都必须计算并比较，以确定最值。
		\item[\textbf{\textcolor{CTrap}{错因分析（单调性认识不清）}}] 对单调性判断不准确，误以为最值只出现在某一点；实际端点值至少有一个是最值。
	\end{description}
\end{trapbox}
