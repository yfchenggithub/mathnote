\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（二次函数）：}}] 设 $f(x)=ax^2+bx+c$，其中 $a\neq0$。
		\item[\textbf{\textcolor{CStatement}{条件 2（轴和区间）：}}] 已知对称轴 $x_0=-\frac{b}{2a}$ 与闭区间 $[m,n]$（$m<n$）均为固定值。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（开口向上）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			当 $a>0$ 时，最小值为顶点值（对称轴在区间内）或端点较小值；最大值为端点较大值。
			\[
				f_{\min}=
				\begin{cases}
					f(x_0), & x_0\in[m,n]\\
					\min\{f(m),f(n)\}, & x_0\notin[m,n]
			\end{cases},
				\qquad
				f_{\max}=\max\{f(m),f(n)\}.
			\]

		\item[\textbf{\textcolor{CStatement}{结论二（开口向下）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			当 $a<0$ 时，最大值为顶点值（对称轴在区间内）或端点较大值；最小值为端点较小值。
			\[
				f_{\max}=
				\begin{cases}
					f(x_0), & x_0\in[m,n]\\
					\max\{f(m),f(n)\}, & x_0\notin[m,n]
			\end{cases},
				\qquad
				f_{\min}=\min\{f(m),f(n)\}.
			\]

		\item[\textbf{\textcolor{CStatement}{常见变形（含参最值）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			若二次函数含参数（对称轴随参数变化），区间固定，则最值需按对称轴与区间的位置关系分类讨论，规则与上相同。
	\end{description}
\end{statementbox}
