\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	二次函数在闭区间上的最值，要么在顶点（对称轴处）取到，要么在区间端点取到。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（开口方向）：}] 确定抛物线增减趋势。$a>0$ 开口向上，先减后增；$a<0$ 开口向下，先增后减。开口方向决定了顶点值是最小值还是最大值。
		\item[\textbf{要点二（对称轴位置）：}] 判断对称轴 $x_0$ 是否落在区间 $[m,n]$ 内。若在区间内，则顶点参与最值比较；若不在，则函数在区间上单调，最值仅在端点取得。
		\item[\textbf{要点三（端点比较）：}] 无论何种情形，两个端点值 $f(m),f(n)$ 中至少有一个成为最值。实际计算时不能遗漏。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（抛物线对称性）}}

	抛物线关于 $x=x_0$ 对称，因此区间内离对称轴越远，函数值偏离顶点值越大。最值出现在对称轴或离对称轴最远的端点。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（单调性分段）}}

	二次函数在对称轴两侧单调性相反，故区间被对称轴分割后，最值出现在单调性改变点（顶点）或区间端点。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（分类讨论）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（直接套用）：} 已知具体二次函数和闭区间，直接判断开口方向与对称轴位置，套规则求最值。
		\item \textbf{考法二（参数讨论）：} 函数或区间含参数时，需分类讨论对称轴与区间的相对位置，再应用基本规则。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	\textbf{“一轴二端”原则：最值候选只有顶点和两个端点；根据开口方向与对称轴位置，排除一个候选即可得到最值。}

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（最值计算）：} 给定二次函数和固定区间，快速计算最值。
		\item \textbf{场景二（含参推理）：} 已知最值条件反求参数，需先确定对称轴位置，再列方程或不等式。
	\end{itemize}
\end{explanationbox}
