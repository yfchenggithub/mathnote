\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	\textbf{利用函数方程中的常数加减关系，直接套用周期公式$T=|a|$、$T=2|a|$或$T=|a-b|$，快速得到周期。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（定义域封闭性）：} 函数定义域须对等式中的平移常数封闭，即若$x$在定义域内，则$x$加减该常数仍在定义域内。
		\item \textbf{条件2（等式类型识别）：} 准确判断条件属于哪种形式（加常数、反号、倒数、差值），才能选用对应公式。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（周期长度计算）：} 反号型和倒数型周期是$2|a|$，不是$|a|$；差值型注意$a,b$含符号，$T=|a-b|$，不能直接减。
		\item \textbf{检查项（前提条件确认）：} 使用倒数型公式前必须确认$f(x)\neq0$；使用差值型需保证$a\neq b$。
	\end{itemize}

\end{summarybox}
