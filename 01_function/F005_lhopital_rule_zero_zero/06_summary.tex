\begin{summarybox}

\textbf{\textcolor{CSummary}{一句话核心}}

洛必达法则把 $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$ 型极限转化为分子分母导数之比的极限，简化计算。

\medskip
\textbf{\textcolor{CSummary}{使用条件}}
\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
    \item \textbf{条件1（未定式前提）：} 极限必须为 $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$ 型。
    \item \textbf{条件2（可导条件）：} 在去心邻域内 $f,g$ 均可导且 $g'(x)\neq0$，且 $\lim_{x\to a}\frac{f'(x)}{g'(x)}$ 存在或为无穷大。
\end{itemize}

\medskip
\textbf{\textcolor{CSummary}{关键提醒}}
\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
    \item \textbf{易错点（型与导数）：} 每次使用前必须验证未定式类型，并确保分母导数非零。
    \item \textbf{检查项（极限存在性）：} 若 $\lim_{x\to a}\frac{f'(x)}{g'(x)}$ 不存在（非无穷），则法则失效，不能直接判断原极限。
\end{itemize}

\end{summarybox}
