\begin{statementbox}

\textbf{\textcolor{CStatement}{条件}}
\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
    \item[\textbf{\textcolor{CStatement}{条件1（极限同时为零）：}}] 当 $x\to a$ 时，$\displaystyle\lim_{x\to a}f(x)=0$ 且 $\displaystyle\lim_{x\to a}g(x)=0$；
    \item[\textbf{\textcolor{CStatement}{条件2（可导分母非零）：}}] 在 $a$ 的某去心邻域内 $f'(x),g'(x)$ 存在，且 $g'(x)\neq0$；
    \item[\textbf{\textcolor{CStatement}{条件3（导数比极限存在）：}}] 极限 $\displaystyle\lim_{x\to a}\frac{f'(x)}{g'(x)}$ 存在或为无穷大。
\end{description}

\textbf{\textcolor{CStatement}{结论}}
\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
    \item[\textbf{\textcolor{CStatement}{结论一（$\frac{0}{0}$型法则）：}}]
    \textbf{\textcolor{CStatement}{直观描述：}} 分子分母同时导，极限不变。
    \[
    \lim_{x\to a}\frac{f(x)}{g(x)} = \lim_{x\to a}\frac{f'(x)}{g'(x)}.
    \]

    \item[\textbf{\textcolor{CStatement}{推广结论（$\frac{\infty}{\infty}$型法则）：}}]
    \textbf{\textcolor{CStatement}{直观描述：}} 若分子分母均趋于无穷大，同样可用洛必达法则直接对分子分母分别求导。
    \[
    \lim_{x\to a}\frac{f(x)}{g(x)} = \lim_{x\to a}\frac{f'(x)}{g'(x)}.
    \]
\end{description}

\end{statementbox}
