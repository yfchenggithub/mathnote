\begin{trapbox}
\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
    \item[\textbf{\textcolor{CTrap}{易错点一（未先验型）}}] 求极限时未检查是否为 $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$，直接对分子分母求导。
    \item[\textbf{\textcolor{CTrap}{正确理解（判定在先）：}}] 只有未定式才能使用洛必达法则，其他形式（如 $\frac{1}{0}$、$\frac{\text{常数}}{0}$ 等）不能用法则。
    \item[\textbf{\textcolor{CTrap}{错因分析（条件缺漏）：}}] 忽略法则前提，误以为所有分式极限都能通过求导简化。例如 $\lim_{x\to1}\frac{x}{x-1}$ 为 $\frac{1}{0}$ 型，若用洛必达得 $1$，但实际极限为无穷大，错误。
\end{description}

\medskip
\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
    \item[\textbf{\textcolor{CTrap}{易错点二（忽略非零）}}] 使用前未验证分母导数在去心邻域内是否恒不为零，就盲目应用。
    \item[\textbf{\textcolor{CTrap}{正确理解（导数非零）：}}] 必须确保 $g'(x)\neq0$（在 $a$ 的去心邻域内），否则法则失效。
    \item[\textbf{\textcolor{CTrap}{错因分析（条件缺失）：}}] 若 $g'(x)$ 在某点为零，则柯西中值定理的条件被破坏，导致结论不成立。例如 $g(x)=x^2\sin\frac1x$ 在 $0$ 附近导数有零点，不能轻易使用法则。
\end{description}

\medskip
\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
    \item[\textbf{\textcolor{CTrap}{易错点三（无限滥用）}}] 认为可以无限次使用洛必达法则，或认为导数比极限不存在时原极限也不存在。
    \item[\textbf{\textcolor{CTrap}{正确理解（逐步验证）：}}] 每次使用后都须检查是否为未定式；若导数比极限不存在（非无穷），则原极限可能存在也可能不存在。
    \item[\textbf{\textcolor{CTrap}{错因分析（判断误区）：}}] 例如 $\lim_{x\to\infty}\frac{x+\sin x}{x}$，洛必达得 $\frac{1+\cos x}{1}$，该极限不存在，但原极限为 $1$，因此不能盲目下结论。
\end{description}
\end{trapbox}
