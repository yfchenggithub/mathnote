\begin{explanationbox}

\textbf{\textcolor{CExplanation}{一句话直觉}}

洛必达法则的核心是：当分式极限为 $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$ 时，可以分别对分子分母求导，再用求导后的极限代替原极限，通常能简化计算。

\medskip
\textbf{\textcolor{CExplanation}{核心拆解}}
\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
    \item[\textbf{要点一（未定式判定）：}] 使用前必须确认极限是 $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$ 型，否则直接使用会得到错误结果。
    \item[\textbf{要点二（可导与非零检查）：}] 在 $a$ 的去心邻域内，$f(x),g(x)$ 必须可导，且 $g'(x)\neq0$，这是法则成立的前提。
    \item[\textbf{要点三（极限存在性）：}] 极限 $\displaystyle\lim_{x\to a}\frac{f'(x)}{g'(x)}$ 必须存在（或为无穷大），若这个极限不存在（非无穷震荡），则法则失效。
\end{description}

\medskip
\textbf{\textcolor{CExplanation}{几何本质（斜率比近似）}}

当 $x$ 趋近于 $a$ 时，$f(x)$ 和 $g(x)$ 都趋近于 $0$，它们的比值可看作 $x$ 的增量很小的情况下，$f$ 和 $g$ 在 $a$ 处的切线斜率之比。因此，求导后的比值更简单。

\medskip
\textbf{\textcolor{CExplanation}{代数意义（极限转化简化）}}

洛必达法则将原极限 $\displaystyle\lim_{x\to a}\frac{f(x)}{g(x)}$ 转化为其导数之比的极限 $\displaystyle\lim_{x\to a}\frac{f'(x)}{g'(x)}$，往往能消去零因子或无穷大因子，从而简化计算。

\medskip
\textbf{\textcolor{CExplanation}{考点价值（导数求限）}}
\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
    \item \textbf{考法一（直接求极限）：} 出现 $\frac{0}{0}$ 或 $\frac{\infty}{\infty}$ 型的分式极限时，直接用洛必达法则将分子分母同时求导，再代值或进一步变形。
    \item \textbf{考法二（参数范围确定）：} 通过洛必达求出参数影响下的极限值，再结合函数连续性或单调性确定参数取值范围。
\end{itemize}

\medskip
\textbf{\textcolor{CExplanation}{顿悟点}}

洛必达法则是针对未定式极限的“调整工具”，不是普通求导再相加。记住：必须先验型，后求导，再代值。若一次求导后仍是未定式，可继续使用。

\medskip
\textbf{\textcolor{CExplanation}{使用场景}}
\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
    \item \textbf{场景一（分式未定式）：} 如 $\displaystyle\lim_{x\to0}\frac{\sin x}{x}$、$\displaystyle\lim_{x\to0}\frac{e^x-1}{x}$ 等典型 $\frac{0}{0}$ 极限。
    \item \textbf{场景二（无穷小量比值）：} 当分子分母均为无穷小或无穷大，且含有对数、指数、三角函数时，优先考虑洛必达法则。
\end{itemize}
\end{explanationbox}
