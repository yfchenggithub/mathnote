\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知定义在 $\mathbb{R}$ 上的奇函数 $f(x)$ 在 $[0,+\infty)$ 上单调递增，解不等式 $f(x-1)+f(2x)>0$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别条件）：}] 函数为奇函数且在 $[0,+\infty)$ 递增，符合奇函数和式结论使用条件。
			\par\textbf{理由：} 已知条件直接满足定理前提。
		\item[\textbf{第二步（转化不等式）：}] 由结论得 $(x-1)+(2x)>0$。
			\par\textbf{理由：} 奇函数且单调递增，函数值和大于零等价于自变量和大于零。
		\item[\textbf{第三步（解代数式）：}] 化简 $3x-1>0$，解得 $x>\frac13$。
			\par\textbf{理由：} 一元一次不等式求解。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	原不等式的解集为 $\left(\frac13,+\infty\right)$。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知定义在 $\mathbb{R}$ 上的偶函数 $f(x)$ 在 $[0,+\infty)$ 上单调递增，且 $f(1)<f(2a-1)$，求实数 $a$ 的取值范围。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别条件）：}] 偶函数且在 $[0,+\infty)$ 递增，可用偶函数大小比较结论 $f(u)>f(v)\iff|u|>|v|$。
			\par\textbf{理由：} 已知条件符合定理使用前提。
		\item[\textbf{第二步（转化不等式）：}] $f(1)<f(2a-1)$ 等价于 $|1|<|2a-1|$，即 $|2a-1|>1$。
			\par\textbf{理由：} 偶函数递增时，函数值大对应绝对值大；所以 $f(1)<f(2a-1)$ 意味着 $|1|<|2a-1|$。
		\item[\textbf{第三步（解绝对值不等式）：}] 解 $|2a-1|>1$ 得 $a<0$ 或 $a>1$。
			\par\textbf{理由：} 绝对值不等式解法：$|X|>1\iff X<-1$ 或 $X>1$。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$a$ 的取值范围是 $(-\infty,0)\cup(1,+\infty)$。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知定义在 $[-3,3]$ 上的奇函数 $f(x)$ 在 $[0,3]$ 上单调递减，解不等式 $f(t)+f(t+1)>0$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别反向条件）：}] 奇函数且在 $[0,3]$ 上递减，故使用反向结论 $f(a)+f(b)>0\iff a+b<0$。
			\par\textbf{理由：} 由定理，单调递减时不等号反向。
		\item[\textbf{第二步（转化不等式）：}] 由结论得 $t+(t+1)<0$，解得 $t<-\frac12$。
			\par\textbf{理由：} 直接套用 $a+b<0$。
		\item[\textbf{第三步（定义域检查）：}] 由
			\[
				\left\{\begin{array}{l}
				-3\le t\le 3,\\
				-3\le t+1\le 3
		\end{array}\right.
		\]
		解得 $-3\le t\le2$。与 $t<-\frac12$ 取交集得 $-3\le t<-\frac12$。
		\par\textbf{理由：} 函数定义域必须满足；转化后的自变量需在定义域内。
\end{description}

\textbf{\textcolor{CExample}{关键结论：}}
原不等式的解集为 $[-3,-\frac12)$。
\end{examplebox}
