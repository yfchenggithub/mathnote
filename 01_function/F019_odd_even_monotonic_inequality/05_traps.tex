\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（奇偶结论混淆）}}] 将奇函数的和式结论 $f(a)+f(b)>0\iff a+b>0$ 直接用于偶函数。
		\item[\textbf{\textcolor{CTrap}{正确理解（偶函数用法不同）：}}] 偶函数没有类似的和式等价关系；对于大小比较，有 $f(a)>f(b)\iff|a|>|b|$，但和式无简洁结论。
		\item[\textbf{\textcolor{CTrap}{错因分析（条件误植）：}}] 死记硬背结论，忽略了结论成立的前提是函数为奇函数（不是偶函数）。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（偶函数直接比）}}] 由 $f(a)>f(b)$ 直接推出 $a>b$。
		\item[\textbf{\textcolor{CTrap}{正确理解（需转化绝对值）：}}] 偶函数且单调递增时，应转化为 $|a|>|b|$；若单调递减则转化为 $|a|<|b|$。
		\item[\textbf{\textcolor{CTrap}{错因分析（忽略对称性）：}}] 误认为偶函数在定义域内单调一致，实际在 $(-\infty,0]$ 上单调性与正半轴相反。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（单调方向忽略）}}] 当 $f$ 在 $[0,+\infty)$ 单调递减时，仍套用递增时的等价关系。
		\item[\textbf{\textcolor{CTrap}{正确理解（不等号反向）：}}] 单调递减时，奇函数等价变为 $f(a)+f(b)>0\iff a+b<0$，偶函数变为 $f(a)>f(b)\iff|a|<|b|$。
		\item[\textbf{\textcolor{CTrap}{错因分析（方向忽略）：}}] 只记住了结论形式，未注意单调性对不等号方向的决定作用。
	\end{description}
\end{trapbox}
