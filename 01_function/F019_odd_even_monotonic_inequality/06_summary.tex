\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	奇函数单调递增时，$f(a)+f(b)>0\iff a+b>0$；偶函数单调递增时，$f(a)>f(b)\iff|a|>|b|$。递减则反向。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（定义域与奇偶）：} 函数定义域关于原点对称，且奇偶性已知。
		\item \textbf{条件2（半轴单调）：} 已知 $f$ 在 $[0,+\infty)$ 上严格单调（增或减）。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（奇偶混淆）：} 奇函数用和式，偶函数用绝对值，不可混用。
		\item \textbf{检查项（方向与定义域）：} 注意单调方向决定不等号是否反向；解不等式后注意定义域限制。
	\end{itemize}
\end{summarybox}
