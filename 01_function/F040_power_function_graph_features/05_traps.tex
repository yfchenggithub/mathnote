\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（过原点条件）}}] 认为所有幂函数图像都经过原点（即认为 $\alpha$ 取任何值都有 $0^\alpha=0$）。
		\item[\textbf{\textcolor{CTrap}{正确理解（分类记忆）：}}] 只有当 $\alpha>0$ 时，$0^\alpha=0$，图像才过原点；$\alpha=0$ 时 $0^0$ 无意义，图像是水平线去掉原点；$\alpha<0$ 时 $0^\alpha$ 无意义，图像不过原点。
		\item[\textbf{\textcolor{CTrap}{错因分析（记忆混淆）：}}] 将幂函数与二次函数类比过度，没有注意到负指数和零指数时 $0^\alpha$ 无定义。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（比较法则条件）}}] 比较两个同底不同指数幂的大小时，直接套用“指数越大值越大”，忽视底数的范围。
		\item[\textbf{\textcolor{CTrap}{正确理解（底数分段）：}}] 应先判断底数 $x$ 与 $1$ 的关系：若 $x>1$，则指数越大值越大；若 $0<x<1$，则指数越大值越小。
		\item[\textbf{\textcolor{CTrap}{错因分析（思维定式）：}}] 长期接触底数大于 $1$ 的指数函数，形成了“指数大值大”的思维定式，未主动区分底数区间。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（凸性方向混淆）}}] 不能正确判断幂函数的上凸与下凸，例如误认为 $y=x^{0.5}$ 是下凸。
		\item[\textbf{\textcolor{CTrap}{正确理解（凸性判定）：}}] 利用二阶导数符号：$y''>0$ 为下凸（凹向上），$y''<0$ 为上凸。或记忆：$\alpha>1$ 时下凸，$0<\alpha<1$ 时上凸。
		\item[\textbf{\textcolor{CTrap}{错因分析（直观误解）：}}] 将“凸”与“鼓包”关联，或者记忆不准确，导致方向颠倒。
	\end{description}
\end{trapbox}
