\begin{statementbox}

	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（函数形式）：}}] 幂函数 $y=x^\alpha$，$\alpha\in\mathbb{R}$。
		\item[\textbf{\textcolor{CStatement}{条件2（定义域限制）：}}] 仅考虑第一象限，即 $x>0$。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（定点性质）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			无论 $\alpha$ 取何值，所有幂函数图像都经过点 $(1,1)$。
			\[
				1^\alpha = 1\quad(\forall\alpha\in\mathbb{R}).
			\]

		\item[\textbf{\textcolor{CStatement}{结论二（单调凸性）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			幂函数的单调性与凸性完全由 $\alpha$ 与 $0,1$ 的比较决定，分为五种类型：$\alpha>1$ 下凸递增，$\alpha=1$ 直线，$0<\alpha<1$ 上凸递增，$\alpha=0$ 常数（去原点），$\alpha<0$ 递减以坐标轴为渐近线。

		\item[\textbf{\textcolor{CStatement}{结论三（指数比较）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			在区间 $(1,+\infty)$ 上，指数 $\alpha$ 越大，对应的函数值越大。
			\[
				x^{\alpha_1} > x^{\alpha_2} \iff \alpha_1 > \alpha_2
				\qquad
				(x>1).
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（区间互补）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			在区间 $(0,1)$ 上，指数 $\alpha$ 越大，对应的函数值反而越小。
			\[
				x^{\alpha_1} < x^{\alpha_2} \iff \alpha_1 > \alpha_2
				\qquad
				(0<x<1).
			\]
	\end{description}

\end{statementbox}
