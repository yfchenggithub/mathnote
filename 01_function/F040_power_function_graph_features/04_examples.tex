\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知幂函数 $f(x)=x^{\alpha}$ 的图像经过点 $(4,2)$。
	(1) 求 $\alpha$ 的值并判断 $f(x)$ 在 $(0,+\infty)$ 上的单调性与凸性。
	(2) 不计算，比较 $f(0.8)$ 与 $f(0.5)$ 的大小。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求指数）：}] 将点 $(4,2)$ 代入 $y=x^{\alpha}$：$4^{\alpha}=2$。由 $4=2^{2}$，得 $2^{2\alpha}=2^{1}$，所以 $2\alpha=1$，$\alpha=\dfrac12$。
			\par\textbf{理由：} 同底幂相等则指数相等。
		\item[\textbf{第二步（判定性质）：}] $\alpha=\frac12\in(0,1)$，由结论二知 $f(x)$ 在 $(0,+\infty)$ 上严格递增且上凸（增长越来越慢），图像过原点。
			\par\textbf{理由：} $0<\alpha<1$ 属于“上凸严格递增”类型。
		\item[\textbf{第三步（比较函数值）：}] 因为 $f(x)$ 在 $(0,+\infty)$ 上严格递增，且 $0.8>0.5$，所以 $f(0.8)>f(0.5)$。
			\par\textbf{理由：} 单调递增函数，自变量大则函数值大。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	幂函数的指数 $\alpha$ 确定后，其单调性和凸性即可确定；利用单调性可以比较自变量不同时的函数值大小。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	比较下列各组数的大小，并说明理由：
	(1) $1.7^{0.3}$ 与 $1.7^{0.5}$；
	(2) $0.8^{0.2}$ 与 $0.8^{0.3}$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别底数）：}] 对于 (1)，底数 $1.7>1$；对于 (2)，底数 $0.8<1$。
			\par\textbf{理由：} 比较同底幂的大小需要根据底数与 $1$ 的关系选择不同的结论。
		\item[\textbf{第二步（应用结论）：}] (1) 由结论三，当 $x>1$ 时指数越大值越大，故 $1.7^{0.3}<1.7^{0.5}$。
			(2) 由推广结论，当 $0<x<1$ 时指数越大值越小，故 $0.8^{0.2}>0.8^{0.3}$。
			\par\textbf{理由：} 注意区别两个区间上的单调性相反。
		\item[\textbf{第三步（直接写出答案）：}] (1) $<$, (2) $>$。
			\par\textbf{理由：} 略。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	比较 $x^{\alpha_1}$ 与 $x^{\alpha_2}$ 的大小，只看底数 $x$ 与 $1$ 的关系：$x>1$ 时指数大则值大；$0<x<1$ 时指数大则值小。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知幂函数 $y=x^{\alpha}$ 在 $(0,+\infty)$ 上单调递减，且 $a^{\alpha}>b^{\alpha}$（$a,b>0$），求 $a$ 与 $b$ 的大小关系。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（确定符号）：}] 由 $y=x^{\alpha}$ 在 $(0,+\infty)$ 上单调递减，根据结论二可得 $\alpha<0$。
			\par\textbf{理由：} $\alpha<0$ 时函数严格递减；$\alpha>0$ 时严格递增。
		\item[\textbf{第二步（确认单调性）：}] 对幂函数 $h(x)=x^{\alpha}$（$\alpha<0$），在 $(0,+\infty)$ 上单调递减。
			\par\textbf{理由：} 结论二已指明。
		\item[\textbf{第三步（应用单调性）：}] 由 $a^{\alpha}>b^{\alpha}$ 且 $\alpha<0$，利用递减函数性质：函数值越大则自变量越小，故 $a<b$。
			\par\textbf{理由：} 若 $a\geq b$，则 $a^{\alpha}\leq b^{\alpha}$，与题设矛盾。所以 $a<b$。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	幂函数 $\alpha<0$ 时单调递减，此时不等号方向反向：$x_1^{\alpha}>x_2^{\alpha} \Rightarrow x_1<x_2$。
\end{examplebox}
