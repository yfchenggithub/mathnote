\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	幂函数 $y=x^\alpha$（$\alpha\in\mathbb{R}$）在第一象限的图像恒过 $(1,1)$；单调性由 $\alpha$ 正负决定，凸性由 $\alpha$ 与 $1$ 的大小决定；当 $x>1$ 时指数越大值越大，当 $0<x<1$ 时指数越大值越小。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（函数形式）：} 函数必须是幂函数形式 $y=x^\alpha$，$\alpha$ 为任意实数。
		\item \textbf{条件2（定义区间）：} 以上结论仅针对第一象限 $x>0$ 成立，其他象限需单独讨论。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（底数分段）：} 比较 $x^{\alpha_1}$ 与 $x^{\alpha_2}$ 时，务必先看 $x>1$ 还是 $0<x<1$，不能一律认为“指数大值大”。
		\item \textbf{检查项（原点条件）：} 只有 $\alpha>0$ 时图像才过原点，$\alpha\leq0$ 时原点是空心点或不存在。
	\end{itemize}
\end{summarybox}
