\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	帕德逼近是用有理分式近似复杂函数，在$x=0$附近以较小代价获得较高精度；它是局部近似，不是恒等式。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（邻域范围）：} 逼近仅在$x$充分接近0时精度较高；$|x|<1$可作为粗略经验范围，但不是严格数学边界。
		\item \textbf{条件2（光滑性）：} 被逼近函数在$x=0$处需有足够高阶导数，才能通过泰勒展开匹配系数。
		\item \textbf{条件3（定义域）：} 使用$\ln(1+x)$时必须满足$x>-1$。
		\item \textbf{条件4（弧度制）：} 使用$\sin x,\cos x,\tan x$的逼近时，$x$必须是弧度制。
		\item \textbf{条件5（分母限制）：} 使用$\tan x\approx\dfrac{3x}{3-x^2}$时，应保证$3-x^2\ne0$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（局部有效）：} 不可在大$x$时无条件使用，否则误差可能明显增大。
		\item \textbf{检查项（公式对应）：} 使用前确认函数与帕德分式的对应关系，尤其注意
		      \[
			      \cos x\approx\frac{12-5x^2}{12+x^2}.
		      \]
		\item \textbf{符号意识（近似非恒等）：} $\approx$不能当作$=$随意变形；用于不等式证明时，必须另外证明上界、下界或误差符号。
	\end{itemize}
\end{summarybox}