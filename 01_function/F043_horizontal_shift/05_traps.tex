\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（方向混淆）}}] 错误地认为向左平移 $a$ 个单位对应 $y=f(x-a)$，向右平移对应 $y=f(x+a)$。
		\item[\textbf{\textcolor{CTrap}{正确理解（左加右减）：}}] 向左平移 $a$ 个单位对应 $y=f(x+a)$，向右平移 $a$ 个单位对应 $y=f(x-a)$。记忆口诀为“左加右减”。
		\item[\textbf{\textcolor{CTrap}{错因分析（记忆错误）：}}] 可能受到“点坐标平移法则”的影响：点向左移，横坐标减 $a$，但函数解析式的变化恰相反。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（加减对象错误）}}] 平移时直接在函数表达式末加减常数，而不是对自变量整体操作。例如 $f(x)=2x$ 左移 $1$，错误写成 $2x+1$，正确应为 $2(x+1)=2x+2$。
		\item[\textbf{\textcolor{CTrap}{正确理解（整体代换）：}}] 平移变换要求将原函数自变量 $x$ 替换为 $x+a$ 或 $x-a$，再展开化简。
		\item[\textbf{\textcolor{CTrap}{错因分析（局部思维）：}}] 误以为平移是针对函数值进行，而没有理解平移操作是对自变量进行的。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（定义域忽略）}}] 平移后忽略了定义域的变化，仍使用原定义域。例如 $y=\sqrt{x-1}$ 向右移 $1$ 个单位后解析式为 $y=\sqrt{x-2}$，定义域应为 $[2,+\infty)$，若仍当作 $[1,+\infty)$ 则出错。
		\item[\textbf{\textcolor{CTrap}{正确理解（定义域同步）：}}] 平移后图像向左或向右移动，定义域也相应地左右平移相同的距离，需重新确定。
		\item[\textbf{\textcolor{CTrap}{错因分析（惯性思维）：}}] 习惯性认为定义域只由初始解析式决定，未注意自变量代换对定义域的影响。
	\end{description}
\end{trapbox}
