\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{函数图像左右平移时，解析式中的自变量 $x$ 遵循“左加右减”：向左平移 $a$ 个单位将 $x$ 替换为 $x+a$，向右平移替换为 $x-a$。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（定义域允许）：} 平移必须在定义域允许的范围内进行，不能超出定义域。
		\item \textbf{条件2（平移距离）：} 平移距离 $a$ 为正数。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（方向混淆）：} 记住左加右减，不要与点坐标平移混淆。
		\item \textbf{检查项（整体代换）：} 确保是对自变量整体加或减，而不是仅对参数部分操作。
	\end{itemize}
\end{summarybox}
