\begin{proofbox}
	\textbf{\textcolor{CProof}{思路提示}}

	通过点坐标变换：原图像点平移后得到新图像点，将原函数关系代入新坐标，得到新解析式。

	\medskip
	\textbf{\textcolor{CProof}{正式推导}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.55em]
		\item[\textbf{步骤一（左移坐标关系）：}] 设原图像上任意点 $P_0=(x_0,y_0)$ 满足 $y_0=f(x_0)$。左移 $a$ 后对应新点 $P=(X,Y)$，有
			\[
				X=x_0-a,\ Y=y_0.
			\]
			\par \textbf{理由：} 左移时横坐标减少 $a$，纵坐标不变。
		\item[\textbf{步骤二（左移解析式推导）：}] 由 $X=x_0-a$ 解出 $x_0=X+a$，代入 $y_0=f(x_0)$ 得
			\[
				Y=f(X+a).
			\]
			故新函数解析式为 $y=f(x+a)$。
			\par \textbf{理由：} 反代原坐标关系，利用函数定义得到新关系。
		\item[\textbf{步骤三（右移坐标关系）：}] 右移 $a$ 后，新点 $(X,Y)$ 与原图像点 $(x_0,y_0)$ 满足
			\[
				X=x_0+a,\ Y=y_0.
			\]
			\par \textbf{理由：} 右移横坐标增加 $a$，纵坐标不变。
		\item[\textbf{步骤四（右移解析式推导）：}] 由 $X=x_0+a$ 得 $x_0=X-a$，代入 $y_0=f(x_0)$ 得
			\[
				Y=f(X-a).
			\]
			故新函数解析式为 $y=f(x-a)$。
			\par \textbf{理由：} 反代坐标关系得到。
	\end{description}

	\medskip
	\textbf{\textcolor{CProof}{结论回扣}}

	以上推导表明：向左平移 $a$ 个单位对应 “$x$ 变为 $x+a$”，向右平移对应 “$x$ 变为 $x-a$”，即“左加右减”。
\end{proofbox}
