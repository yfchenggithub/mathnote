\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)=x^2$，将它的图像向左平移 $3$ 个单位，求平移后的函数解析式 $g(x)$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别方向）：}] 左移 $3$ 个单位，应使用“左加”规则。
			\par \textbf{理由：} 左加右减，左移对应自变量加。
		\item[\textbf{第二步（应用口诀）：}] 在 $f(x)$ 中将 $x$ 替换为 $x+3$，得
			\[
				\begin{aligned}
					g(x) &= f(x+3) \\
					&= (x+3)^2.
			\end{aligned}
			\]
			\par \textbf{理由：} 直接代入平移法则。
		\item[\textbf{第三步（整理结果）：}] 所以 $g(x)=(x+3)^2$。
			\par \textbf{理由：} 变换完成。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$g(x)=(x+3)^2$}

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)=3x-2$，将其图像向右平移 $a$ 个单位后得到 $g(x)=3x-8$，求 $a$ 的值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（写出平移解析式）：}] 右移 $a$ 个单位得
			\[
				\begin{aligned}
					g(x) &= f(x-a) \\
					&= 3(x-a)-2 \\
					&= 3x-3a-2.
			\end{aligned}
			\]
			\par \textbf{理由：} 右移对应自变量减 $a$。
		\item[\textbf{第二步（建立方程）：}] 与 $g(x)=3x-8$ 比较，得 $3x-3a-2=3x-8$，化简得 $-3a-2=-8$，即 $-3a=-6$。
			\par \textbf{理由：} 函数相等要求对应系数相等，一次项系数相同，常数项相等。
		\item[\textbf{第三步（求解得方向）：}] 解得 $a=2$。因此向右平移了 $2$ 个单位。
			\par \textbf{理由：} 解方程。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$a=2$}

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知二次函数 $f(x)=x^2-2x$，将其图像向右平移 $t$ 个单位后，所得图像的对称轴为直线 $x=3$，求 $t$ 的值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求原函数对称轴）：}] 将 $f(x)$ 配方：$f(x)=(x-1)^2-1$，故对称轴为 $x=1$。
			\par \textbf{理由：} 二次函数对称轴方程。
		\item[\textbf{第二步（平移后解析式）：}] 右移 $t$ 个单位得
			\[
				\begin{aligned}
					g(x) &= f(x-t) \\
					&= (x-t)^2-2(x-t) \\
					&= x^2-2(t+1)x+(t^2+2t),
			\end{aligned}
			\]
			其对称轴为 $x=t+1$。
			\par \textbf{理由：} 平移后解析式通过自变量替换得到，对称轴相应平移。
		\item[\textbf{第三步（列方程求解）：}] 由对称轴为 $x=3$ 得 $t+1=3$，解得 $t=2$。
			\par \textbf{理由：} 解一次方程。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$t=2$}，即向右平移 $2$ 个单位。
\end{examplebox}
