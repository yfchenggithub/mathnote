\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（定义域允许）：}}] 函数 $y=f(x)$ 的定义域允许在 $x$ 方向平移。
		\item[\textbf{\textcolor{CStatement}{条件2（正值平移）：}}] 平移距离 $a>0$。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（左移右移）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			左移时自变量加 $a$，右移时自变量减 $a$。
			\[
				y = f(x+a)
			\]
			左移 $a$ 个单位。
			\[
				y = f(x-a)
			\]
			右移 $a$ 个单位。
		\item[\textbf{\textcolor{CStatement}{推广结论（平移通式）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			平移距离 $h$ 可正可负，新函数为 $y = f(x-h)$。
			\[
				y = f(x-h)
			\]
			其中 $h>0$ 时图像向右平移 $|h|$ 个单位，$h<0$ 时向左平移 $|h|$ 个单位。
	\end{description}
\end{statementbox}
