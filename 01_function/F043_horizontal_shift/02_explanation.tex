\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	左加右减：当自变量加上正数 $a$ 时，图像向左移动 $a$ 个单位；当自变量减 $a$ 时，图像向右移动 $a$ 个单位。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（自变量替换）：}] 平移后的解析式通过在自变量 $x$ 上加或减 $a$ 得到。例如，将 $y=f(x)$ 左移 $a$ 个单位，所有原图像上的点 $(x,y)$ 变为 $(x-a,y)$，即新图像上的横坐标为 $x-a$，对应的原横坐标为 $x$，则新函数解析式为 $y=f(x+a)$。这可以理解为“新函数要‘提前’输入一个值才能得到与原函数相同的输出”。
		\item[\textbf{要点二（记忆口诀）：}] “左加右减”是指：向左平移在自变量上做加法，向右平移做减法。注意操作对象是 $x$，直接对 $x$ 进行加减，而不是对 $y$ 操作。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（平移变换）}}

	平移前后坐标对应关系：左移 $a$ 个单位后，新图像上任意点 $(X,Y)$ 在原图像上的对应点为 $(X+a,Y)$，因此满足 $Y = f(X+a)$。同理，右移 $a$ 个单位后，对应点 $(X-a,Y)$，满足 $Y = f(X-a)$。这解释了为什么左加右减。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（变量代换）}}

	得到平移后的函数时，只需要在原函数表达式中的 $x$ 处替换为 $x+a$ 或 $x-a$（左加右减）。这种代换体现了自变量变换对函数整体表达的影响。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（常考题型）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（解析式求变）：} 给出平移方向和距离，要求写出平移后的函数解析式。直接套用口诀：左加右减。
		\item \textbf{考法二（逆向求距）：} 给出平移前后的函数解析式，求平移方向与距离。比较 $x$ 替换项，确定 $a$ 的正负。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	关键是把图像移动与自变量增减对应：图像向左移，意味着需要提前得到函数值，所以要在自变量上“加”一个数，使 $f$ 的输入更大，从而更早地出现相同输出。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（直接平移）：} 已知原函数和具体平移参数，求新函数解析式。
		\item \textbf{场景二（平移识别）：} 给出两种函数关系，判断平移方向与距离。
	\end{itemize}
\end{explanationbox}
