\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	奇函数的图像关于原点对称；若原点在图像上，则其纵坐标必为 $0$，即 $f(0)=0$。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（定义代入）：}] 由奇函数定义 $f(-x)=-f(x)$，代入 $x=0$ 得 $f(0)=-f(0)$。
		\item[\textbf{要点二（代数移项）：}] 移项 $f(0)+f(0)=0$，即 $2f(0)=0$，故 $f(0)=0$。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（对称中心）}}

	图像关于原点中心对称：若 $(0,f(0))$ 在图像上，其对称点 $(0,-f(0))$ 也必须在图像上，这要求 $f(0)=-f(0)$，从而 $f(0)=0$。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（强制等式）}}

	由定义直接推出方程 $f(0)=-f(0)$，这是代数恒等式，求解即得 $f(0)=0$。它体现了奇函数的“自相消”特性。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（快速判定）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（求函数值）：} 已知 $f(x)$ 为奇函数且 $0$ 在定义域内，直接得 $f(0)=0$，用于求解含参函数在 $0$ 处的取值。
		\item \textbf{考法二（验证奇偶性）：} 若 $f(0)\neq0$ 且 $0$ 在定义域内，则函数一定不是奇函数，可作为排除手段。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	\textbf{将 $x=0$ 代入定义是解决奇函数问题的一种常见技巧；当定义域包含 $0$ 时，奇函数自动输出 $f(0)=0$，不需要额外计算。}

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（含参奇函数）：} 已知 $f(x)$ 是奇函数且 $0$ 在定义域内，求参数值时可列 $f(0)=0$ 方程。
		\item \textbf{场景二（判断奇偶性）：} 计算 $f(0)$ 是否为 $0$（前提 $0\in D$），若不等于 $0$ 则直接判定非奇函数。
	\end{itemize}
\end{explanationbox}
