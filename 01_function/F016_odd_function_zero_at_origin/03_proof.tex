\begin{proofbox}
	\textbf{\textcolor{CProof}{思路提示}}

	由奇函数定义，代入 $x=0$ 后得到一个关于 $f(0)$ 的方程，解方程即得结论。

	\medskip
	\textbf{\textcolor{CProof}{正式推导}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.55em]
		\item[\textbf{步骤一（奇函数定义）：}]
			对任意 $x$ 属于定义域 $D$，有 $f(-x)=-f(x)$。
			\[
				f(-x)=-f(x),\quad\forall x\in D.
			\]
			\par\textbf{理由：} 这是奇函数的基本定义。

		\item[\textbf{步骤二（代入$x=0$）：}]
			由于 $0\in D$，取 $x=0$ 代入上式，得
			\[
				f(0)=-f(0).
			\]
			\par\textbf{理由：} 原定义对任意 $x$ 成立，特别地 $x=0$ 也成立；$0$ 在定义域内是代入的前提。

		\item[\textbf{步骤三（移项合并）：}]
			将等式右边的 $-f(0)$ 移到左边，得 $f(0)+f(0)=0$，即 $2f(0)=0$。
			\[
				2f(0)=0.
			\]
			\par\textbf{理由：} 移项后合并同类项。

		\item[\textbf{步骤四（系数化一）：}]
			方程两边同除以 $2$（$2\neq0$），得 $f(0)=0$。
			\[
				f(0)=0.
			\]
			\par\textbf{理由：} 等式性质。
	\end{description}

	\medskip
	\textbf{\textcolor{CProof}{结论回扣}}

	由以上推导可知，在 $0$ 属于定义域的条件下，奇函数必然满足 $f(0)=0$。这表明 $f(0)=0$ 是奇函数（定义域含 $0$）的必要条件。
\end{proofbox}
