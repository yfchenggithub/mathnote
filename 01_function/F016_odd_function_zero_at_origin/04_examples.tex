\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)=x^3+px^2+qx+r$ 是奇函数，定义域为 $\mathbb{R}$。求常数 $r$ 的值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（确认条件）：}] 定义域为 $\mathbb{R}$，故 $0$ 属于定义域。
			\[
				0\in D_f.
			\]
			\par\textbf{理由：} 定义域是全体实数，自然包含 $0$。

		\item[\textbf{第二步（应用结论）：}] 由奇函数性质，若 $0$ 在定义域内，则必有 $f(0)=0$。
			\[
				f(0)=0.
			\]
			\par\textbf{理由：} 本节课的核心结论。

		\item[\textbf{第三步（代入求解）：}] 将 $x=0$ 代入表达式得 $f(0)=r$，结合 $f(0)=0$ 得 $r=0$。
			\[
				r=0.
			\]
			\par\textbf{理由：} 代入解析式并利用第二步结果。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$r=0$}。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)=ax^5+bx^3+cx+d$ 是定义在 $\mathbb{R}$ 上的奇函数。求参数 $d$ 及系数 $a,b,c$ 应满足的条件。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（定义域确认）：}] 定义域为 $\mathbb{R}$，故 $0$ 在定义域内。
			\[
				0\in D_f.
			\]
			\par\textbf{理由：} 多项式定义域为全体实数。

		\item[\textbf{第二步（结论求参）：}] 由奇函数且 $0\in D_f$ 得 $f(0)=0$，代入得 $d=0$。
			\[
				d=0.
			\]
			\par\textbf{理由：} $f(0)=a\cdot0^5+b\cdot0^3+c\cdot0+d=d$，结合 $f(0)=0$ 得 $d=0$。

		\item[\textbf{第三步（定义验证）：}] 由 $f(-x)=-f(x)$ 对任意 $x$ 成立，可得系数关系。计算得 $f(-x)=-ax^5-bx^3-cx+d$，$-f(x)=-ax^5-bx^3-cx-d$，故需 $d=-d$ 即 $d=0$（已满足），且奇次项系数自动满足，故 $a,b,c$ 可为任意实数。
			\[
				f(-x)=-f(x),\quad\forall x\in\mathbb{R}.
			\]
			\par\textbf{理由：} 奇函数定义要求对所有实数成立。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$d=0$，$a,b,c$ 为任意实数}。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)=x^3+ax^2+bx+c$ 是定义在 $\mathbb{R}$ 上的奇函数，且 $f(1)=2$。求 $f(0)$ 及 $b$ 的值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（定义域确认）：}] 定义域为 $\mathbb{R}$，故 $0\in D_f$。
			\[
				0\in D_f.
			\]
			\par\textbf{理由：} 多项式定义域为全体实数。

		\item[\textbf{第二步（结论求常数）：}] 由奇函数且 $0\in D_f$ 得 $f(0)=0$，代入得 $c=0$。
			\[
				c=0.
			\]
			\par\textbf{理由：} $f(0)=c$，且 $f(0)=0$，故 $c=0$。

		\item[\textbf{第三步（奇函数定系数）：}] 由奇函数定义 $f(-x)=-f(x)$，对比系数知偶次项系数 $a=0$。
			\[
				a=0.
			\]
			\par\textbf{理由：} 若 $a\neq0$，则 $x^2$ 项在 $f(-x)$ 与 $-f(x)$ 中符号矛盾。

		\item[\textbf{第四步（利用已知点）：}] 由 $f(1)=2$ 得 $1^3+ a\cdot1^2+b\cdot1+c=2$。代入 $a=0,c=0$ 得 $1+b=2$。
			\[
				b=1.
			\]
			\par\textbf{理由：} 解一次方程。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f(0)=0$，$b=1$}。
\end{examplebox}
