\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（忽略定义域）}}] 直接使用 $f(0)=0$ 而不先确认 $0$ 是否在定义域内。
		\item[\textbf{\textcolor{CTrap}{正确理解（先验条件）：}}] 必须已知 $0$ 属于定义域，才能推出 $f(0)=0$。
		\item[\textbf{\textcolor{CTrap}{错因分析（条件遗漏）：}}] 奇函数定义域不一定包含 $0$，如 $f(x)=\frac1x$，若盲目使用会得出错误结论。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（充分混淆）}}] 认为 $f(0)=0$ 是函数为奇函数的充分条件，从而仅凭 $f(0)=0$ 判定奇偶性。
		\item[\textbf{\textcolor{CTrap}{正确理解（必要非充）：}}] $f(0)=0$ 是奇函数且 $0$ 在定义域内的必要条件，不是充分条件。函数是否为奇函数还需检验 $f(-x)=-f(x)$ 对定义域内所有 $x$ 成立。
		\item[\textbf{\textcolor{CTrap}{错因分析（逻辑倒置）：}}] 将必要条件和充分条件混淆，忽略奇函数定义的普适性。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（求参失检）}}] 利用 $f(0)=0$ 求参数时，忽略 $0$ 必须在定义域内的前提，导致参数取值扩大。
		\item[\textbf{\textcolor{CTrap}{正确理解（验证定义域）：}}] 列式 $f(0)=0$ 后，必须回头检查当参数取所求值时，$0$ 是否确实属于定义域，否则可能得到非奇函数。
		\item[\textbf{\textcolor{CTrap}{错因分析（忽略前提）：}}] 对 $f(0)=0$ 的适用条件记忆不牢固，忘记验证定义域。
	\end{description}
\end{trapbox}
