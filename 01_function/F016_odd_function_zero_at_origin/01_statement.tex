\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（函数条件）：}}] 函数 $f(x)$ 是奇函数，且 $0$ 属于 $f$ 的定义域。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（函数值性质）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			函数在原点处取值必为零。
			\[
				\boxed{f(0)=0}.
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（逆否判定）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			若 $f(0)\neq0$，则必存在定义域内的点不满足奇函数定义，从而函数不是奇函数。
			\[
				f(0)\neq0 \;\Longrightarrow\; \exists\,x_0\in D_f,\ f(-x_0)\neq -f(x_0).
			\]
	\end{description}

	\medskip
	\textbf{\textcolor{CStatement}{使用提醒：}}
	必须确认 $0$ 在定义域内才能使用 $f(0)=0$；若 $0$ 不在定义域，则结论无意义。
\end{statementbox}
