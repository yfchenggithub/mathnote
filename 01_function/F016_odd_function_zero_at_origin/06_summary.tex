\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{奇函数若定义域含 $0$，则 $f(0)=0$；该结论常用于求参和快速判定。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（函数性质）：} $f(x)$ 必须是奇函数。
		\item \textbf{条件 2（定义域要求）：} $0$ 位于 $f$ 的定义域内。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（忽视前提）：} 使用前务必检查 $0$ 是否在定义域内。
		\item \textbf{检查项（逻辑关系）：} $f(0)=0$ 不能反过来证明函数是奇函数。
	\end{itemize}
\end{summarybox}
