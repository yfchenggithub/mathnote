\begin{trapbox}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（忽略参数范围）}}]
			将结论应用于 $a\leq1$ 或 $\alpha\leq0$ 的情形。
		\item[\textbf{\textcolor{CTrap}{正确理解（限制条件）：}}]
			结论只对 $a>1$ 且 $\alpha>0$ 成立。当 $a=1$ 时 $a^x\equiv1$，无增长；$0<a<1$ 时 $a^x\to0$，不可与无穷大比较；$\alpha=0$ 时 $x^\alpha=1$，不是无穷大。
		\item[\textbf{\textcolor{CTrap}{错因分析（条件遗忘）：}}]
			未严格检查参数范围，凭记忆直接用。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（方向混淆）}}]
			认为 $\frac{a^x}{x^\alpha}\to0$，即幂函数增长快于指数函数。
		\item[\textbf{\textcolor{CTrap}{正确理解（正确方向）：}}]
			$\frac{a^x}{x^\alpha}\to+\infty$，指数函数远快于幂函数；$\frac{x^\alpha}{\log_a x}\to+\infty$，幂函数远快于对数函数。
		\item[\textbf{\textcolor{CTrap}{错因分析（直观误区）：}}]
			当 $\alpha$ 很大时，可能在 $x$ 较小时 $x^\alpha$ 大于 $a^x$，但 $x$ 足够大后指数反超，学生易忽略“足够大”的条件。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（忽略主部）}}]
			在极限运算中，将 $a^x+x^\alpha$ 视为无法简化，直接求极限，或错误地保留低阶项。
		\item[\textbf{\textcolor{CTrap}{正确理解（主部提取）：}}]
			当 $x\to+\infty$ 时，$a^x\gg x^\alpha$，所以 $a^x+x^\alpha\sim a^x$；同理 $x^\alpha+\log_a x\sim x^\alpha$。极限计算中可忽略低阶无穷大。
		\item[\textbf{\textcolor{CTrap}{错因分析（不熟悉阶比较）：}}]
			不了解在无穷大求和时高阶项主导，未掌握等价无穷大替换的思想。
	\end{description}

\end{trapbox}
