\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}
	当$x$充分大时，指数函数$a^x$的增长速度像“火箭”，幂函数$x^\alpha$像“汽车”，对数函数$\log_a x$则像“蜗牛”。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（增长速率差异）：}]
			指数函数导数与自身成正比，保持高速增长；幂函数导数与$x^{\alpha-1}$成正比，增速逐渐放缓；对数函数导数与$1/x$成正比，增速趋于零。
		\item[\textbf{要点二（$\ln a>0$的作用）：}]
			底数$a>1$保证了$\ln a>0$，使得$\ln(a^x)=x\ln a$随$x$线性增长，这是指数“碾压”幂函数的关键。
		\item[\textbf{要点三（对数增长缓慢）：}]
			对数函数$\log_a x$的增长速度最终慢于任何幂函数，即使幂指数$\alpha$非常小。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（图像趋势）}}
	在$x\to+\infty$时，指数函数的图像会超越并远离任何幂函数图像；幂函数图像最终也会超越并远离任何对数函数图像。坐标系中，三条曲线依次“分离”，斜率差异巨大。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（阶的比较）}}
	在进行极限运算时，可以利用这些比较直接判断无穷大阶：当$x\to+\infty$时，$a^x$是比$x^\alpha$“高阶”的无穷大，$x^\alpha$是比$\log_a x$“高阶”的无穷大（对任意$a>1,\alpha>0$）。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（极限与级数）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（比较无穷大阶）：} 在$\frac{\infty}{\infty}$型极限中，根据分子分母的阶直接得出结果，避免繁琐运算。
		\item \textbf{考法二（级数收敛性判断）：} 构造适当的比较函数，利用阶的关系判定级数$\sum u_n$的收敛性（如比较判别法）。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	“指数打所有，幂打对数”：只要底数$a>1$，指数增长终将压倒任何幂函数；只要幂指数$\alpha>0$，幂增长终将压倒任何对数函数（包括其幂次）。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（$\frac{\infty}{\infty}$极限）：} 例如$\lim_{x\to+\infty}\frac{e^x}{x^{10}}$，直接由结论得$+\infty$。
		\item \textbf{场景二（主部提取）：} 在有多个无穷大相加时，保留阶最高的项，简化极限。
	\end{itemize}

\end{explanationbox}
