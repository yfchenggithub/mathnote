\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	当二次函数开口向上（$a>0$）时，图形像碗，对称轴是碗底所在；区间在碗底附近则最小值在碗底，否则在较近碗边。最大值总是取较高的碗边。开口向下时反过来。
	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（对称轴作用）：}] 对称轴是函数取得极值（抛物线顶点）的位置，也是区分函数在区间上是否单调的分界点。
		\item[\textbf{要点二（分类策略）：}] 判断对称轴 $x_0$ 是否属于区间 $[m,n]$：若属于，则最值出现在 $x_0$ 和离 $x_0$ 较远的端点（开口向上最大在远端点，开口向下最小在远端点）；若不属于，则函数在区间上单调，最值在两端点中取。因此，求最值时只需比较 $f(m),f(n)$以及（若 $x_0\in[m,n]$）$f(x_0)$ 的大小，无需考虑单调方向。
	\end{description}
	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（抛物线特性）}}
	二次函数的图象是抛物线，对称轴 $x_0$ 是它的对称线。开口向上时，抛物线向下凸（碗形），顶点是最低点；开口向下时，抛物线向上凸（倒碗），顶点是最高点。区间截取一段抛物线弧，顶点在弧内则最值出现在顶点；顶点在弧外，则弧为单调段，最值在端点。
	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（导数判别）}}
	$f'(x)=2ax+b$，零点 $x_0=-\frac{b}{2a}$ 是极值点。当 $a>0$ 时 $f'(x)$ 在 $x_0$ 左为负、右为正，故 $f(x_0)$ 为极小值；当 $a<0$ 时相反，$f(x_0)$ 为极大值。若 $x_0$ 不在区间 $[m,n]$ 内，则 $f'(x)$ 在区间上不变号，函数单调，最值在端点。
	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（分类讨论）}}
	此结论是含参二次函数在闭区间上最值问题的核心，高考中常以含参函数值域、不等式中参数的取值范围等形式出现。关键在于正确分类对称轴与区间的位置关系，并比较端点函数值。
	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	最值的取法不是死记硬背，而是由对称轴与区间的位置关系自然决定。开口方向仅影响极值本身的属性（最大/小），但不影响比较方法：若 $x_0\in[m,n]$，只需比较 $f(x_0)$、$f(m)$、$f(n)$ 三者大小；若 $x_0\notin[m,n]$，只需比较 $f(m)$、$f(n)$ 的大小。
	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（定轴动区间）：} 已知二次函数，区间一端含参数，求最值或取值范围。
		\item \textbf{场景二（定区间动轴）：} 对称轴含参数（如 $f(x)=x^2-2ax+3$），区间固定，需根据 $a$ 分类讨论最值。
	\end{itemize}
\end{explanationbox}
