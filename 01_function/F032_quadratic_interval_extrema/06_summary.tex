\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{二次函数在闭区间上的最值，由对称轴与区间的位置关系及开口方向决定，通过比较两端点函数值 $f(m),f(n)$ 以及（若对称轴含于区间）顶点函数值 $f(x_0)$ 即可求得。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（函数二次）：} 确认 $f(x)=ax^2+bx+c$ 且 $a\neq0$。
		\item \textbf{条件 2（区间闭）：} 给定区间 $[m,n]$，$m<n$。
		\item \textbf{条件 3（位置分析）：} 明确对称轴 $x_0$ 是否在区间内，并识别开口方向。
	\end{itemize}
	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（忽略轴位）：} 必须判断对称轴是否在区间内，不可默认顶点在区间内。
		\item \textbf{检查项（直接比较）：} 最值计算时，若 $x_0\in[m,n]$，则比较 $f(x_0)$、$f(m)$、$f(n)$ 三者大小；若 $x_0\notin[m,n]$，则比较 $f(m)$、$f(n)$ 的大小。无需强行记忆单调性结论。
	\end{itemize}
\end{summarybox}
