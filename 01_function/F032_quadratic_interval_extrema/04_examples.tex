\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知二次函数 $f(x)=2x^2-8x+3$，求其在区间 $[1,4]$ 上的最大值和最小值。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求对称轴）：}]
			$x_0=-\frac{-8}{2\times2}=2$。
			\par\textbf{理由：} 对称轴公式 $x_0=-\frac{b}{2a}$。
		\item[\textbf{第二步（判位置）：}]
			因为 $1\le x_0\le4$，所以对称轴属于区间 $[1,4]$。
			\par\textbf{理由：} 比较 $x_0$ 与端点的大小即可。
		\item[\textbf{第三步（求最值）：}]
			$f_{\min}=f(2)=2\times4-8\times2+3=-5$；$f_{\max}=\max\{f(1)=2-8+3=-3,\ f(4)=32-32+3=3\}=3$。
			\par\textbf{理由：} 开口向上，对称轴在区间内时最小值在顶点取，最大值取两端较大者。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{最小值 $-5$，最大值 $3$。}

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知 $f(x)=-x^2+4x-1$，区间 $[t,t+2]$（$t\in\mathbb{R}$），求函数的最大值。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求对称轴与开口）：}]
			对称轴 $x_0=-\frac{4}{2\times(-1)}=2$，开口向下（$a=-1<0$）。
			\par\textbf{理由：} 由 $f(x)$ 解析式直接计算。
		\item[\textbf{第二步（分类讨论）：}]
			根据对称轴 $x_0=2$ 与区间 $[t,t+2]$ 的位置关系分三种情况：
			\begin{itemize}
			\item 若 $x_0\in[t,t+2]$（即 $0\le t\le2$），则 $f_{\max}=f(2)=3$；
			\item 若 $t>2$，则 $x_0$ 在区间左侧，函数在区间上递减，$f_{\max}=f(t)=-t^2+4t-1$；
			\item 若 $t+2<2$（即 $t<0$），则 $x_0$ 在区间右侧，函数在区间上递增，$f_{\max}=f(t+2)=-(t+2)^2+4(t+2)-1=-t^2+3$。
		\end{itemize}
			\par\textbf{理由：} 开口向下时，对称轴含于区间则顶点是最大值；否则函数单调，最大值在离对称轴较近的端点（通过单调性判断）。
		\item[\textbf{第三步（整合）：}]
			$\displaystyle f_{\max}=
			\begin{cases}
				3, & 0\le t\le2,\\ -t^2+4t-1, & t>2,\\ -t^2+3, & t<0.
		\end{cases}
			$
			\par\textbf{理由：} 将三种情况用分段函数统一表达。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{最大值分段表达式如上。}

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}
	\textbf{\textcolor{CExample}{题目：}}
	已知 $f(x)=x^2-2x+2$ 在区间 $[a,a+2]$ 上的最小值为 $3$，求实数 $a$ 的值。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求对称轴与开口）：}]
			对称轴 $x_0=1$，开口向上（$a=1>0$）。
			\par\textbf{理由：} 直接计算。
		\item[\textbf{第二步（分类最小值表达式）：}]
			根据对称轴 $x_0=1$ 与区间 $[a,a+2]$ 的位置分三类：
			\begin{itemize}
			\item 若 $1\in[a,a+2]$（即 $-1\le a\le1$），$f_{\min}=f(1)=1$。令 $1=3$ 无解，故不存在；
			\item 若 $a>1$，函数在区间上递增，$f_{\min}=f(a)=a^2-2a+2$。令 $a^2-2a+2=3$，解得 $a=1\pm\sqrt{2}$，结合 $a>1$ 得 $a=1+\sqrt{2}$；
			\item 若 $a+2<1$（即 $a<-1$），函数在区间上递减，$f_{\min}=f(a+2)=a^2+2a+2$。令 $a^2+2a+2=3$，解得 $a=-1\pm\sqrt{2}$，结合 $a<-1$ 得 $a=-1-\sqrt{2}$。
		\end{itemize}
			\par\textbf{理由：} 开口向上，对称轴在区间内则取顶点最小值；在左侧则递增最小值在左端点；在右侧则递减最小值在右端点。解方程后需检验是否满足区间条件。
		\item[\textbf{第三步（综合）：}]
			$a=1+\sqrt{2}$ 或 $a=-1-\sqrt{2}$。
			\par\textbf{理由：} 代入验证，两个解均使最小值为 $3$ 且符合区间设定。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$a=1+\sqrt{2}$ 或 $a=-1-\sqrt{2}$。}
\end{examplebox}
