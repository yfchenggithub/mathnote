\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件一（周期存在）：}}]
			函数 $f(x)$ 定义在 $\mathbb{R}$ 上，且存在正周期 $T>0$，即 $f(x+T)=f(x)$ 对一切 $x\in\mathbb{R}$ 成立。
		\item[\textbf{\textcolor{CStatement}{条件二（对称性存在）：}}]
			函数 $f(x)$ 存在一条对称轴 $x=a$，或者存在一个对称中心 $(b,c)$。
	\end{description}
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（对称轴连锁）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			每过半周期即出现一条对称轴。
			\[
				x = a + \frac{kT}{2},\quad k\in\mathbb{Z}.
			\]
		\item[\textbf{\textcolor{CStatement}{结论二（对称中心连锁）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			每过半周期对称中心重复出现。
			\[
				\left(b + \frac{kT}{2},\,c\right),\quad k\in\mathbb{Z}.
			\]
		\item[\textbf{\textcolor{CStatement}{推广结论（两轴定周期）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			两条不同对称轴间距的两倍就是周期。
			\[
				T = 2\lvert a_1-a_2\rvert.
			\]
	\end{description}

\end{statementbox}
