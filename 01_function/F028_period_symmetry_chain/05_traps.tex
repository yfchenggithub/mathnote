\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（把“一组”误写成“全部”）：}}]
		      已知 $T$ 是函数的一个周期，就直接说全部对称轴为
		      \[
			      x=a+\frac{kT}{2}.
		      \]

		\item[\textbf{\textcolor{CTrap}{正确理解：}}]
		      当 $T$ 只是一个周期时，
		      \[
			      x=a+\frac{kT}{2}
		      \]
		      仅表示由已知条件必然推出的一组对称轴。

		      若进一步知道 $T_0$ 是最小正周期，则全部对称轴恰为
		      \[
			      x=a+\frac{kT_0}{2},
			      \quad k\in\mathbb{Z}.
		      \]

		\item[\textbf{\textcolor{CTrap}{反例：}}]
		      函数
		      \[
			      f(x)=\cos x
		      \]
		      以 $4\pi$ 为一个周期。

		      取对称轴 $x=0$，公式只给出
		      \[
			      x=2k\pi,
		      \]
		      但实际还有
		      \[
			      x=(2k+1)\pi.
		      \]
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（把半周期写成整周期）：}}]
		      误写成
		      \[
			      x=a+kT.
		      \]

		\item[\textbf{\textcolor{CTrap}{正确理解：}}]
		      同类对称轴的位置每隔 $T/2$ 必然再次出现，因此应写成
		      \[
			      x=a+\frac{kT}{2},
			      \quad k\in\mathbb{Z}.
		      \]

		      注意：这并不表示 $T/2$ 本身一定是函数的周期。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（对称中心纵坐标也跟着变化）：}}]
		      误写为
		      \[
			      \left(
			      b+\frac{kT}{2},
			      \,c+\frac{kT}{2}
			      \right).
		      \]

		\item[\textbf{\textcolor{CTrap}{正确理解：}}]
		      这里只改变对称中心的横坐标，纵坐标保持为 $c$，应写为
		      \[
			      \left(
			      b+\frac{kT}{2},
			      \,c
			      \right).
		      \]
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点四（把两条对称轴的间距直接当周期）：}}]
		      误认为周期为
		      \[
			      |a_1-a_2|.
		      \]

		\item[\textbf{\textcolor{CTrap}{正确理解：}}]
		      两次关于平行直线的轴对称合成为一次平移，平移量是轴间距的两倍，所以
		      \[
			      2|a_1-a_2|
		      \]
		      是函数的一个周期。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点五（把推出的周期当作最小正周期）：}}]
		      由两条对称轴得到
		      \[
			      2|a_1-a_2|
		      \]
		      后，直接称其为最小正周期。

		\item[\textbf{\textcolor{CTrap}{正确理解：}}]
		      该数只能保证是函数的一个正周期。

		      它是否为最小正周期，还需要结合函数的其他条件进一步判断。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点六（把对称轴的“全部刻画”照搬给对称中心）：}}]
		      看到最小正周期 $T_0$ 和一个对称中心 $(b,c)$，就无条件断言全部对称中心为
		      \[
			      \left(b+\frac{kT_0}{2},c\right).
		      \]

		\item[\textbf{\textcolor{CTrap}{正确理解：}}]
		      在当前只假设函数定义于 $\mathbb{R}$ 的一般抽象框架下，上式只能保证给出一组对称中心。若要断言它们是全部对称中心，还需要题目提供适当的附加条件。
	\end{description}
\end{trapbox}