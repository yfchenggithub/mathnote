\begin{trapbox}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（周期系数）}}]
			误将全部对称轴写为 $x = a + kT$（直接加整周期）。
		\item[\textbf{\textcolor{CTrap}{正确理解（半周期倍数）：}}]
			对称轴间距应为 $\dfrac{T}{2}$，公式为 $x = a + k\cdot\dfrac{T}{2}$。
		\item[\textbf{\textcolor{CTrap}{错因分析（周期概念混淆）：}}]
			学生误以为周期 $T$ 就是对称轴之间的间隔，实际上每经过半个周期就会出现一条对称轴，因此间隔为 $T/2$。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（纵坐标变化）}}]
			在写出对称中心时，认为纵坐标也会随 $k$ 改变，如写成 $(b+\dfrac{kT}{2},\,c+\dfrac{kT}{2})$。
		\item[\textbf{\textcolor{CTrap}{正确理解（纵坐标恒定）：}}]
			对称中心的纵坐标始终为初始的 $c$，与 $k$ 无关。
		\item[\textbf{\textcolor{CTrap}{错因分析（套用对称轴模式）：}}]
			学生将对称轴中横坐标的规律推广到纵坐标，忽略了中心对称中纵坐标是固定的。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（距离缺倍）}}]
			给出两条对称轴 $x=a_1$ 和 $x=a_2$ 后，直接说周期为 $|a_1-a_2|$。
		\item[\textbf{\textcolor{CTrap}{正确理解（两倍间距）：}}]
			正确周期应为 $2|a_1-a_2|$。
		\item[\textbf{\textcolor{CTrap}{错因分析（推导遗漏一步）：}}]
			从对称轴推导周期时，经过代换后得到 $f(u)=f(u+2(a_2-a_1))$，距离应为两倍，学生容易只看出一个间距。
	\end{description}

\end{trapbox}
