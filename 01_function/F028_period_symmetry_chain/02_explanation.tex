\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}
	同时具有周期和对称轴的函数，每平移半个周期就会重复对称性。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（周期定义）：}]
			$f(x+T)=f(x)$ 对任意 $x$ 成立。
		\item[\textbf{要点二（对称轴定义）：}]
			$f(a+x)=f(a-x)$ 对任意 $x$ 成立。
		\item[\textbf{要点三（连锁本质）：}]
			由前两个等式可推出 $f(a+T/2+x)=f(a+T/2-x)$，即 $a+T/2$ 也是对称轴。反复应用可得全部对称轴。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（镜像与平移）}}
	周期意味着平移 $T$ 后图像重合；对称轴意味着镜像对称。两者结合，将对称轴平移半周期后仍然是对称轴，因为平移一个周期再镜像等价于镜像后再平移。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（等式推导）}}
	由 $f(x)=f(x+T)$ 和 $f(a+x)=f(a-x)$ 可以推出 $f(x)=f(2a-x)$，进一步可推出 $f(x+T)=f(2a-x)$，从而得到 $f(a+T/2+x)=f(a+T/2-x)$。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（快速找对称）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（求对称轴）：} 已知周期和一条对称轴，直接写出所有对称轴。
		\item \textbf{考法二（求周期）：} 已知两条对称轴，直接用公式 $T=2|a_1-a_2|$ 求出周期。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	\textbf{对称轴与周期通过“半周期”紧密相连，抓住这个纽带即可统一处理两类性质。}

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（抽象函数）：} 已知抽象函数的周期与一条对称轴，可推出所有对称轴，进而简化求值。
		\item \textbf{场景二（三角函数）：} 已知三角函数的周期与一条对称轴（通常从最值取得点判断），可确定函数解析式中的相位参数。
	\end{itemize}

\end{explanationbox}
