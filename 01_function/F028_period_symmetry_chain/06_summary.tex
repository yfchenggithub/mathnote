\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{一个已知周期可以把对称轴或对称中心按半周期向左右连锁；两条不同的对称轴则能推出一个等于两倍轴间距的周期。}

	\medskip
	\textbf{\textcolor{CSummary}{核心公式}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item 若 $T$ 是函数的一个周期，$x=a$ 是一条对称轴，则
		      \[
			      x=a+\frac{kT}{2},
			      \quad k\in\mathbb{Z},
		      \]
		      均为对称轴。

		\item 若 $T$ 是函数的一个周期，$(b,c)$ 是一个对称中心，则
		      \[
			      \left(
			      b+\frac{kT}{2},
			      \,c
			      \right),
			      \quad k\in\mathbb{Z},
		      \]
		      均为对称中心。

		\item 若 $x=a_1$、$x=a_2$ 是两条不同的对称轴，则
		      \[
			      P=2|a_1-a_2|
		      \]
		      是函数的一个正周期。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{最重要的限定}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item $T$ 只是“一个周期”时，前两个公式给出的是一组必然存在的对称轴或对称中心，不能轻易写成“全部”。

		\item 若 $T_0$ 是最小正周期，且已知一条对称轴 $x=a$，则全部对称轴恰为
		      \[
			      x=a+\frac{kT_0}{2},
			      \quad k\in\mathbb{Z}.
		      \]

		\item 由两条对称轴推出的
		      \[
			      2|a_1-a_2|
		      \]
		      是一个周期，但不一定是最小正周期。

		\item “对称位置每隔 $T/2$ 出现”不等于“$T/2$ 是函数的周期”。

		\item 对称轴在最小正周期条件下可以完整刻画；对一般抽象函数的全部对称中心，不应在没有附加条件时直接类推。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{记忆口诀}}

	\[
		\boxed{\text{半周期连锁对称，两倍轴距产生周期}}
	\]

	同时牢记：

	\[
		\boxed{\text{一个周期}\ne\text{最小正周期}}
	\]
\end{summarybox}