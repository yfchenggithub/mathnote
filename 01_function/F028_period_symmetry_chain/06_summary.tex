\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	\textbf{周期函数若有一条对称轴或对称中心，则会在每半个周期后重复出现相同的对称轴或对称中心。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（周期已知）：} 函数存在正周期 $T>0$。
		\item \textbf{条件 2（对称性已知）：} 已知一条对称轴或一个对称中心。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（半周期书写）：} 写对称轴时务必用 $T/2$ 而不是 $T$。
		\item \textbf{检查项（间距乘以二）：} 由两条对称轴求周期时，需将距离乘以 $2$。
	\end{itemize}

\end{summarybox}
