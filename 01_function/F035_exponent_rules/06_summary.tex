\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	幂运算简化为指数运算：同底相乘指数加，幂的乘方指数乘，积的乘方指数分配。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（底数正数）：} 当指数为任意实数时，底数必须大于 $0$。
		\item \textbf{条件2（整数放宽）：} 当指数为整数时，底数条件可放松为不等于 $0$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（底数条件）：} 使用实数指数必须确保底数 $>0$，不能对负底数直接套用。
		\item \textbf{检查项（运算区分）：} 区分好指数相加（同底乘法）和指数相乘（幂的乘方），避免混淆。
	\end{itemize}
\end{summarybox}
