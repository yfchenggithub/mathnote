\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（底数不同误合）：}}] 将不同底数的幂 $a^m b^n$ 写成 $(ab)^{m+n}$。
		\item[\textbf{\textcolor{CTrap}{正确理解（分清条件）：}}] 只有底数相同时才能合并指数；底数不同时，除非化为相同底数，否则不能直接合并。
		\item[\textbf{\textcolor{CTrap}{错因分析（混淆法则）：}}] 将积的乘方法则 $(ab)^m = a^m b^m$ 逆用或误以为所有幂运算都可以合并指数。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（乘方嵌套错序）：}}] 把 $(a^m)^n$ 算成 $a^{m+n}$ 或 $a^{m^n}$。
		\item[\textbf{\textcolor{CTrap}{正确理解（指数相乘）：}}] 幂的乘方是指数相乘，即 $(a^m)^n = a^{mn}$。
		\item[\textbf{\textcolor{CTrap}{错因分析（记忆混淆）：}}] 与同底数幂乘法 $a^m a^n = a^{m+n}$ 形式相似，但运算法则不同。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（忽略正底条件）：}}] 在指数为实数时，直接对负底数或零应用运算法则。
		\item[\textbf{\textcolor{CTrap}{正确理解（底数正限制）：}}] 指数幂运算法则对于实数指数要求底数 $>0$；只有指数为整数时才可放宽至 $a,b\neq0$。
		\item[\textbf{\textcolor{CTrap}{错因分析（条件遗忘）：}}] 学生往往只记公式，忽略底数为正的前提，导致在实数指数时出错。
	\end{description}
\end{trapbox}
