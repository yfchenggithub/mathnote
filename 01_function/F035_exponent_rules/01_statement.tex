\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（底数为正）：}}] 底数 $a,b>0$，指数 $m,n\in\mathbb{R}$。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（同底相乘）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			同底数幂相乘，指数相加。
			\[
				a^m \cdot a^n = a^{m+n}.
			\]

		\item[\textbf{\textcolor{CStatement}{结论二（幂的乘方）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			幂的乘方，指数相乘。
			\[
				(a^m)^n = a^{mn}.
			\]

		\item[\textbf{\textcolor{CStatement}{结论三（积的乘方）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			积的乘方等于乘方的积。
			\[
				(ab)^m = a^m b^m.
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（整数放宽）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			当指数为整数时，底数可以取非零实数。
			\[
				\begin{aligned}
					a^m a^n &= a^{m+n},\\
					(a^m)^n &= a^{mn},\\
					(ab)^m &= a^m b^m,
			\end{aligned}
				\qquad (a,b\neq0,\; m,n\in\mathbb{Z}).
			\]
	\end{description}
\end{statementbox}
