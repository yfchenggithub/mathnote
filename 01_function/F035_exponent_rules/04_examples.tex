\begin{examplebox}
	\textbf{\textcolor{CExample}{例1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	计算 $a^3 \cdot a^5$（$a>0$）。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别底数）：}] 观察到两个幂的底数都是 $a$，满足同底数条件。
			\par\textbf{理由：} 只有底数相同时才能直接合并指数。
		\item[\textbf{第二步（套用法则）：}] $a^3 \cdot a^5 = a^{3+5}$。
			\par\textbf{理由：} 同底数幂相乘，指数相加。
		\item[\textbf{第三步（计算指数）：}] $a^{3+5} = a^8$。
			\par\textbf{理由：} 指数加法运算。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$a^3\cdot a^5 = a^8$。}
\end{examplebox}
\begin{examplebox}
	\textbf{\textcolor{CExample}{例2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	计算 $(2x^3)^2 \cdot x^4$（$x>0$）。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（积的乘方）：}] $(2x^3)^2 = 2^2 \cdot (x^3)^2 = 4x^6$。
			\par\textbf{理由：} 积的乘方法则 $(ab)^m = a^m b^m$ 和幂的乘方法则 $(a^m)^n = a^{mn}$。
		\item[\textbf{第二步（同底合并）：}] $4x^6 \cdot x^4 = 4x^{6+4}$。
			\par\textbf{理由：} 同底数幂乘法法则 $a^m a^n = a^{m+n}$。
		\item[\textbf{第三步（化简结果）：}] $4x^{10}$。
			\par\textbf{理由：} 指数相加。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$(2x^3)^2 \cdot x^4 = 4x^{10}$。}
\end{examplebox}
\begin{examplebox}
	\textbf{\textcolor{CExample}{例3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	解方程 $2^{x+1} = 8$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（化同底）：}] $8 = 2^3$。
			\par\textbf{理由：} $2^3 = 8$。
		\item[\textbf{第二步（方程变形）：}] $2^{x+1} = 2^3$。
			\par\textbf{理由：} 将右边化为底数为2的幂。
		\item[\textbf{第三步（指数相等）：}] 由 $2^{x+1}=2^3$ 且 $2>0$，得 $x+1=3$，解得 $x=2$。
			\par\textbf{理由：} 若 $a>0$ 且 $a\neq1$，则 $a^p = a^q \Rightarrow p=q$。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{方程的解为 $x=2$。}
\end{examplebox}
