\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	指数运算法则的核心是：将幂之间的乘除转化为指数的加减乘除。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（底数不变）：}] 各法则中底数本身保持不变，运算只影响指数。
		\item[\textbf{要点二（指数平移）：}] 同底数幂相乘，指数相加；幂的乘方，指数相乘；积的乘方，指数分配到各个因子。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（计数因子）}}

	重复乘法时，一个因子连续乘的次数就是指数；三个法则分别对应：合并相同因子的次数、嵌套计数次数、因数分解后次数分配。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（运算统一）}}

	指数运算法则把乘法运算转化为加法运算，把幂运算转化为乘法运算，使得代数式化简更加统一和简洁。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（简化计算）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（合并指数）：} 将多个幂乘积合并为一个幂，快速求值。
		\item \textbf{考法二（解指数方程）：} 利用相同底数将方程转化为指数相等，从而求解。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	指数运算法则的本质是“指数作为计数器”的加减乘除，只要底数一致或可以分解，运算可降维。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（化简幂式）：} 遇到多个幂乘除时，先看底数是否相同，若相同则合并指数。
		\item \textbf{场景二（分配指数）：} 对于积的乘方或乘方嵌套，将指数分配到每个因数或层数。
	\end{itemize}
\end{explanationbox}
