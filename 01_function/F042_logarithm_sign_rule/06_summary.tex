\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{对数 $\log_a b$ 的正负等价于 $(a-1)$ 与 $(b-1)$ 同号为正、异号为负；即 $\log_a b>0 \iff (a-1)(b-1)>0$，$\log_a b<0 \iff (a-1)(b-1)<0$。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*,itemsep=0.5em,topsep=0.4em]
		\item \textbf{条件1（定义域限制）：} $a>0$, $a\neq1$, $b>0$。
		\item \textbf{条件2（等价叙述）：} 也可表述为：若 $a>1,\ b>1$ 或 $0<a<1,\ 0<b<1$，则 $\log_a b>0$；若 $a>1,\ 0<b<1$ 或 $0<a<1,\ b>1$，则 $\log_a b<0$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*,itemsep=0.5em,topsep=0.4em]
		\item \textbf{易错点（忽略定义域）：} 务必先检验 $b>0$，否则判断无意义。
		\item \textbf{检查项（边界情况）：} 当 $b=1$ 时 $\log_a b=0$，不适用正负法则；当 $a=1$ 时对数无定义。
	\end{itemize}
\end{summarybox}
