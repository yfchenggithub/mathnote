\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（周期误判）}}] 错误认为$f(x+a)=f(x-a)$等价于周期为$a$（直接由加$a$和减$a$的差为$2a$，误以为差$a$是周期）。
		\item[\textbf{\textcolor{CTrap}{正确理解（周期为2a）：}}] 正确的周期是$2a$，因为代换后得到$f(x+2a)=f(x)$。
		\item[\textbf{\textcolor{CTrap}{错因分析（逻辑错误）：}}] 误以为$f(x+a)=f(x-a)$意味着$x$增加$a$等于减少$a$，得出周期是$a$，忽略了必须构造$f(x+T)=f(x)$的形式。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（定义域忽略）}}] 错误认为在部分点满足$f(x+a)=f(x-a)$即可推出周期性。
		\item[\textbf{\textcolor{CTrap}{正确理解（恒成立要求）：}}] 结论要求等式对定义域内所有$x$成立，否则不能保证整体周期性。
		\item[\textbf{\textcolor{CTrap}{错因分析（忽略条件）：}}] 周期性定义要求每个$x$都满足$f(x+T)=f(x)$，若只对部分$x$成立，不能推出周期函数。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（对称混淆）}}] 错误将$f(x+a)=f(x-a)$当作对称轴条件（$f(x+a)=f(a-x)$才是对称）。
		\item[\textbf{\textcolor{CTrap}{正确理解（周期特征）：}}] 该等式是周期条件，表示$f(x+2a)=f(x)$，是对称轴条件的常见混淆。
		\item[\textbf{\textcolor{CTrap}{错因分析（概念不清）：}}] 对称轴条件形如$f(a+x)=f(a-x)$，类似形式易误用。
	\end{description}
\end{trapbox}
