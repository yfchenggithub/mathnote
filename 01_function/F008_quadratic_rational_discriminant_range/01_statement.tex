\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline,leftmargin=2.8em,labelsep=0.5em,itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（分母无实根）：}}]
			分母二次式 $dx^2+ex+f$ 满足 $d\neq0$ 且判别式 $\Delta_d=e^2-4df<0$，从而分母恒正或恒负，函数自然定义域为 $\mathbb{R}$。
	\end{description}

	\medskip
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline,leftmargin=2.8em,labelsep=0.5em,itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（判别式法）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			将函数化为关于 $x$ 的二次方程，利用方程有实根的判别式条件解出 $y$ 的取值范围。
			\[
				(a-dy)x^2+(b-ey)x+(c-fy)=0.
			\]
			当 $a-dy\neq0$ 时，由 $\Delta(y)=(b-ey)^2-4(a-dy)(c-fy)\ge0$ 解出 $y$ 的范围；当 $a-dy=0$ 时，代入检验是否存在实数 $x$ 满足原式，若存在则将该 $y$ 并入值域。
	\end{description}

	\medskip
	\textbf{\textcolor{CStatement}{适用范围：}}
	仅适用于分子、分母均为二次的多项式分式函数，且分母二次式必须非退化（$d\neq0$）；若分子次数高于或低于二次，不能直接使用本方法。
\end{statementbox}
