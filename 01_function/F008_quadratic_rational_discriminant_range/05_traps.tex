\begin{trapbox}

	\begin{description}[style=nextline,leftmargin=2.8em,labelsep=0.5em,itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（忽略系数退化）}}]
			只解判别式不等式，不检验二次项系数 $a-dy=0$ 的情况。
		\item[\textbf{\textcolor{CTrap}{正确理解（单独验证）：}}]
			必须将 $a-dy=0$ 对应的 $y$ 代入原函数或原方程，检查是否存在定义域内的 $x$，若存在则须添加，若不存在则剔除。
		\item[\textbf{\textcolor{CTrap}{错因分析（分类遗漏）：}}]
			误以为判别式法自动覆盖所有情况，缺少分类讨论意识。
	\end{description}

	\medskip
	\begin{description}[style=nextline,leftmargin=2.8em,labelsep=0.5em,itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（定义域误判）}}]
			使用判别式法前未验证分母的判别式 $\Delta_d$，默认定义域为 $\mathbb{R}$，导致当 $\Delta_d\ge0$ 时得出错误值域。
		\item[\textbf{\textcolor{CTrap}{正确理解（先判分母）：}}]
			先计算 $\Delta_d$，若 $\Delta_d<0$ 可直接用判别式法；若 $\Delta_d\ge0$，需排除分母零点，再结合判别式法求值域。
		\item[\textbf{\textcolor{CTrap}{错因分析（条件遗忘）：}}]
			忽略定义域对值域的限制，只考虑方程有解，未考虑解必须使分母非零。
	\end{description}

	\medskip
	\begin{description}[style=nextline,leftmargin=2.8em,labelsep=0.5em,itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（展开符号错误）}}]
			展开判别式 $\Delta(y)=(b-ey)^2-4(a-dy)(c-fy)$ 时，多项式乘法符号处理出错，导致二次不等式错误。
		\item[\textbf{\textcolor{CTrap}{正确理解（细心展开）：}}]
			展开时注意 $-(a-dy)(c-fy)=-(ac-afy-cdy+dfy^2)=-ac+afy+cdy-dfy^2$，再合并同类项。
		\item[\textbf{\textcolor{CTrap}{错因分析（运算不过关）：}}]
			基础运算功底不扎实，可代入具体值检验。
	\end{description}

\end{trapbox}
