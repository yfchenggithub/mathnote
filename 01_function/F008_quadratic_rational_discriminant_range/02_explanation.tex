\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}
	函数值域就是使关于 $x$ 的二次方程有实数解的 $y$ 的集合。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline,leftmargin=2.8em,labelsep=0.5em,itemsep=0.45em]
		\item[\textbf{要点一（构造方程）：}]
			将 $y$ 视为参数，通过去分母整理成关于 $x$ 的二次方程 $(a-dy)x^2+(b-ey)x+(c-fy)=0$。
		\item[\textbf{要点二（判别式条件）：}]
			若 $x$ 有实数解，则二次方程判别式 $\Delta(y)\ge0$。
		\item[\textbf{要点三（退化处理）：}]
			当 $a-dy=0$ 时，方程退化为一次或常数，需单独判断是否存在 $x$。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（水平线交点）}}
	函数 $y=f(x)$ 的图像与水平直线 $y=y_0$ 相交的充要条件是存在 $x$ 使 $f(x)=y_0$。判别式法正是将这一几何条件转化为关于 $x$ 的二次方程有实根。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（方程有解条件）}}
	通过引入参数 $y$，将分式函数的值域问题转化为含参二次方程有实根的条件，进而用判别式非负得到关于 $y$ 的不等式，解得值域。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（中档高频）}}
	判别式法是求二次分式函数值域的经典方法，常出现在高考选择题填空题中，也常与不等式、函数单调性结合考查。

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	关键在于将分式看成一个方程，而不是先研究单调性。一旦构造出二次方程，值域问题就变成了二次不等式求解。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*,itemsep=0.5em,topsep=0.4em]
		\item\textbf{场景一（分母无实根）：}当分母 $\Delta_d<0$ 时可直接使用，无需考虑定义域漏洞。\item\textbf{场景二（分子分母都二次）：}仅当分子分母均为二次且 $d\neq0$ 时适用；若次数不匹配，需先变形。
	\end{itemize}

\end{explanationbox}
