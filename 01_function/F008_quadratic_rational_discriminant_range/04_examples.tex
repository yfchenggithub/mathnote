\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\medskip
	\textbf{\textcolor{CExample}{题目：}}
	求函数 $y = \dfrac{x^2+1}{x^2+2x+2}$ 的值域。

	\medskip
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline,leftmargin=2.8em,labelsep=0.5em,itemsep=0.45em]
		\item[\textbf{第一步（构造方程）：}]
			去分母并整理得 $(1-y)x^2-2yx+(1-2y)=0$。
			\par\textbf{理由：} 化分式为整式，建立关于 $x$ 的二次方程。
		\item[\textbf{第二步（判别式非负）：}]
			当 $1-y\neq0$ 时，$\Delta(y)=(-2y)^2-4(1-y)(1-2y)\ge0$。
			\par\textbf{理由：} 二次方程有实根的条件。
		\item[\textbf{第三步（解不等式）：}]
			展开得 $\Delta(y)=4(-y^2+3y-1)\ge0$，即 $y^2-3y+1\le0$，解得 $\dfrac{3-\sqrt5}{2}\le y\le\dfrac{3+\sqrt5}{2}$。
			\par\textbf{理由：} 解二次不等式得到 $y$ 范围。
		\item[\textbf{第四步（检验退化）：}]
			当 $y=1$ 时，方程化为 $-2x-1=0$，得 $x=-\dfrac12$，故 $y=1$ 可取，且 $1$ 已在上述区间内。
			\par\textbf{理由：} 二次系数为零时需单独验证。
	\end{description}

	\medskip
	\textbf{\textcolor{CExample}{关键结论：}}
	值域为 $\left[\dfrac{3-\sqrt5}{2},\dfrac{3+\sqrt5}{2}\right]$。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\medskip
	\textbf{\textcolor{CExample}{题目：}}
	求函数 $y = \dfrac{2x^2+3}{x^2+1}$ 的值域。

	\medskip
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline,leftmargin=2.8em,labelsep=0.5em,itemsep=0.45em]
		\item[\textbf{第一步（构造方程）：}]
			去分母得 $(2-y)x^2+(3-y)=0$。
			\par\textbf{理由：} 化分式为整式。
		\item[\textbf{第二步（判别式非负）：}]
			当 $2-y\neq0$ 时，$\Delta(y)=0-4(2-y)(3-y)=-4(2-y)(3-y)\ge0$。
			\par\textbf{理由：} 方程有实根条件。
		\item[\textbf{第三步（解不等式）：}]
			由 $(2-y)(3-y)\le0$ 得 $2\le y\le3$。
			\par\textbf{理由：} 解二次不等式。
		\item[\textbf{第四步（检验退化）：}]
			当 $y=2$ 时，代入原方程得 $x^2$ 项消失，剩下 $0\cdot x^2+(3-2)=0$，即 $1=0$ 矛盾，故 $y=2$ 不可取；$y=3$ 对应 $x=0$ 可取。
			\par\textbf{理由：} 二次系数为零时需验证。
	\end{description}

	\medskip
	\textbf{\textcolor{CExample}{关键结论：}}
	值域为 $(2,3]$。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\medskip
	\textbf{\textcolor{CExample}{题目：}}
	求函数 $y = \dfrac{3x^2+2x+1}{x^2+x+1}$ 的值域。

	\medskip
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline,leftmargin=2.8em,labelsep=0.5em,itemsep=0.45em]
		\item[\textbf{第一步（构造方程）：}]
			去分母得 $(3-y)x^2+(2-y)x+(1-y)=0$。
			\par\textbf{理由：} 化分式为整式。
		\item[\textbf{第二步（判别式非负）：}]
			当 $3-y\neq0$ 时，$\Delta(y)=(2-y)^2-4(3-y)(1-y)\ge0$。
			\par\textbf{理由：} 二次方程有实根条件。
		\item[\textbf{第三步（解不等式）：}]
			展开 $\Delta(y)=-3y^2+12y-8\ge0$，即 $3y^2-12y+8\le0$，解得 $2-\dfrac{2\sqrt3}{3}\le y\le2+\dfrac{2\sqrt3}{3}$。
			\par\textbf{理由：} 解二次不等式得到 $y$ 的范围。
		\item[\textbf{第四步（检验退化）：}]
			当 $y=3$ 时，方程化为 $-x-2=0$，得 $x=-2$，在定义域内，故 $y=3$ 可取。计算可知 $3\in\left[2-\frac{2\sqrt3}{3},2+\frac{2\sqrt3}{3}\right]$，该值已在上述区间内，值域无需额外补充。
			\par\textbf{理由：} 二次系数为零时单独检验，确认与判别式结果一致。
	\end{description}

	\medskip
	\textbf{\textcolor{CExample}{关键结论：}}
	值域为 $\left[2-\frac{2\sqrt3}{3},2+\frac{2\sqrt3}{3}\right]$。
\end{examplebox}
