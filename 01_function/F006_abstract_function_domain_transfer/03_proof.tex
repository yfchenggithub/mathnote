\begin{proofbox}
	\textbf{\textcolor{CProof}{思路提示}}

	从函数定义域的基本定义出发，通过复合关系分析自变量$x$必须满足的条件，推导两个方向规则。

	\medskip
	\textbf{\textcolor{CProof}{正式推导}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.55em]
		\item[\textbf{步骤一（正向规则证明）：}]
			设 $f(x)$ 的定义域为 $D$。对于 $f[g(x)]$，其自变量是 $x$，复合有意义当且仅当 $g(x)\in D$。因此 $x$ 的取值范围 $C =\{x\mid g(x)\in D\}$ 即为 $f[g(x)]$ 的定义域。
			\par\textbf{理由：}
			定义域是自变量的取值集合，必须使函数有意义；复合函数要求内层函数值属于外层函数定义域。

		\item[\textbf{步骤二（逆向规则必要性）：}]
			设 $f[g(x)]$ 的定义域为 $A$。令 $t = g(x)$，则对任意 $x\in A$，$f(t)$ 有意义，故 $t$ 必须在 $f$ 的定义域内。因此 $f$ 的定义域 $D_f$ 必须包含集合 $B =\{g(x)\mid x\in A\}$。
			\par\textbf{理由：}
			若 $t$ 不在 $D_f$ 中，则 $f(t)$ 无定义，与复合有意义矛盾。

		\item[\textbf{步骤三（逆向规则结论）：}]
			在高中范围内，通常 $g(x)$ 是连续函数，$B$ 是一个区间，且题设隐含 $f$ 的定义域就是使 $f$ 有意义的 $t$ 的集合，而已知条件仅能确定 $B$ 中的 $t$ 有意义，故 $D_f = B$，即 $f(x)$ 的定义域为 $\{g(x)\mid x\in A\}$。
			\par\textbf{理由：}
			若 $D_f$ 包含不在 $B$ 中的值，则这些值对应的 $x$ 不包含在 $A$ 中，无法通过已知条件验证，故实际解题中取 $D_f = B$。
	\end{description}

	\medskip
	\textbf{\textcolor{CProof}{结论回扣}}

	\textbf{以上两条规则完整描述了抽象函数定义域的互推关系，掌握后可直接套用求解。}
\end{proofbox}
