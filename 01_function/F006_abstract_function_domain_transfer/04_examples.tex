\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知 $f(x)$ 的定义域为 $[0,3]$，求 $f(2x+1)$ 的定义域。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别类型）：}] 已知 $f$ 定义域，求 $f[g(x)]$ 定义域，使用正向规则。
			\par\textbf{理由：}
			条件符合“若 $f(x)$ 定义域为 $D$，则 $f[g(x)]$ 定义域由 $g(x)\in D$ 决定”。

		\item[\textbf{第二步（列不等式）：}] 由 $f(2x+1)$ 有意义，需 $0\le 2x+1\le 3$。
			\par\textbf{理由：}
			因为 $f$ 的定义域是 $[0,3]$，所以括号内 $2x+1$ 必须在此区间内。

		\item[\textbf{第三步（解不等式）：}] 解 $0\le 2x+1\le 3$ 得 $-\frac12\le x\le 1$。
			\par\textbf{理由：}
			同时减 $1$ 得 $-1\le 2x\le 2$，再除以 $2$（正数）得 $-\frac12\le x\le 1$。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$f(2x+1)$ 的定义域为 $\left[-\frac12,1\right]$。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知 $f(2x-1)$ 的定义域为 $[1,4]$，求 $f(x)$ 的定义域。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别类型）：}] 已知 $f[g(x)]$ 定义域，求 $f$ 定义域，使用逆向规则。
			\par\textbf{理由：}
			条件符合“若 $f[g(x)]$ 定义域为 $A$，则 $f(x)$ 定义域是 $g(x)$ 在 $A$ 上的值域”。

		\item[\textbf{第二步（设中间变量）：}] 令 $t=2x-1$，由 $x\in[1,4]$ 得 $t\in[1,7]$。
			\par\textbf{理由：}
			当 $x=1$ 时 $t=1$，当 $x=4$ 时 $t=7$，且 $t$ 随 $x$ 连续变化。

		\item[\textbf{第三步（得定义域）：}] 因此，$f(x)$ 的定义域为 $[1,7]$。
			\par\textbf{理由：}
			由逆向规则，$f$ 定义域等于 $t$ 的取值范围。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$f(x)$ 的定义域为 $[1,7]$。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知 $f(x+1)$ 的定义域为 $[0,2]$，求 $f(2x-1)$ 的定义域。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求原函数定义域）：}] 由 $f(x+1)$ 定义域 $[0,2]$，逆向规则得 $f(x)$ 定义域。令 $u=x+1$，$x\in[0,2]$ 得 $u\in[1,3]$，故 $f(x)$ 定义域为 $[1,3]$。
			\par\textbf{理由：}
			先求原函数定义域，这是中间跳板。

		\item[\textbf{第二步（求复合定义域）：}] 再由 $f(x)$ 定义域 $[1,3]$，正向规则得 $f(2x-1)$ 定义域。解 $1\le 2x-1\le 3$。
			\par\textbf{理由：}
			要求 $2x-1$ 落在 $f$ 定义域内。

		\item[\textbf{第三步（解不等式）：}] 解 $1\le 2x-1\le 3$ 得 $1\le x\le 2$。
			\par\textbf{理由：}
			加 $1$ 得 $2\le 2x\le 4$，除以 $2$ 得 $1\le x\le 2$。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$f(2x-1)$ 的定义域为 $[1,2]$。
\end{examplebox}
