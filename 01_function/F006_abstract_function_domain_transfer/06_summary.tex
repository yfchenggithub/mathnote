\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{抽象函数定义域互推规则：已知原定义域求复合定义域用不等式，已知复合定义域求原定义域用值域。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（统一对应法则）：} 函数$f$是同一个对应法则。
		\item \textbf{条件2（明确自变量）：} 始终区分自变量$x$与中间变量$g(x)$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（混淆变量）：} 不要把定义域误认为括号内表达式范围。
		\item \textbf{检查项（端点条件）：} 解不等式时注意原定义域端点是否包含。
	\end{itemize}
\end{summarybox}
