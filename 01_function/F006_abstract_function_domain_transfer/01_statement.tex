\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（对应法则）：}}] 函数$f$的对应法则保持不变。
		\item[\textbf{\textcolor{CStatement}{条件2（自变量身份）：}}] 定义域始终是自变量$x$的取值范围。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（已知$f$求复合）：}}]
			若 $f(x)$ 的定义域为 $D$，则 $f[g(x)]$ 的定义域为
			\[
				\{x \mid g(x)\in D\}.
			\]

		\item[\textbf{\textcolor{CStatement}{结论二（已知复合求$f$）：}}]
			若 $f[g(x)]$ 的定义域为 $A$，则 $f(x)$ 的定义域为
			\[
				\{g(x)\mid x\in A\}.
			\]
	\end{description}
\end{statementbox}
