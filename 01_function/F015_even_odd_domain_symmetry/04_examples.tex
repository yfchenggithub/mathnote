\begin{examplebox}
	\textbf{\textcolor{CExample}{例1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	判断函数$f(x)=x^{3}-2x$的奇偶性。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（定义域检查）：}] $f(x)$的定义域为$\mathbb{R}$，关于原点对称。
			\par\textbf{理由：} $\mathbb{R}$是对称区间，任何实数的相反数仍是实数。
		\item[\textbf{第二步（计算$f(-x)$）：}] $f(-x)=(-x)^{3}-2(-x)$，即$-x^{3}+2x$。
			\par\textbf{理由：} 直接代入负数进行计算。
		\item[\textbf{第三步（比较判断）：}] 比较$f(-x)$与$f(x)$，发现$f(-x)=-(x^{3}-2x)$，即$-f(x)$。
			\par\textbf{理由：} 满足奇函数定义$f(-x)=-f(x)$。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f(x)$为奇函数。}

	\medskip
	\textbf{\textcolor{CExample}{例2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	判断函数$f(x)=\dfrac{x^{2}+1}{x}$的奇偶性。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（定义域检查）：}] 分母不为零，定义域为$\{x\mid x\neq0\}$，关于原点对称。
			\par\textbf{理由：} 若$x\neq0$，则$-x\neq0$，故定义域对称。
		\item[\textbf{第二步（计算$f(-x)$）：}] $f(-x)=\dfrac{(-x)^{2}+1}{-x}$，化简得$\dfrac{x^{2}+1}{-x}$，也就是$-\dfrac{x^{2}+1}{x}$。
			\par\textbf{理由：} 代入并化简。
		\item[\textbf{第三步（比较判断）：}] 显然$f(-x)=-f(x)$。
			\par\textbf{理由：} $f(x)=\dfrac{x^{2}+1}{x}$，所以$f(-x)=-f(x)$成立。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f(x)$为奇函数。}

	\medskip
	\textbf{\textcolor{CExample}{例3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知$f(x)=x^{5}+ax^{3}+bx-7$，且$f(3)=10$，求$f(-3)$的值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（构造奇函数）：}] 令$g(x)=f(x)+7$，即$g(x)=x^{5}+ax^{3}+bx$，则$g(x)$的定义域为$\mathbb{R}$。计算$g(-x)=(-x)^{5}+a(-x)^{3}+b(-x)$，化简得$g(-x)=-(x^{5}+ax^{3}+bx)$，也就是$-g(x)$，故$g(x)$是奇函数。
			\par\textbf{理由：} $g(x)$只含奇次项，满足奇函数定义。
		\item[\textbf{第二步（应用奇函数性质）：}] 由$g(x)$是奇函数得$g(-3)=-g(3)$，即$f(-3)+7=-(f(3)+7)$。
			\par\textbf{理由：} 奇函数定义$g(-x)=-g(x)$。
		\item[\textbf{第三步（代入求值）：}] 已知$f(3)=10$，代入得$f(-3)+7=-(10+7)$，即$f(-3)+7=-17$，所以$f(-3)=-24$。
			\par\textbf{理由：} 解关于$f(-3)$的方程。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f(-3)=-24$。}
\end{examplebox}
