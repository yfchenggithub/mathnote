\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（忽略定义域检查）}}] 判断奇偶性时，直接计算$f(-x)$，而不先检查定义域是否关于原点对称。
		\item[\textbf{\textcolor{CTrap}{正确理解（定义域必须对称）}}] 必须先验证定义域对称。若不对称，函数必为非奇非偶，无需继续。
		\item[\textbf{\textcolor{CTrap}{错因分析（对称是前提）}}] 奇偶性的定义要求对任意$x\in D$都有$-x\in D$，否则$f(-x)$无意义。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（忽视关系验证）}}] 认为只要定义域对称，函数就一定是奇函数或偶函数。
		\item[\textbf{\textcolor{CTrap}{正确理解（还需关系式成立）}}] 定义域对称只是必要条件，必须验证$f(-x)=\pm f(x)$对所有$x$成立。
		\item[\textbf{\textcolor{CTrap}{错因分析（定义不完整）}}] 只记住对称条件，忘记关系式，导致误判。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（误用特殊值）}}] 用$f(0)=0$直接判定函数为奇函数。
		\item[\textbf{\textcolor{CTrap}{正确理解（特殊值不充分）}}] $f(0)=0$仅是奇函数在$0$点有定义时的性质，不能作为奇函数的判据。
		\item[\textbf{\textcolor{CTrap}{错因分析（范围缩小）}}] 将个别点的值当成整体性质，忽略了定义的普遍性。
	\end{description}
\end{trapbox}
