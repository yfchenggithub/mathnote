\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（定义域对称）：}}] 函数$f(x)$的定义域$D$关于原点对称，即对任意$x\in D$，都有$-x\in D$。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（偶函数判定）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			对于定义域内任意$x$，函数值在自变量取相反数时保持不变。
			\[
				f(-x)=f(x).
			\]

		\item[\textbf{\textcolor{CStatement}{结论二（奇函数判定）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			对于定义域内任意$x$，函数值在自变量取相反数时变为相反数。
			\[
				f(-x)=-f(x).
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（等价变形）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			通过移项得到判定式的等价形式，便于代数运算。

			偶函数：
			\[
				f(x)-f(-x)=0.
			\]

			奇函数：
			\[
				f(x)+f(-x)=0.
			\]
	\end{description}
\end{statementbox}
