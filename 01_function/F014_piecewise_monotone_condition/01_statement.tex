\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（分段定义）：}}]
			函数 $f(x)$ 写为 $f(x)=
			\begin{cases}
				g(x), & x\le x_0, \\ h(x), & x>x_0
		\end{cases}
			$，其中 $g$ 在 $(-\infty,x_0]$ 有定义，$h$ 在 $(x_0,+\infty)$ 有定义。
		\item[\textbf{\textcolor{CStatement}{条件 2（单调方向一致）：}}]
			各分段在各自子区间上单调方向相同（同增或同减）。
		\item[\textbf{\textcolor{CStatement}{条件 3（衔接大小关系）：}}]
			在分界点 $x_0$ 处，左段终点与右段起点极限满足：递增时 $g(x_0)\le \lim\limits_{x\to x_0^+}h(x)$，递减时方向反向。
	\end{description}
	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（整体单调递增）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			两段各自递增且在分界点处左值不超过右端极限。
			\[
				f(x)\text{在}\mathbb{R}\text{上单调递增}\iff
				\begin{cases}
					g(x)\text{在}(-\infty,x_0]\text{上单调递增},\
					\[
						2pt] h(x)\text{在}(x_0,+\infty)\text{上单调递增},\
						\[
							2pt] g(x_0)\le\displaystyle\lim_{x\to x_0^+}h(x).
					\end{cases}
					\]
				\item[\textbf{\textcolor{CStatement}{推广结论(单调递减充要):}}]
					\textbf{\textcolor{CStatement}{直观描述:}}
					两段各自递减且在分界点处左值不小于右端极限.
					\[
						f(x)\text{在}\mathbb{R}\text{上单调递减}\iff
						\begin{cases}
							g(x)\text{在}(-\infty,x_0]\text{上单调递减},\
							\[
								2pt] h(x)\text{在}(x_0,+\infty)\text{上单调递减},\
								\[
									2pt] g(x_0)\ge\displaystyle\lim_{x\to x_0^+}h(x).
							\end{cases}
							\]
					\end{description}

				\end{statementbox}
