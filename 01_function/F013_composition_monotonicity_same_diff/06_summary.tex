\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	复合函数单调性遵循“同增异减”：内外单调相同则增，相异则减。

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（内层单调）：} 内层函数在区间上单调。
		\item \textbf{条件2（值域包含）：} 内层值域在外层定义域内。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（忽略定义域）：} 使用前必须检查值域是否包含。
		\item \textbf{检查项（单调符号）：} 内外单调性乘积为正增为负减。
	\end{itemize}
\end{summarybox}
