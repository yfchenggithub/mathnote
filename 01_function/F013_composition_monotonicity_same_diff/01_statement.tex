\begin{statementbox}

	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件1（内层单调）：}}] 内层函数 $g$ 在区间 $I$ 上单调。
		\item[\textbf{\textcolor{CStatement}{条件2（外层单调）：}}] 外层函数 $f$ 在区间 $U$ 上单调。
		\item[\textbf{\textcolor{CStatement}{条件3（值域包含）：}}] 内层函数的值域 $g(I)\subseteq U$，即复合函数 $f\circ g$ 在 $I$ 上有定义。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（同增异减规则）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			内外单调性一致则复合递增，内外单调性相异则复合递减。
			\[
				\begin{cases}
					f\nearrow,\; g\nearrow \implies f\circ g \nearrow,\\
					f\searrow,\; g\searrow \implies f\circ g \nearrow,\\
					f\nearrow,\; g\searrow \implies f\circ g \searrow,\\
					f\searrow,\; g\nearrow \implies f\circ g \searrow.
			\end{cases}
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（多层复合）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			对于多层复合，每层依次应用同增异减，总单调性由各层单调性符号的乘积决定：乘积为正则递增，乘积为负则递减。
			\[
				s_{h} = \prod_{i=1}^n s_i,\quad s_i\in\{+1,-1\}.
			\]
	\end{description}

\end{statementbox}
