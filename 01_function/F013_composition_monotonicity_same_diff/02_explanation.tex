\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	复合函数单调性取决于内外层单调方向的“异同”：内外同向（都增或都减）则复合递增；内外反向则复合递减。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（内外层关系）：}] 复合函数 $y=f(g(x))$ 的单调性由两层“叠加”而成：先经 $g$ 变换，再经 $f$ 变换。若两层变换的方向一致，整体就增强；不一致，整体就减弱。
		\item[\textbf{要点二（符号化表示）：}] 定义单调方向符号：递增记 $+1$，递减记 $-1$，则复合后的方向符号为两层符号之积：$s_{f\circ g}=s_f\cdot s_g$。“同增异减”即 $s_f\cdot s_g=+1$ 时增，$s_f\cdot s_g=-1$ 时减。
		\item[\textbf{要点三（条件约束）：}] 使用此结论前必须确认 $g(I)\subseteq U$（内层值域在外层定义域内），否则 $f\circ g$ 在 $I$ 上无定义或间断，不能直接套用。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（映射方向）}}

	复合映射相当于“两次折返”：若内层映射使自变量增大时因变量增大（或减小），外层映射又在自己的定义域内继续增大（或减小），则复合结果相当于方向和 $+1$ 与 $-1$ 的乘法。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（不等式传递）}}

	设 $x_1<x_2$，由 $g$ 单调得 $g(x_1)$ 与 $g(x_2)$ 的大小关系；再由 $f$ 单调得到 $f(g(x_1))$ 与 $f(g(x_2))$ 的大小关系。通过“同向不等号传递”可严格证明四种情形。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（高频应用）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（求单调区间）：} 给出复合函数表达式，分解内外层，分别求单调区间，再根据“同增异减”合成。
		\item \textbf{考法二（含参单调性）：} 已知复合函数在某个区间上的单调性，反推参数取值范围，常需结合内外层单调性分类讨论。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	口诀“同增异减”的实质是两层单调性符号的乘法运算。记不清时，用简单函数举例验证（如 $f(x)=x^2$ 在 $(-\infty,0)$ 递减，$g(x)=2x+1$ 递增，复合后 $(2x+1)^2$ 在 $(-\infty,-1/2)$ 递减，验证“内增外减得减”）。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（直接判断）：} 给出内外层单调性，直接套用规则得出复合单调性，常见于选择题。
		\item \textbf{场景二（参数分析）：} 含参数时，需先讨论内外层单调性，再根据复合结果列不等式。
	\end{itemize}
\end{explanationbox}
