\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（忽略值域）}}] 使用规则前未检查 $g(I)\subseteq U$，导致复合函数在部分区间无定义，单调性结论错误。
		\item[\textbf{\textcolor{CTrap}{正确理解（条件验证）：}}] 必须先确认内层函数的值域在外层定义域内，才能对整体使用同增异减。
		\item[\textbf{\textcolor{CTrap}{错因分析（知识漏洞）：}}] 学生只记口诀，不清楚规则成立的前提条件，忽略定义域约束。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（区间错位）}}] 求单调区间时，将内层函数的单调区间直接当作复合函数的单调区间，未转换到自变量 $x$ 的区间。
		\item[\textbf{\textcolor{CTrap}{正确理解（区间对应）：}}] 复合后，需在定义域内根据内外层单调性，通过 $g(x)$ 映射确定 $x$ 的区间：该区间既要使内层单调，又要使内层值域落在外层的同一单调区间内，而不是简单地将内层单调区间与定义域取交集。
		\item[\textbf{\textcolor{CTrap}{错因分析（映射混淆）：}}] 没有明确内外层变量关系，将 $t$ 的单调区间误写为 $x$ 的区间。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（符号误用）}}] 误认为“同增异减”是指复合函数与内层函数单调性相同或相反，忽略外层作用。
		\item[\textbf{\textcolor{CTrap}{正确理解（两层叠加）：}}] “同增异减”比较的是内层与外层的单调性，而不是与复合结果直接比。
		\item[\textbf{\textcolor{CTrap}{错因分析（概念模糊）：}}] 对复合结构理解不深，难以区分内层、外层、复合三者的单调关系。
	\end{description}
\end{trapbox}
