\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	判断函数 $h(x)=(\frac13)^{x^2-2x}$ 的单调区间。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（函数分解）：}] 令 $t=g(x)=x^2-2x$，则 $h(x)=f(t)=(\frac13)^t$。
			\par\textbf{理由：} 分解为基本初等函数。

		\item[\textbf{第二步（内层分析）：}] $g(x)=x^2-2x=(x-1)^2-1$，在 $(-\infty,1)$ 递减，在 $(1,+\infty)$ 递增。
			\par\textbf{理由：} 二次函数开口向上，对称轴 $x=1$。

		\item[\textbf{第三步（复合判断）：}] 外层 $f(t)=(\frac13)^t$ 在 $(-\infty,+\infty)$ 上递减（底数 $\frac13<1$）。由同增异减：$(-\infty,1)$ 内层减、外层减，故复合增；$(1,+\infty)$ 内层增、外层减，故复合减。
			\par\textbf{理由：} 同增异减规则。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	单调递增区间 $(-\infty,1)$，单调递减区间 $(1,+\infty)$。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	判断函数 $f(x)=\ln(x^2-2x-3)$ 的单调区间。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（求定义域）：}] 由 $x^2-2x-3>0$，解得 $x<-1$ 或 $x>3$。
			\par\textbf{理由：} 对数真数必须大于零。

		\item[\textbf{第二步（分解函数）：}] 令 $t=g(x)=x^2-2x-3$，则 $f(x)=\ln t$，外层 $\ln t$ 在 $(0,+\infty)$ 递增。
			\par\textbf{理由：} 对数函数单调性。

		\item[\textbf{第三步（内层单调性）：}] $g(x)=(x-1)^2-4$，在 $(-\infty,1)$ 递减，在 $(1,+\infty)$ 递增。结合定义域，在 $(-\infty,-1)$ 上 $g$ 递减，在 $(3,+\infty)$ 上 $g$ 递增。
			\par\textbf{理由：} 二次函数性质。

		\item[\textbf{第四步（复合判断）：}] 外层递增，故 $f(x)$ 在 $(-\infty,-1)$ 上（$g$ 减外层增，异减）递减；在 $(3,+\infty)$ 上（$g$ 增外层增，同增）递增。
			\par\textbf{理由：} 同增异减。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	单调递减区间 $(-\infty,-1)$，单调递增区间 $(3,+\infty)$。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	已知函数 $f(x)=\ln(x^2-ax+1)$ 在 $(2,+\infty)$ 上单调递增，求实数 $a$ 的取值范围。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（分解转化）：}] 令 $t=h(x)=x^2-ax+1$，则 $f(x)=\ln t$。外层 $\ln t$ 在 $(0,+\infty)$ 递增。由同增异减，要使 $f(x)$ 在 $(2,+\infty)$ 递增，需内层 $h(x)$ 在 $(2,+\infty)$ 也递增且 $h(x)>0$。
			\par\textbf{理由：} 外层增，复合增需内层增（同增）；且定义域要求真数正。

		\item[\textbf{第二步（递增条件）：}] $h(x)=x^2-ax+1$ 开口向上，对称轴 $x=\frac{a}{2}$。在 $(2,+\infty)$ 递增等价于 $\frac{a}{2}\le 2$，即 $a\le 4$。
			\par\textbf{理由：} 二次函数在对称轴右侧递增。

		\item[\textbf{第三步（恒正条件）：}] $h(x)>0$ 在 $(2,+\infty)$ 恒成立。由于 $h$ 在 $(2,+\infty)$ 递增，只需 $h(2)=5-2a\ge 0$，得 $a\le\frac{5}{2}$。此时对称轴自动满足 $\frac{a}{2}<\frac{5}{4}<2$，故 $a\le4$ 自动成立。
			\par\textbf{理由：} 递增函数最小值在左端点，保证真数在 $(2,+\infty)$ 上恒正。

		\item[\textbf{第四步（综合）：}] 取 $a\le\frac{5}{2}$ 即可。
			\par\textbf{理由：} 满足递增和正数条件。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$a\le\frac{5}{2}$。
\end{examplebox}
