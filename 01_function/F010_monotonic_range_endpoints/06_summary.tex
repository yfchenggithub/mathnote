\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	\textbf{单调函数在闭区间上的值域等于其端点函数值所构成的闭区间。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（闭区间单调）：} 函数在闭区间 $[a,b]$ 上有定义且单调。
		\item \textbf{条件 2（端点有定义）：} 区间端点 $a,b$ 处函数值可计算。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（区间开闭）：} 注意区分闭区间与开区间，开区间时值域不一定是闭的。
		\item \textbf{检查项（单调性确认）：} 必须确认函数在该区间上确实单调，不能默认。
	\end{itemize}

\end{summarybox}
