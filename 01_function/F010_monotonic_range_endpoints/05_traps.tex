\begin{trapbox}

	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（混淆区间开闭）}}]
			误以为在开区间 $(a,b)$ 上单调时，值域仍为 $[f(a),f(b)]$。
		\item[\textbf{\textcolor{CTrap}{正确理解（端点可达性）：}}]
			开区间时 $f(a),f(b)$ 不一定取得，值域应为开区间或半开区间，需用极限或补充定义。
		\item[\textbf{\textcolor{CTrap}{错因分析（忽视定义域）：}}]
			死记公式忽略定义域是否包含端点。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（缺单调性判定）}}]
			未判断函数是否单调，直接套用端点求值域。
		\item[\textbf{\textcolor{CTrap}{正确理解（确认单调性）：}}]
			必须先证明或确认函数在区间上单调，才能使用该结论。
		\item[\textbf{\textcolor{CTrap}{错因分析（惯性思维）：}}]
			见到闭区间和端点值就想当然地认为是最值。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（颠倒递减顺序）}}]
			单调递减时错误写成 $[f(a),f(b)]$。
		\item[\textbf{\textcolor{CTrap}{正确理解（递减方向）：}}]
			递减时值域为 $[f(b),f(a)]$，即左端点大于右端点。
		\item[\textbf{\textcolor{CTrap}{错因分析（记忆混淆）：}}]
			只记住了递增情况，对递减未正确转换。
	\end{description}

\end{trapbox}
