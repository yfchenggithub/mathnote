\begin{proofbox}
	\textbf{\textcolor{CProof}{思路提示}}
	利用单调定义得函数值范围，再结合端点值可达，即得值域为两端点构成的闭区间。

	\medskip
	\textbf{\textcolor{CProof}{正式推导}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.55em]
		\item[\textbf{步骤一（递增得范围）：}]
			设 $f$ 在 $[a,b]$ 递增。对任意 $x\in[a,b]$，由 $a\le x\le b$ 得
			\[
				f(a) \le f(x) \le f(b).
			\]
			\par\textbf{理由：} 增函数定义：自变量增大函数值增大。
		\item[\textbf{步骤二（递减得范围）：}]
			若 $f$ 递减，则
			\[
				f(b) \le f(x) \le f(a).
			\]
			\par\textbf{理由：} 减函数定义。
		\item[\textbf{步骤三（端点可达）：}]
			因为 $a,b\in[a,b]$，所以 $f(a),f(b)$ 都是函数值，属于值域。
			\par\textbf{理由：} 定义域包含端点。
		\item[\textbf{步骤四（值域确定）：}]
			对于递增情况，由步骤一知值域 $\subseteq [f(a),f(b)]$，步骤三知 $f(a),f(b)$ 在值域中，因此值域就是 $[f(a),f(b)]$。递减时同理可得值域 $[f(b),f(a)]$。
			\par\textbf{理由：} 值域既被该区间包含又包含其端点，故相等。
	\end{description}

	\medskip
	\textbf{\textcolor{CProof}{结论回扣}}
	因此，命题成立。
\end{proofbox}
