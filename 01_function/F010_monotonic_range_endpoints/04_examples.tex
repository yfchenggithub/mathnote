\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}
	\textbf{\textcolor{CExample}{题目：}}
	求函数 $y=2x+1$ 在 $[0,3]$ 上的值域。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（判单调性）：}]
			$y=2x+1$ 中 $k=2>0$，故在 $\mathbb{R}$ 上单调递增，特别在 $[0,3]$ 上递增。
			\par\textbf{理由：} 一次函数斜率正，单调递增。
		\item[\textbf{第二步（套用公式）：}]
			递增时值域为 $[f(0),f(3)]$。计算 $f(0)=1,\ f(3)=7$。
			\par\textbf{理由：} 直接代入端点。
		\item[\textbf{第三步（写结论）：}]
			因此值域为 $[1,7]$。
			\par\textbf{理由：} 中间步骤已得。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$[1,7]$}。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}
	\textbf{\textcolor{CExample}{题目：}}
	求函数 $f(x)=x^2-2x+3$ 在 $[1,4]$ 上的值域。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（判对称轴）：}]
			配方得 $f(x)=(x-1)^2+2$，对称轴 $x=1$，区间 $[1,4]$ 在对称轴右侧，所以 $f(x)$ 在 $[1,4]$ 上单调递增。
			\par\textbf{理由：} 二次函数在对称轴一侧单调。
		\item[\textbf{第二步（套用公式）：}]
			递增时值域为 $[f(1),f(4)]$。计算 $f(1)=2,\ f(4)=11$。
			\par\textbf{理由：} 直接代入端点。
		\item[\textbf{第三步（写结论）：}]
			因此值域为 $[2,11]$。
			\par\textbf{理由：} 中间步骤已得。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$[2,11]$}。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}
	\textbf{\textcolor{CExample}{题目：}}
	求函数 $f(x)=|x-1|$ 在 $[-1,2]$ 上的值域。
	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（去绝对值分段）：}]
			当 $x\le1$ 时 $f(x)=-x+1$，当 $x\ge1$ 时 $f(x)=x-1$。所以 $f$ 在 $[-1,1]$ 递减，在 $[1,2]$ 递增。
			\par\textbf{理由：} 绝对值函数分段后为一次函数，由斜率得单调性。
		\item[\textbf{第二步（分段求值域）：}]
			递减部分值域 $[f(1),f(-1)]=[0,2]$；递增部分值域 $[f(1),f(2)]=[0,1]$。
			\par\textbf{理由：} 每段用单调性法求值域。
		\item[\textbf{第三步（并集得整体）：}]
			取并集得 $[0,2]$。
			\par\textbf{理由：} 值域为各段值域之并。
	\end{description}
	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$[0,2]$}。
\end{examplebox}
