\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}
	单调函数在闭区间上的值域，由两端点函数值直接确定，因为函数值随自变量单向变化，既无回头也无跳跃。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（端点即最值）：}]
			单调递增时最小值在左端点，最大值在右端点；单调递减时反之。
		\item[\textbf{要点二（闭区间保证取值）：}]
			闭区间保证端点函数值有定义，因此值域的上下界都能取到。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（区间连续映照）}}
	单调函数的图像从左到右只上升或下降，在 $x$ 从 $a$ 到 $b$ 的过程中，$y$ 从 $f(a)$ 到 $f(b)$ 单向移动，覆盖整个竖条区间。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（不等式传递）}}
	由定义，对 $a \le x_1<x_2\le b$，有 $f(x_1)\le f(x_2)$（递增）或 $f(x_1)\ge f(x_2)$（递减），从而任意 $x$ 的函数值均介于 $f(a)$ 与 $f(b)$ 之间。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（简化运算）}}
	利用单调性求值域只需计算两个端点值，避免遍历所有点，常用于一次函数、指数函数、对数函数等单调函数。
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（直接求值域）：}给出单调区间，直接代入端点得值域。
		\item \textbf{考法二（含参单调分析）：}先判断参数对单调性的影响，再求最值。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}
	值域问题转化为端点问题，核心在于“单调”保证函数值不重复、不回头，一次对应。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（已知单调求值域）：}明确告知函数在区间上单调，如一次函数、反比例函数在指定区间。
		\item \textbf{场景二（先证单调再求值）：}未知单调性，用定义或导数证明单调后，再用结论求值域。
	\end{itemize}

\end{explanationbox}
