\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	函数 $f(x)$ 定义在 $\mathbb{R}$ 上，满足 $f(1+x)=f(1-x)$ 且 $f(3+x)=f(3-x)$。求 $f(x)$ 的一个周期。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别对称类型）：}] 由 $f(1+x)=f(1-x)$ 知 $f(x)$ 关于 $x=1$ 轴对称；由 $f(3+x)=f(3-x)$ 知 $f(x)$ 关于 $x=3$ 轴对称。故属于双轴对称。
			\par\textbf{理由：} 轴对称的代数条件 $f(a+x)=f(a-x)$ 对应直线 $x=a$ 对称。
		\item[\textbf{第二步（套用周期公式）：}] 根据结论，双轴对称时周期 $T=2|a-b|$，此处 $a=1,\ b=3$，所以 $T=2|3-1|=4$。
			\par\textbf{理由：} 轴轴对称情形的周期公式 $T=2|a-b|$ 直接适用。
		\item[\textbf{第三步（写出周期）：}] 故 $f(x)$ 的一个周期为 $4$。
			\par\textbf{理由：} 周期定义：存在 $T>0$ 使得 $f(x+T)=f(x)$ 对任意 $x$ 成立。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$T=4$。

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	函数 $f(x)$ 定义在 $\mathbb{R}$ 上，满足 $f(2+x)=f(2-x)$ 且 $f(6+x)+f(6-x)=8$。求 $f(x)$ 的一个周期。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别对称类型）：}] $f(2+x)=f(2-x)$ 表示关于 $x=2$ 轴对称；$f(6+x)+f(6-x)=8$ 表示关于 $(6,4)$ 中心对称（因为 $f(6+x)+f(6-x)=2c$ 得 $c=4$）。故为轴点对称。
			\par\textbf{理由：} 中心对称代数形式 $f(b+x)+f(b-x)=2c$ 对应点 $(b,c)$。
		\item[\textbf{第二步（套用周期公式）：}] 轴点对称周期 $T=4|a-b|$，$a=2,\ b=6$，所以 $T=4|6-2|=16$。
			\par\textbf{理由：} 结论（3）公式适用。
		\item[\textbf{第三步（写出周期）：}] 因此 $f(x)$ 的一个周期为 $16$。
			\par\textbf{理由：} 周期函数存在周期 $16$。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	$T=16$。

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	函数 $f(x)$ 定义在 $\mathbb{R}$ 上，满足 $f(1+x)+f(1-x)=6$ 且 $f(3+x)+f(3-x)=6$，且 $f(0)=0$。求 $f(x)$ 的周期，并计算 $f(2025)$。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（识别对称类型）：}] 由 $f(1+x)+f(1-x)=6$ 知 $f(x)$ 关于 $(1,3)$ 中心对称；由 $f(3+x)+f(3-x)=6$ 知关于 $(3,3)$ 中心对称。故为点点对称。
			\par\textbf{理由：} 中心对称的定义。
		\item[\textbf{第二步（求周期）：}] 点点对称周期 $T=2|a-b|$，$a=1,\ b=3$，所以 $T=2|3-1|=4$。
			\par\textbf{理由：} 结论（2）公式。
		\item[\textbf{第三步（利用周期求值）：}] $2025=4\times506+1$，所以 $f(2025)=f(1)$。由中心对称条件，取 $x=0$ 得 $f(1)+f(1)=6$，故 $f(1)=3$。因此 $f(2025)=3$。
			\par\textbf{理由：} 周期函数性质及对称条件赋值。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	周期 $T=4$，$f(2025)=3$。
\end{examplebox}
