\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{若函数同时具有两个不同的对称性（轴对称或中心对称），则它一定是周期函数，周期为 $2|a-b|$ 或 $4|a-b|$，取决于对称类型是否相同。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（定义域要求）：} 函数 $f(x)$ 定义在 $\mathbb{R}$ 上，否则周期只能在定义域内讨论。
		\item \textbf{条件 2（对称等式成立）：} 给定两个对称等式（如 $f(a+x)=f(a-x)$ 等）必须对所有 $x$ 成立，且 $a\neq b$。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（周期倍率）：} 轴点对称周期为 $4|a-b|$，不是 $2|a-b|$。
		\item \textbf{检查项（中心纵坐标）：} 中心对称的两个常数 $c$ 必须相同，否则公式不成立。
	\end{itemize}
\end{summarybox}
