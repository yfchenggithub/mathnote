\begin{statementbox}

	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（定义域前提）：}}] 函数 $f(x)$ 定义在 $\mathbb{R}$ 上，常数 $a,b,c\in\mathbb{R}$ 且 $a\neq b$。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（轴轴对称）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			同时关于两条竖直直线对称，周期为间距的两倍。
			\[
				f(a+x)=f(a-x),\ f(b+x)=f(b-x)\ \Longrightarrow\ T=2|a-b|.
			\]

		\item[\textbf{\textcolor{CStatement}{结论二（点点对称）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			同时关于两个水平点对称，周期为间距的两倍。
			\[
				f(a+x)+f(a-x)=2c,\ f(b+x)+f(b-x)=2c\ \Longrightarrow\ T=2|a-b|.
			\]

		\item[\textbf{\textcolor{CStatement}{结论三（轴点对称）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			关于一条直线和一个点对称，周期为间距的四倍。
			\[
				f(a+x)=f(a-x),\ f(b+x)+f(b-x)=2c\ \Longrightarrow\ T=4|a-b|.
			\]
	\end{description}

	\medskip
	\textbf{\textcolor{CStatement}{常见变形（分类记忆）}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{同型与异型：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			两个对称类型相同得 $2d$，不同得 $4d$（$d=|a-b|$）。\\
			\qquad 同类型（轴轴或点点）：$T=2d$，\quad 不同类型（轴点）：$T=4d$。
	\end{description}

\end{statementbox}
