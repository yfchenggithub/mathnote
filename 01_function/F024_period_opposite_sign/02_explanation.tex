\begin{explanationbox}
	\textbf{\textcolor{CExplanation}{一句话直觉}}

	对于任意 $x$，函数值先取相反数，再取一次相反数就回到原值，相隔 $2a$ 的函数值必相等。

	\medskip
	\textbf{\textcolor{CExplanation}{核心拆解}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{要点一（周期定义）：}] 周期 $T$ 满足 $f(x+T)=f(x)$ 对一切 $x$ 成立。
		\item[\textbf{要点二（复合变换）：}] 已知 $f(x+a)=-f(x)$，反复使用可得
			\[
				\begin{aligned}
					f(x+2a) &= f((x+a)+a) \\
					&= -f(x+a) \\
					&= f(x).
			\end{aligned}
			\]
		\item[\textbf{要点三（符号规律）：}] 每增加 $a$ 函数值变号，故 $f(x+2ka)=f(x)$，$f(x+(2k+1)a)=-f(x)$。
	\end{description}

	\medskip
	\textbf{\textcolor{CExplanation}{几何本质（半周期反转）}}

	将图像沿 $x$ 轴方向平移 $a$ 个单位后再作关于 $x$ 轴的翻折，与原图像重合；连续两次操作回到原位，故周期为 $2a$。同时，点 $(\frac{a}{2},0)$ 为对称中心。

	\medskip
	\textbf{\textcolor{CExplanation}{代数意义（递归迭代）}}

	条件 $f(x+a)=-f(x)$ 相当于一个递推关系，将 $x$ 替换为 $x+a$ 得
	\[
		\begin{aligned}
			f(x+2a) &= -f(x+a) \\
			&= -(-f(x)) \\
			&= f(x).
		\end{aligned}
	\]
	直接导出周期。

	\medskip
	\textbf{\textcolor{CExplanation}{考点价值（基础周期模型）}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{考法一（周期判定）：} 给函数方程直接判断周期 $T=2a$。
		\item \textbf{考法二（求值应用）：} 利用周期性将自变量转换到已知区间求函数值。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CExplanation}{顿悟点}}

	\textbf{“两次相反得原值”：} 只要看到 $f(x+a)=-f(x)$，立即反应周期 $2a$，不必每次推导。

	\medskip
	\textbf{\textcolor{CExplanation}{使用场景}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{场景一（抽象函数）：} 抽象函数给出类似等式时直接套用周期结论。
		\item \textbf{场景二（三角函数）：} 如 $f(x)=\sin x$，$a=\pi$ 时满足
			\[
				\begin{aligned}
					f(x+\pi) &= -\sin x \\
					&= -f(x),
			\end{aligned}
			\]
			周期 $2\pi$。
	\end{itemize}
\end{explanationbox}
