\begin{examplebox}
	\textbf{\textcolor{CExample}{例 1（基础）}}

	\textbf{\textcolor{CExample}{题目：}}
	定义在 $\mathbb{R}$ 上的函数 $f(x)$ 满足 $f(x+2)=-f(x)$，且 $f(1)=2$，求 $f(5)$ 的值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（确定周期）：}] 由条件 $f(x+2)=-f(x)$ 可知 $a=2$，根据结论周期 $T=2a$，即 $T=4$。
			\par\textbf{理由：} 直接应用二级结论。

		\item[\textbf{第二步（转换自变量）：}] 因为 $5-4=1$，利用周期性得 $f(5)=f(1)$。
			\par\textbf{理由：} 周期函数 $f(x+T)=f(x)$。

		\item[\textbf{第三步（代入求值）：}] 已知 $f(1)=2$，故 $f(5)=2$。
			\par\textbf{理由：} 代入已知数值。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{周期为 $4$，$f(5)=2$。}

	\medskip
	\textbf{\textcolor{CExample}{例 2（稍变形）}}

	\textbf{\textcolor{CExample}{题目：}}
	定义在 $\mathbb{R}$ 上的函数 $f(x)$ 满足 $f(x+3)=-f(x)$，当 $x\in[0,3]$ 时 $f(x)=x$，求 $f(10)$ 的值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（确定周期）：}] 由 $f(x+3)=-f(x)$ 得 $a=3$，周期 $T=6$。
			\par\textbf{理由：} 套用二级结论。

		\item[\textbf{第二步（利用周期化简）：}] 因为 $10-6=4$，所以 $f(10)=f(4)$。
			\par\textbf{理由：} 周期性质。

		\item[\textbf{第三步（用条件转化到已知区间）：}] 将 $4$ 写成 $1+3$，由条件得 $f(4)=f(1+3)$，再由 $f(x+3)=-f(x)$ 令 $x=1$ 得 $f(1+3)=-f(1)$，故 $f(4)=-f(1)$。
			\par\textbf{理由：} 直接使用 $f(x+3)=-f(x)$ 令 $x=1$。

		\item[\textbf{第四步（代入已知解析式）：}] 当 $x\in[0,3]$ 时 $f(x)=x$，所以 $f(1)=1$，于是 $f(4)=-1$，即 $f(10)=-1$。
			\par\textbf{理由：} 代入已知分段函数。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{$f(10)=-1$。}

	\medskip
	\textbf{\textcolor{CExample}{例 3（综合应用）}}

	\textbf{\textcolor{CExample}{题目：}}
	定义在 $\mathbb{R}$ 上的函数 $f(x)$ 满足 $f(x+1)=-f(x)$，且 $f(0)=1$，求 $f(2025)+f(2026)$ 的值。

	\textbf{\textcolor{CExample}{解题步骤：}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{第一步（确定周期）：}] 由条件得 $a=1$，周期 $T=2$。
			\par\textbf{理由：} 二级结论。

		\item[\textbf{第二步（转化自变量）：}] 由周期 $2$ 得 $f(2025)=f(1)$，$f(2026)=f(0)$。
			\par\textbf{理由：} $2025=2\times1012+1$，$2026=2\times1013$。

		\item[\textbf{第三步（利用条件求 $f(1)$）：}] 在条件中令 $x=0$ 得 $f(1)=-f(0)$，由 $f(0)=1$ 知 $f(1)=-1$。
			\par\textbf{理由：} 代入 $x=0$ 到 $f(x+1)=-f(x)$。

		\item[\textbf{第四步（求和）：}] $f(2025)+f(2026)=f(1)+f(0)$，代入 $f(1)=-1$，$f(0)=1$ 得和为 $0$。
			\par\textbf{理由：} 代入已知数值。
	\end{description}

	\textbf{\textcolor{CExample}{关键结论：}}
	\textbf{周期为 $2$，$f(1)=-1$，和为 $0$。}
\end{examplebox}
