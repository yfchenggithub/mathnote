\begin{statementbox}

	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（函数方程）：}}] 对任意 $x\in\mathbb{R}$，满足 $f(x+a)=-f(x)$，其中 $a\neq0$ 为常数。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（周期性质）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			函数以 $2a$ 为周期，每增加 $2a$ 函数值重复。
			\[
				f(x+2a)=f(x) \quad (\forall x\in\mathbb{R}).
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（对称性质）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			函数图像关于点 $(\frac{a}{2},0)$ 中心对称。
			\[
				f\!\left(\frac{a}{2}+x\right) = -f\!\left(\frac{a}{2}-x\right) \quad (\forall x\in\mathbb{R}).
			\]
	\end{description}

\end{statementbox}
