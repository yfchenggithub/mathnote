\begin{trapbox}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点一（周期误判）}}] 认为 $T=a$ 是周期，因为看到 $f(x+a)=-f(x)$ 就类比 $f(x+a)=f(x)$。
		\item[\textbf{\textcolor{CTrap}{正确理解（周期为2a）：}}] 必须满足 $f(x+T)=f(x)$；由条件可得 $f(x+2a)=f(x)$，故 $T=2a$。
		\item[\textbf{\textcolor{CTrap}{错因分析（忽略负号）：}}] 忽略了负号的影响，直接套用了正号条件下的结论。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点二（定义域要求）}}] 认为只要有一个 $x$ 满足 $f(x+a)=-f(x)$ 就能得出周期。
		\item[\textbf{\textcolor{CTrap}{正确理解（全局成立）：}}] 条件必须对定义域内全体 $x$ 恒成立，且 $a\neq0$。
		\item[\textbf{\textcolor{CTrap}{错因分析（条件不完整）：}}] 周期定义要求对任意 $x$，否则不能保证周期性重复。
	\end{description}

	\medskip
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CTrap}{易错点三（符号错误）}}] 在推导中写 $f(x+2a)=f(x+a+a)=f(x+a)-f(x+a)$ 等混乱。
		\item[\textbf{\textcolor{CTrap}{正确理解（正确推导）：}}]
			\[
				\begin{aligned}
					f(x+2a) &= f((x+a)+a) \\
					&= -f(x+a) \\
					&= f(x).
			\end{aligned}
			\] 。
		\item[\textbf{\textcolor{CTrap}{错因分析（代入失误）：}}] 对 $f(x+a)$ 整体作为自变量不清晰，代入时产生符号错误。
	\end{description}
\end{trapbox}
