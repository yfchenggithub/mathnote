\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}

	\textbf{若 $f(x+a)=-f(x)$（$a\neq0$），则 $f(x+2a)=f(x)$，即 $2a$ 是周期。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件 1（方程条件）：} $f(x+a)=-f(x)$ 对任意 $x$ 成立。
		\item \textbf{条件 2（非零常数）：} $a$ 是非零常数。
		\item \textbf{条件 3（定义域要求）：} $f$ 的定义域在加法下封闭（通常为 $\mathbb{R}$）。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{易错点（周期误判）：} 容易误以为周期是 $a$。
		\item \textbf{检查项（定义域验证）：} 检验条件对定义域内所有 $x$ 成立。
	\end{itemize}
\end{summarybox}
