\textbf{结论名称：} 由 \( f(x+a) = -f(x) \) 得周期 \( T = 2a \)

\textbf{适用条件：} 函数 \( f(x) \) 在 \( \mathbb{R} \) 上有定义，\( a \neq 0 \) 为常数，且对定义域内任意 \( x \) 满足恒等式 \( f(x+a) = -f(x) \)。

\textbf{变量说明：} \( a \) 为已知非零常数（常为正数），\( T \) 为函数的一个周期，\( x \) 为定义域内任意自变量。

\textbf{核心公式：}
\[
\boxed{f(x+a) = -f(x) \;\Longrightarrow\; f(x+2a) = f(x), \text{即 } T = 2a}
\]

\textbf{使用场景：} 高考题中快速从函数半周期对称关系识别周期，进而求函数值、画图像或判断周期性质。

\textbf{简单例子：} 已知 \( f(x) \) 满足 \( f(x+1) = -f(x) \)，且 \( f(0) = 3 \)，求 \( f(10) \)。

解：由结论得周期 \( T = 2 \times 1 = 2 \)，故
\[
f(10) = f(0 + 5 \times 2) = f(0) = 3.
\]