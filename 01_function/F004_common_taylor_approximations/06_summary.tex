\begin{summarybox}
	\textbf{\textcolor{CSummary}{一句话核心}}
	\textbf{当 $x\to0$ 时，常见函数可用关于 $x$ 的多项式近似；系数由函数在 $0$ 处的导数决定，误差由余项阶数控制。}

	\medskip
	\textbf{\textcolor{CSummary}{使用条件}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{条件1（小量条件）：} 自变量 $x$ 应接近 $0$，$|x|$ 越小，低阶近似通常越可靠。
		\item \textbf{条件2（弧度制）：} 涉及 $\sin x,\cos x,\tan x$ 时，$x$ 必须用弧度制表示。
		\item \textbf{条件3（定义域条件）：} 使用 $\ln(1+x)$ 时需保证 $1+x>0$；使用 $\tan x$ 时需保证 $\cos x\ne0$。
		\item \textbf{条件4（可导条件）：} 一般函数使用泰勒展开时，需要在 $x=0$ 附近有足够高阶导数。
	\end{itemize}

	\medskip
	\textbf{\textcolor{CSummary}{关键提醒}}
	\begin{itemize}[leftmargin=*, itemsep=0.5em, topsep=0.4em]
		\item \textbf{范围限制：} 不存在统一的“绝对安全阈值”，应结合 $|x|$ 大小、保留阶数和题目精度要求判断。
		\item \textbf{项数匹配：} 求极限时，应展开到能判断分子首个非零项的阶数为止。
		\item \textbf{余项意识：} 正式推导中尽量写成 $O(x^m)$ 或 $o(x^m)$，不要只依赖 $\approx$。
		\item \textbf{公式记忆：} $\sin x$ 的 $x^3$ 项为负，$\cos x$ 有常数项 $1$，这是最常见的符号错误。
	\end{itemize}
\end{summarybox}