\begin{statementbox}
	\textbf{\textcolor{CStatement}{条件}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.35em]
		\item[\textbf{\textcolor{CStatement}{条件 1（底数真数限制）：}}] $a,c>0$ 且 $a,c\neq 1$；$b,M,N>0$；$m\in\mathbb{R}$。
	\end{description}

	\textbf{\textcolor{CStatement}{结论}}
	\begin{description}[style=nextline, leftmargin=2.8em, labelsep=0.5em, itemsep=0.45em]
		\item[\textbf{\textcolor{CStatement}{结论一（积的对数）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			同底两数相乘，对数等于对数相加。
			\[
				\log_a(MN)=\log_a M+\log_a N.
			\]

		\item[\textbf{\textcolor{CStatement}{结论二（商的对数）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			同底两数相除，对数等于对数相减。
			\[
				\log_a\frac{M}{N}=\log_a M-\log_a N.
			\]

		\item[\textbf{\textcolor{CStatement}{结论三（幂的对数）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			数乘方取对数，指数提前成系数。
			\[
				\log_a M^m=m\log_a M.
			\]

		\item[\textbf{\textcolor{CStatement}{推广结论（换底公式）：}}]
			\textbf{\textcolor{CStatement}{直观描述：}}
			对数可换成其他底数，分子是目标对数，分母是底数对数。
			\[
				\log_a b=\frac{\log_c b}{\log_c a}.
			\]
	\end{description}

\end{statementbox}
